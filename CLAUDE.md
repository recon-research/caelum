# CLAUDE.md — Caelum

Caelum is an open-source, **US-origin-clean** Angular 22 component library — Angular Material + CDK with PrimeNG-level breadth, density, and parity — that teams leaving PrimeNG can adopt component-by-component the way they used PrimeNG. Built and tested almost entirely by Claude Code, on autopilot, with the human deferring on architecture and ambition calls. The library's canonical example throughout the docs is **Forge**, a demo admin console.

## Status — *the onboarding & compaction anchor; keep it current*

> The 10-line summary of "where are we" (ownership table below). Detail lives in [`docs/ROADMAP.md`](docs/ROADMAP.md) and the issue tracker — never here.

**As of:** 2026-07-07 · main @ `3255f24` · **repo LIVE + protected** — [github.com/recon-research/caelum](https://github.com/recon-research/caelum) · **Phase:** 2 · **Milestone:** **M0 ☑** · **M1 ☑ Composed — exit criterion MET** (`cae-multi-select` #135, `cae-table` #141, `cae-split-button` #148, `cae-menubar` #153, `cae-context-menu` #157, `cae-tab-menu` #164 + `cae-confirm` #101; every one 4-lens-reviewed) · **M2 ◐ Adapters — grid track ☑ EXIT CRITERION MET**: **#168** (grid public-API shape; provisional) · **#169 ☑** provenance sign-off + D-07 ratified (PR #172, TanStack PASS) · **#170 ☑** neutral grid interface + `cae-data-grid` + client default (PR #180, 4-lens-reviewed) · **#171 ☑** TanStack adapter behind the *identical* port + at-scale Forge screen = the **isolation proof** (PR #183, 4-lens-reviewed w/ leakage lens) · **#176 ☑** server-side engine (`ServerGridAdapter` — the lazy/remote `dataRequest`/`applyServerResult` seam, a **3rd engine** behind the identical port + remote-backed Forge screen; PR #187, 4-lens-reviewed); **editor (D-09 Lexical) next in M2**, **charts (D-14) deferred**. **CI:** `full` + blocking, main green (required checks: `static gates` + `heavy matrix gate`). Full slice index → [`docs/ROADMAP.md`](docs/ROADMAP.md); recipe + gotchas → [`docs/PATTERNS.md`](docs/PATTERNS.md).

**Done — M2 Adapters** (latest first): **pager/sort disable-while-loading + busy slot #192** (PR #199 — the coherent completion of #188's loading story, from the #188 review. While the consumer-owned `[loading]` is true, `cae-data-grid` now makes its **own** sort headers + pager Prev/Next **`aria-disabled`** (not native `disabled`) and **no-ops their handlers** — closing the **overlapping-`dataRequest` footgun** at the source (a Prev/Next or sort *during* a fetch could otherwise emit a second, racing request). **`aria-disabled` on purpose**: the controls stay focusable, so a keyboard user's focus is **never dropped to `<body>`** when a fetch starts — native `disabled` would strand it, the very #189 boundary problem. Adds an optional **`[caeDataGridLoading]`** projection slot: a **decorative** (`aria-hidden`, `pointer-events:none`) spinner/skeleton rendered over the grid only while loading (the `role=status` region still owns the announcement) — **no default ships** (library stays dep- + motion-free); **Forge** dogfoods a pure-CSS spinner (motion in the consumer, `prefers-reduced-motion` honoured). Client + TanStack paths **byte-identical** (loading never set there). **4-lens review: 0 majors** (none raised/survived), **3 minors all applied** — slot renamed `caeGridBusy`→`caeDataGridLoading` (element-consistent per the `cae-toolbar → caeToolbarStart/End` precedent + vocab-consistent with `[loading]`/`loadingMessage`, aligned before external adoption locks it); slot documented **decorative-only** (a focusable control inside the `aria-hidden` overlay = **WCAG 4.1.2** violation — cancel/retry belongs *outside* the grid, as Forge's Retry is). caelum-grid budget **20480→24576 B** (measured 19276 gzip; the +4096 ratchet for template + expanded JSDoc). Forge initial **879.67 kB < 890** (#142 untouched). **469 tests** (415 caelum + 54 forge).) · **pager focus-on-disable #189** (PR #197 — a11y: `cae-data-grid`'s pager dropped keyboard focus to `<body>` when Prev/Next self-disabled at the first/last page. Now focus moves to the still-enabled sibling — but **only when the pressed button held focus** (Safari/Firefox don't focus a clicked button, and a programmatic click never does, so an unconditional move would *steal* focus — WCAG 3.2.x), deferred via **`afterNextRender`** (the sibling still carries the previous page's `disabled` attr at click time — the `[disabled]` binding updates in the pending CD, so a sync focus is a no-op; this would fail in a real browser too, caught by an attached-fixture test). **2-lens focused review** (a11y + correctness, proportionate to a ~30-line internal change): **both independently confirmed the focus-theft major** (fixed) + narrowed the docstring (async server total-shrink disable is #190). Internal a11y only — no public API change. **464 tests** (410 caelum + 54 forge); no-steal regression test locks the guard. caelum-grid 18575/20480 B.) · **grid loading/busy state #188** (PR #195 — from the #176 review. `cae-data-grid` gains a **consumer-owned `[loading]` + `[loadingMessage]`** (mirroring p-table's `[loading]` **input contract**): while loading, `aria-busy="true"` on the **row viewport** (not the whole `role=table`, so the sibling `role=status` live region still announces) holds the now-stale rows, and the status region shows the loading message **instead of** the empty state → an in-flight fetch reads as *loading*, not *empty* ("0 rows"). **Consumer-owned, not auto-derived**: only the consumer knows a fetch is in flight (it owns the async behind the seam), so it can never get stuck and clears in a `.finally` on success **or** failure; the client + TanStack engines never set it → **byte-identical**. Non-blocking (pager/sort stay live — disable-while-loading + spinner slot is **#192**). **Forge** `orders-grid-demo` binds `[loading]` (starts true — fetches on mount) + adds an on-demand `role=alert` error banner + Retry (dogfoods `cae-button`) proving a failed fetch never strands loading. **4-lens review: 0 surviving majors**, 5 cheap-correct minors applied (dropped a row-dim for WCAG 1.4.3 contrast; `role=alert` wraps only the message per APG; softened p-table wording to the input contract; scoped the demo docstring); enhancements filed **#192** (disable-while-loading + spinner), **#193** (auto initial-load empty-suppress, relates #190), **#194** (first-load announce + banner focus, relates #189). **No new dep**; port + all 3 engines untouched. caelum-grid 20480 B unchanged (17421 gzip). Forge initial **879.67 kB < 890** (#142 untouched — `cae-button` already eager). **461 tests** (407 caelum + 54 forge).) · **server-side grid engine #176** (PR #187 — the grid's **lazy/remote seam**, now a **third engine behind the *identical* `CaeGridAdapter` port** (client #170, TanStack #171, server #176 — no consumer change). #170 typed the seam; #176 wires it. **`projects/caelum/grid/server-grid-adapter.ts`** (dependency-free, imports **no** grid library — under the base fence, not the dot-file): `ServerGridAdapter<T>` holds **no full set** — it emits `dataRequest` on every sort/page change (the p-table `onLazyLoad` analogue, Book 13 §3.4) and renders exactly the slice pushed via `applyServerResult`; page clamping, sort-resets-to-page-0 (port contract), page-global row ids (`page*pageSize+index`, unique across pages). **`dataRequest` reads the RAW `_page`** (not the clamped `page`) so `applyServerResult` — touching only `_slice`/`_total` — can never recompute it → **no redundant re-fetch** (the key correctness invariant). `provideServerGrid()`/`serverGridAdapterFactory`. **`cae-data-grid` gained a `[total]` input** (p-table `[totalRecords]`): **mode is single-sourced from the ENGINE** (`adapter.dataRequest() !== null`), NOT the input — the client engine **ignores** `[total]` (client path byte-identical) + dev-warns; a server engine uses it (missing → page-length fallback + dev-warn). Export scope is per-engine (server = current page only; full-fetch export #177). **Forge**: `@defer`'d `app-orders-grid-demo` drives a **4800-row "server"** (in-memory dataset behind a `Promise` = the round-trip); the grid holds **one 25-row page**, emits `(dataRequest)`, fetches each slice and pushes `[data]`/`[total]` back — own **3.19 kB lazy chunk** off eager, **14th @defer block**, Forge initial **879.64 kB < 890** (#142 untouched). caelum-grid budget **16384→20480 B** (measured 16760 gzip = adapter source + JSDoc, engine-free). Provenance **unaffected** (no new dep). **4-lens review** (api/correctness/a11y/leakage/over-eng): **2 surviving majors fixed** — (1) mode was keyed off `[total]` not the engine → a client consumer binding `[total]` silently lost client sort/paginate → re-keyed on the engine + dev guards + regression test; (2) `exportRows()` over-promised "full dataset" for the server engine → docs corrected + dev-warn. Cheap-correct minors applied (page-global ids, `pageSize:0` doc, stale-"followup" JSDoc, de-noised Forge live-region); enhancements filed **#188** (loading/busy + `aria-busy`), **#189** (pager focus-on-disable), **#190** (total-shrink/out-of-order/datum-stable-id seam robustness). **454 tests** (402 caelum + 52 forge).) · **TanStack grid adapter #171** (PR #183 — the **M2 grid EXIT CRITERION**: `@tanstack/table-core@8.21.3` (headless zero-dep core **only**, not `@tanstack/angular-table`) admitted behind the **identical** `CaeGridAdapter` port from #170, so `cae-data-grid` + `data-grid.spec.ts` are **unchanged** — the DI swap IS the isolation proof (D-03; Book 13 §3.2). **`projects/caelum/grid/grid.adapter.ts`** (the ONE file allowed to import `@tanstack/*`): `TanStackGridAdapter<T>` bridges table-core's imperative memoized store (`createTable` + `getCoreRowModel`/`getSortedRowModel`/`getPaginationRowModel`) into the port's **signals** via `setOptions`-in-a-computed; behaviourally interchangeable with `ClientGridAdapter` — numeric stable ids, page clamping, sort-resets-to-page-0, and — via the **shared `grid-sort.ts` `compareValues` comparator** + shared `grid-csv.ts` writer — **identical `viewRows` order + byte-identical CSV** (the 4-lens major: table-core's default `auto` collation diverged from the client's `localeCompare` for string columns → fixed by one shared comparator both engines sort by; locked by a string-collation parity test). **`provideTanStackGrid()`/`tanStackGridAdapterFactory`** — a **separate** provider from client `provideCaelumGrid()` on purpose (a runtime-flag switch can't tree-shake a static import); table-core left **external** in the fesm + declared an **optional peerDependency** → client-default consumers ship **zero** engine bytes (consumer tree-shake guard #182). **ESLint fence pinned** to `grid.adapter.ts` (dot); base fence still covers `projects/caelum/grid/**` so the port `grid-adapter.ts` (hyphen) + all else stay 3p-free (verified: static + dynamic `@tanstack` imports error everywhere but the dot-file). **Forge**: the Activity log demo is now its own `@defer`'d `app-activity-grid-demo` that **locally** provides `CAE_GRID`=TanStack and drives **5000 rows** → table-core rides the lazy chunk, **off the eager bundle** (Forge initial **879.50 kB < 890**, verified absent from eager `main`; slightly *lower* than #170's 881.51 as the demo's data/logic moved lazy). caelum-grid budget **11264→16384 B** (measured 13860 gzip = adapter source + JSDoc, engine external — not the illustrative ~40 kB since table-core is not inlined). provenance GREEN (table-core: MIT, dependency-free, US; `shipped:conditional`). **4-lens review** (leakage lens): 1 surviving major (sort-collation parity — fixed), 6 minors all applied (enableSorting drop for non-sortable-column parity, numeric ids, `shipped:conditional`, app-level engine-identity assertion, Export-CSV end-to-end test, size-note precision + #182). **433 tests** (385 caelum + 48 forge).) · **cae-data-grid #170** (PR #180 — the **first component over a neutral, engine-swappable interface**; the M2 step-up from M1 wrappers. Own entry point `caelum/grid`: vendor-free `Cae*` value types (`CaeColumn<T>` with a `value(row)` accessor — a superset of `CaeTableColumn.key`; `CaeSort`, `CaeRow<T>`, `CaeGridDataRequest`, `CaeGridExportFormat`), the **`CaeGridAdapter<T>` abstract engine port** + `CAE_GRID` token + `CaeGridAdapterFactory` + `provideCaelumGrid()`, a **dependency-free `ClientGridAdapter`** default (signal-projected client sort + client pagination + RFC-4180 CSV export), and **`cae-data-grid`** rendering its **own DOM** via `cdk-virtual-scroll` (standalone `CdkVirtualScrollViewport`/`CdkFixedSizeVirtualScroll`/`CdkVirtualForOf` imports — the NgModule leaks eager), token-only theming, inputs ergonomically mirroring `cae-table` (`[columns]`/`[data]`/`caption`/`[paginated]`/`[pageSize]`=10/`sortActive`/`sortDirection`). **No 3p in #170** — the swap to `@tanstack/table-core` behind the *identical* port is #171 (the isolation proof). **a11y: `role="table"`** (not `grid` — v1 cells are read-only text with no cell-cursor, the correct ARIA table pattern + p-table parity; becomes `grid` when arrow-key cell nav lands #175) named via **`aria-labelledby`** → the visible caption (`role=table` is not name-from-content — the surviving-major fix), `aria-sort` tri-state header buttons, `aria-rowcount`/`rowindex`/`colindex` for the virtualized partial render, persistent `role=status` empty region. Public `exportRows()` passthrough + a live Forge "Export CSV" button (via a **template ref, not `viewChild(CaeDataGrid)`** — a viewChild locator is an eager value ref that de-defers the grid). `@defer`'d Forge "Activity log" demo drives a live table over ~480 rows (13th defer block); own **~10 kB / 3.1 kB-gzip lazy chunk**; **Forge initial 881.51 kB** — warn budget raised **875→890** (`decision` #142; cdk/scrolling is eager-anchored by cdk/overlay so virtual-scroll adds ~18 kB eager, one-time; content-visibility alternative #178). **4-lens review**: 2 surviving majors (same defect — caption named nothing — fixed), 1 refuted (server-seam asymmetry, #176), 11 minors triaged (public export + demo, role=table, pageSize 10, dev-mode `validateConfig`, sort-resets-page-0 port contract). caelum-grid budget 9691/11264 B. Follow-ups **#174** (M4 virtual-scroll verify) / **#175** (grid keyboard) / **#176** (server engine) / **#177** (parity extras) / **#178** (content-visibility renderer) / **#179** (MatGridAdapter fallback).)

**Done — breadth** (independent of the M2 track): **cae-chip-set `[emptyFocusTarget]` #202** (PR #207 — the optional **empty-set focus hook** completing #84's a11y story: when the **last** chip is removed the set has no in-set target so focus fell to `<body>`; bind `[emptyFocusTarget]` (an `HTMLElement` / `ElementRef` / `viewChild()` result) and the set lands focus there after the emptying render. **Focus-steal-gated** (WCAG 3.2.x, the #189 principle): moves focus **only when the removal held focus inside the set** (a keyboard / focusing-pointer remove) — a non-focusing pointer × (Safari/FF) or a *programmatic* `[items]` clear leaves focus untouched; deferred via `afterNextRender` (the chip is destroyed in the pending CD). **Unset ⇒ byte-identical** to before. **Forge dogfoods it** — the hand-rolled `removeTag` empty-case guard + `tagsStatusRegion` viewChild **DELETED**. **4-lens review: 1 major raised → 0 surviving** (non-focusable-target dev-warn refuted 2/2 as fail-safe + documented → filed **#206**); 4 distinct minors all applied incl. a **guard-regression test empirically proven to fail iff `if (!heldFocus) return;` is deleted** (the original tests didn't catch it), a precise gate JSDoc (programmatic-clear is safe *by construction*, not the gate), and sync-drop + live-region-double-announce caveats; robust async-destruction focus filed **#205**. caelum-chip-set 5418/6656 B; **no new dep**; Forge initial **879.09 kB < 890** (#142 untouched). **486 tests** (431 caelum + 55 forge).) · **cae-chip-set #84** (PR #203 — the **managed removable-chip list**, the multi-chip companion to standalone `cae-chip` #83. Own entry point `caelum/chip-set`: **data-driven** (generic `[items]<T>` + a `[label]` accessor; **adapter-isolated** — `(removed)` emits a vendor-neutral `CaeChipRemoveEvent<T>`, no Material type in the public API), wrapping **`mat-chip-grid` + `mat-chip-row`** (chosen from the **verified Material source**: a plain `mat-chip-set`+`mat-chip` **drops focus to `<body>`** on removal because `MatChip.focus()` no-ops without a primary action; `mat-chip-row` **has** one, so the **focus-redirect-on-removal** lands — the reason the component exists). Grid a11y (`role=grid/row/gridcell`), auto-distinct `"Remove <label>"` names, dev `validateConfig` (**throws** on duplicate items — `track item` is required for the redirect identity; **warns** on an object item with no `[label]`). **Forge dogfoods it**: the hand-rolled tag row (`role=list` of `cae-chip` + a manual focus-move effect) is **replaced** and that effect **DELETED** — the set redirects focus itself; only a **targeted empty-case guard** remains (focus → status region when the last tag goes, the documented consumer responsibility). **4-lens review: 0 surviving majors**; 2 refuted majors (last-chip focus → `<body>`) **confirmed empirically** + handled (docstring narrowed to "while a sibling remains", Forge guard, contract test, component-level hook filed **#202**); 6 minors resolved (dev `validateConfig`; **`removable` dropped** YAGNI — a non-removable rove-but-can't-act `role=grid`; **`removeAriaLabel` kept** as the i18n hook; overclaim narrowed; per-item metadata filed **#201**). caelum-chip-set 4110/5120 B; **no new dep** (MatChips in-tree via cae-chip); Forge initial **879.21 kB < 890** (#142 untouched). **483 tests** (428 caelum + 55 forge).)

**Done — M1 Composed** (latest first): **cae-tab-menu #164** (PR #166 — the **last canonical M1 slice; M1 exit criterion now MET**. The Direct wrapper over Material's `mat-tab-nav-bar` + `mat-tab-link` (`p-tabMenu` → `cae-tab-menu`): a horizontal, tab-styled **navigation/selection** bar with a Material ink-bar + full roving keyboard (Left/Right/Home/End+wrap; Enter/Space activate). **Distinct from `cae-tabs`** (`mat-tab-group` content tabs) — a tab-menu is a bar of links that *select* an item; the active item's content is the consumer's, **projected into the wrapper** and swapped as the value changes. v1 = **manual-`active` mode** (first-class parity — `mat-tab-nav-bar`'s `active` is manual by design): data-driven `[items]` (`CaeTabMenuItem<TValue = string> { label; value; disabled? }`), two-way **`[(activeValue)]`** (a `model` → a click highlights immediately even uncontrolled), `ariaLabel`, opt-in `panelTabIndex`, `(itemSelect)`. The wrapper **owns the `mat-tab-nav-panel` internally** (Material *requires* `[tabPanel]` — its nav bar throws without one) and projects `<ng-content>` in → the full **ARIA tabs pattern** (role tablist/tab/tabpanel, aria-selected, aria-controls) is wired automatically and the consumer never touches a Material type (adapter isolation). **Selection is VALUE-based**, not p-tabMenu's reference-based `activeItem` (COMPARISON note). Own entry point `caelum/tab-menu` (3277 B gzip / 3968); `MatTabsModule` already in-tree via `cae-tabs` → no new dep; `@defer`'d Forge "Workspace sections" demo swaps a projected region (12th defer block); Forge initial **862.46 kB < 875** so **`decision` #142 untouched** (the ~3 kB rise is the expected shared-`MatTabsModule` hoist across two deferred tab chunks; `CaeTabMenu` verified deferred, not eager). **4-lens review: 0 majors, 5 minors all applied** — opt-in `panelTabIndex` (a11y: keyboard-focus/scroll a static panel, mirrors `mat-tab-group` `contentTabIndex`), documented `value` uniqueness contract, hardened spec to assert `itemSelect` fires **exactly once** + Space coverage, COMPARISON value-based note, budget 3456→3968. Follow-ups **#165** (router-linked mode + M4 verify, icons, rich/responsive/RTL/closable — **Forge has no router** so router mode is deferred). Verified the `MatTabNav`/`MatTabLink`/`MatTabNavPanel` API against the real Material source (keyboard bridge, the required-`tabPanel` throw), cited no fabricated `Book §`.) · **cae-context-menu #157** (PR #159 — the **first wrapper over an *unstyled* CDK primitive**: a right-click context menu over the **CDK Menu** family (`@angular/cdk/menu`, `cdkContextMenuTriggerFor`) — the right-click path `MatMenu` cannot do (reach-for ladder drops below Material, Book 09 §3.4 / Book 05 §3.5). `<cae-context-menu [items] (itemSelect)>` projects the target; a declarative `[cdkContextMenuTriggerFor]="panel"` **template-ref** opens a data-driven `cdkMenu` — **no TS wiring**; reuses `CaeMenuItem` (re-exported). CDK Menu is **behaviour-only** → the wrapper **token-styles the overlay panel** with **`ViewEncapsulation.None`** + namespaced `cae-context-menu__*` classes (`--mat-sys-surface-container-high` + `--cae-elevation-2`; item state-layers via `color-mix`). Empty items **disables the trigger** (`cdkContextMenuDisabled`) so right-click falls through to the native menu; owned `cdkMenuItem` carry `type="button"` (#148); `CdkMenuItem` disabled reflects as `.cdk-menu-item-disabled`/`aria-disabled` **not** native `disabled`. Own **6 kB lazy chunk** (@angular/cdk/menu new to Forge, kept off eager by `@defer`), Forge initial ~860 kB < 875 so **`decision` #142 untouched**. 4-lens review fixed **1 confirmed major** (all 3 a11y/api/parity lenses, 0/2 refuted): the `display:contents` target was **not keyboard-focusable**, so the Menu key/Shift+F10 could never open it while the docstring claimed keyboard access → wrapper now makes the target a **focusable region by default** (`[tabindex]="items().length?0:-1"` + `--cae-focus-ring` on a `display:block` box); minors — real focus ring on **every** roved item (CDK roves onto disabled — `skipPredicate` off), library focus-ring token reuse, corrected roving comment, size budget 3072→3456. Follow-ups **#158** (submenus/rich items/groups/global-open/per-target data + M4 real-browser verify). **PATTERNS §5c**: *when you wrap a CDK trigger, the wrapper owns the trigger's focusability, not just the panel's.*) · **cae-menubar #153** (PR #154 — the flagship composed widget: a horizontal application menu bar, a `MatToolbar` shell (`role="menubar"`) of owned `<button matButton>` triggers each embedding a whole `cae-menu` (§5b *embed-the-wrapper*); **bar roving is a CDK a11y `FocusKeyManager`** (D-13 rung 2) over the triggers — roving tabindex, Left/Right/Home/End + typeahead, skip-disabled; MatMenu owns each panel. `CaeMenubarItem { label; items: CaeMenuItem[]; disabled? }`, own entry point re-exporting `CaeMenuItem`. **Composed-over-composed** — a deferred Forge "command bar" demo drives a live command log; own **3 kB lazy chunk**, Forge initial ~857 kB < 875 so **`decision` #142 untouched**. 4-lens review fixed **2 majors**: Down/Up *roved* the bar instead of opening the panel while the docs/demo promised "Down opens" → `withVerticalOrientation(false)` + intercept Down/Up to `open()` the active menu; the internal `MenubarTriggerItem` roving directive leaked into the public API (`export *`) → name-export only the public symbols; minors — `FocusKeyManager.destroy()` on teardown, dropped a dead `booleanAttribute` (YAGNI), empty-items groups treated as disabled, corrected a false defer rationale. Follow-ups **#155** (submenus, rich items, responsive collapse, RTL roving, disabled-interactive)). · **cae-split-button #148** (PR #151 — primary command joined to a secondary-action dropdown, composed over two **owned** `MatButton` halves + an embedded `cae-menu` (data-driven `CaeMenuItem[]`, the `caeMenuTriggerFor` a11y seam, `(itemSelect)`); **composed-over-composed** — Forge's "New member" demo grows a `members` signal that drives the `cae-table`. 4-lens review fixed **1 confirmed major** (raised by all 4 lenses, 0/2 refuted): `matButton` sets no default `type`, so the owned `<button>`s defaulted to `type=submit` → would submit an enclosing `<form>` (toggle submits merely to open its menu) — fixed via hardcoded `type="button"` on the toggle + a `type` input on the primary; minors applied (required `label`, aria-label empty-guard, `primaryClick` emits the event, seamless outlined join, self-typing entry point). Own **2.4 kB lazy chunk**, Forge initial untouched (~843 kB) so **`decision` #142 unaffected**; follow-ups **#149** button-side extras / **#150** rich menu items). · **cae-table #141** (PR #146 — declarative `columns`/`data` config over `MatTable` + `MatSort` + `MatPaginator`, client-side sort + pagination via `MatTableDataSource` bridged from signals; projected `<caption>` name; **unconstrained generic `<T>`** so plain typed interfaces bind; persistent `role="status"` empty region; own entry point + `@defer`'d Forge demo (68 kB lazy chunk). 4-lens review fixed 3 majors — empty-state a11y announcement, `currentColor` fallback, initial-vs-reactive pageSize docs; follow-ups #143 cell templates / #144 sticky-expandable-selection / #145 server-side-filter-reorder; provisional Forge warn-budget 850→**875** kB, `decision` #142). · **cae-multi-select #135** (PR #136 — `string[]` CVA over `MatSelect[multiple]` + chip-summary trigger + opt-in in-panel filter; **generalized `CaeFormFieldControlBase` to `<T = string>`** via an `emptyValue()` override, string controls source-unchanged; 4-lens review defaulted `filterable` off pending accessible filter #138).

**Done — M0 Direct-component family** (full write-up in each issue/PR): cae-checkbox/switch naming seam #70 · cae-toolbar/badge #126 · cae-autocomplete #119 · cae-listbox #114 · cae-slider #109 · **D-15 dialog/toast** (cae-confirm #101 · cae-dialog #100 · cae-toast #96) · Forge @defer budget #85 · display primitives #88 · cae-chip #83 · accordion #77 · toggle/select-button #73 · cae-switch #68 · cae-button a11y forwarding (#36/#57/#58) · stepper linear #40 · `CaeFormFieldControlBase` #46 · validation-error forwarding #29/#47 · secondary entry points #28 · Direct batches 1–3 #5/#26/#27 · foundation #1–#6. Housekeeping: #7, #123, **#97** (PR #162 — relocated Forge's genuinely-global app-shell styles `.forge-bar*`/`.forge-help*`/`.forge-main`/`.forge-panel*`/`:host`→`app-root`/`code` from `app.scss` into the global `styles.scss`: `app.scss` compiled 7.36→5.39 kB, ~1.6 kB under the 7 kB `anyComponentStyle` warn; net-neutral on the initial bundle since App is the eager root; per-demo card styles stay in `app.scss`; the heavier per-section-sub-component split is filed as **#161** for if it ever creeps back).

**Next:** **M2 grid track is DONE** (#169/#170/#171 exit criterion; **#176 server engine** added the lazy/remote seam on top). The remaining M2 gap is the **editor (D-09 Lexical)**, a **cut-candidate** (`docs/ROADMAP.md` Cut order #1: heaviest gap, narrowest usage) and **charts (D-14) deferred** (team not using) — both gaps parked. **The real next move is a scope call** (surface as needed, don't guess): either (a) **pursue the editor track** — starting with its **own transitive-provenance sign-off** (the grid-track pattern: sign-off → neutral interface → adapter), or (b) **scope M2 to grid-only** (editor → M3/cut, charts deferred) and declare M2 done, advancing toward **M3 (build-S/M long tail)**. **Independent work available now regardless of that call:** the grid follow-up backlog — **#193**/**#194** (grid loading follow-ups from the #192/#188 line — auto initial-load empty-suppress; first-load announce + transient-banner focus), **#175** (grid keyboard/arrow-nav → `role=grid`), **#177** (parity extras), **#190** (server-seam robustness), **#182** (consumer tree-shake guard), **#174** (M4 virtual-scroll real-browser verify), **#178** (MatGridAdapter fallback), **#179** (content-visibility renderer) — or the cae-chip-set follow-ups **#201** (per-item removable/disabled + text-entry field + listbox) / **#202** (empty-set focus hook), or the M1 follow-up backlog (#165/#158/#155/#149/#150/#143/#144/#145/#137/#138/#139).

**Decisions:** `D-01..D-15` resolved (ARCHITECTURE Appendix A; **D-07 ratified 2026-07-05** via #169 provenance PASS); **3 open `decision`s** — **#185** M2 scope / next milestone (grid track done → pursue editor (D-09, cut-candidate) vs scope M2 grid-only and advance to M3; **provisional on advancing/backlog** — recommended default (a), silence ratifies) · **#142** Forge initial-bundle warn-budget (provisional 850→875→**890** kB, raised for #170's one-time virtual-scroll eager step; reversible; silence ratifies) · **#168** grid public-API shape (provisional on the library-grounded default; **#170 + #171 both shipped on that default** — inputs mirror cae-table, `[data]` not `[rows]`, render mechanism + engine not part of the API; the whole grid track landed on it; every grid PR carried `Provisional on #168`; silence past the window ratifies). Open issues: **55 total** (`gh issue list`) = the 3 decisions (#142, #168, #185) + **52 followups/debt/ideas** (grid track #169/#170/#171/**#176** + follow-ups **#188**/**#189**/**#192** all **closed**; incl. the grid family **#174/#175/#177/#178/#179 + #182** (tree-shake guard) + **#190** (from the #176 review) + **#193/#194** (from the #188 review; #192 closed) + the **cae-chip-set** follow-ups **#201** + **#205**/**#206** (from the #202 review; **#84**/**#202** closed) + **#165** tab-menu + #155/#158/#149/#150/#143/#144/#145/#137/#138/#139 + the M4 real-browser verify family #41/#79/#91/#107/#110/#128 + #8/#12/#15/#33/#48/#51/#54/#61/#72/#75/#78/#80/#90/#102/#161/#105/#111/#116/#117/#120/#127/#129/#133).

**Resume:** `main` @ `3255f24` · **M2 grid track ☑ COMPLETE + extended** (#169/#170/#171 exit criterion; **#176** server/lazy engine; **#188** consumer-owned `[loading]`/`aria-busy` state; **#189** pager focus-on-disable; **#192** disable-while-loading + `[caeDataGridLoading]` busy slot — all closed); **breadth: cae-chip-set #84** + **#202** (`[emptyFocusTarget]` empty-set focus hook, PR #207 — focus-steal-gated + `afterNextRender`, Forge dogfood) merged; follow-ups #201/#205/#206. **Next = a scope call** (surface, don't guess): pursue the **editor (D-09 Lexical)** track (its **own provenance sign-off first**, the grid-track pattern) — a **cut-candidate** (Cut order #1) — **or** scope M2 to grid-only (editor→M3/cut, charts deferred) and advance to **M3**. **Independent work now regardless:** the grid follow-up backlog (**#193**/**#194** grid loading follow-ups from the #192 line — auto empty-suppress; first-load announce + transient-banner focus, **#175** grid keyboard→`role=grid`, **#177** parity extras, **#190** server-seam robustness, **#182** tree-shake guard, **#174** M4 verify, **#178** MatGridAdapter, **#179** content-visibility) or the cae-chip-set follow-ups **#201/#205/#206**. Docs current, truth = tracker. **Flow** (details: [`PROJECT_CONVENTIONS.md`](PROJECT_CONVENTIONS.md) › Merge policy / Tracker): ticket-first → `slice/<n>-<slug>` branch → PR → green CI → merge (manual `--squash --delete-branch`; **repo auto-merge disabled**) → merge-time checkpoint; every public-API/a11y/adapter-boundary slice gets a **4-lens ultracode adversarial review** before merge. **Verify:** `PATH="$HOME/nodejs/bin:$PATH" npm ci && npm run build:lib && npx ng build forge && CI=true npx ng test caelum && CI=true npx ng test` (**486 pass — 431 caelum + 55 forge**; `ng test` alone runs Forge only → also run `ng test caelum`) · `python3 scripts/check_provenance.py` (GREEN) · `PATH="$HOME/nodejs/bin:$PATH" bash scripts/preflight.sh` (PASS 0-skipped — the PATH prefix is REQUIRED, else Node-dependent stages SKIP and it reports "5 skipped").

## Read these first (in order)

1. [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) — the shape, the invariants, and the decision log (`D-01..`, Appendix A).
2. [docs/ROADMAP.md](docs/ROADMAP.md) — milestones `M0..`, each with goal / exit-criterion / status; the live plan.
3. [PROJECT_CONVENTIONS.md](PROJECT_CONVENTIONS.md) — paths, commands, stack, tracker conventions. **Every skill reads this.**
4. [textbooks/AGENT_GUIDE.md](textbooks/AGENT_GUIDE.md) — the build loop for turning the library into shipped work.

## Source of truth — docs are caches

Truth order: **git/CI &gt; issue tracker &gt; docs &gt; chat memory.** On any mismatch, fix the doc to match reality *before* working — cheap now, poisonous later. **Single-home each fact** — policy here, project mechanics/config in `PROJECT_CONVENTIONS.md`, procedure in the owning skill, operating reference in `docs/`; every other mention is a one-line cross-link, never a second copy (a fact written twice drifts — `README.md` › the single-home rule). Carve-out: **ARCHITECTURE invariants / `D-NN` rows are commitments, not caches** — code contradicting one is a defect or a supersede-candidate (file `bug` or `decision`; never edit the log to match drift). *(The pre-repo carve-out — `PROJECT_BACKLOG.md` as the interim tracker — was retired at go-live 2026-07-01; the tracker is now GitHub Issues.)* Ownership (who updates what, when):

| Artifact | When it's written |
|---|---|
| **Status block** (above) | one line at **every merge** (the merge-time checkpoint); fully at `prepare_compaction`; verified by `onboard` |
| **docs/ROADMAP.md** | the moment a slice/milestone changes state |
| **docs/ARCHITECTURE.md** | only when a `D-NN` decision lands |
| **Issue tracker** | continuously — **defer = file now**, never "I'll note it later" |

## The reference library — use it

[`textbooks/`](textbooks/) is this project's RAG knowledge library (built per-domain; see [textbooks/LIBRARY_SEED.md](textbooks/LIBRARY_SEED.md)). When planning or implementing:

- Route topics via [`textbooks/MANIFEST.json`](textbooks/MANIFEST.json) (`topic_to_books`, `rag_hints`); follow the loop in [`textbooks/AGENT_GUIDE.md`](textbooks/AGENT_GUIDE.md).
- **Verify every `Book NN §X` citation against [`textbooks/SECTIONS.json`](textbooks/SECTIONS.json) before asserting it** — grep it, don't load it whole. The library is audited; don't invent sections.
- Pre-mortem with `textbooks/reference/ANTI_PATTERNS.md` + `SYMPTOMS.md`; resolve architectural forks with `DECISION_TREES.md`.
- **Frontier topics** live in [`research/`](research/) — survey notes (every claim a **real fetched URL + accessed date + tier**), pre-registered experiments (`EXP-NN`), and paper-style reports (`RR-NN`) that ground `D-NN` decisions like book sections do. **Cite as `research/notes/<file>.md` / `RR-NN`, never as a `Book §`** — different trust models; notes stale ~2 quarters. Discipline + audit: [research/README.md](research/README.md).

## How we work — the daily loop

This project runs on autopilot. Day-to-day, the human's input is mostly three moves:

### 1. "Welcome back, let's onboard and continue" → run [`onboard`](.claude/skills/onboard/SKILL.md)
Preflight → anchors → reconcile docs vs tracker → **read the decision answers** (issue comments) + digest → resume the [build loop](textbooks/AGENT_GUIDE.md). The skill owns the detail, including the edge cases (dirty tree, red main, dead `gh`).

### 2. "Let's prepare for compaction" → run [`prepare_compaction`](.claude/skills/prepare_compaction/SKILL.md)
Rewrite + stamp Status → **verify its claims against the tracker** → update ROADMAP/ARCHITECTURE → sweep deferrals into tickets → clean tree, pushed via the checkpoint path → emit a ready-to-paste `/compact` command. If merge-time checkpointing happened all along, this is verification, not archaeology. **The agent raises this proactively** — at a clean checkpoint (just merged, tree pushed, no in-flight slice) after a heavy session, it recommends compacting rather than letting the window force an unfocused auto-compaction, **and carries the ready-to-paste `/compact` command in that same recommendation** (not a bare "want me to prepare it?"); the skill owns the timing signals.

### 3. A decision is needed → surface a fork; don't guess, don't stall
Architecture and scope are ~1× leverage (the human's judgment). When a real fork appears:
- **File it as an issue labeled `decision`** (template provided): 2–4 options, each with its trade-off and the `Book NN §X` / `DECISION_TREES Dn` behind it, **recommended default first** (per the policy below).
- **Reversible fork?** Proceed **provisionally** on the recommended default and keep working — the issue states the objection window; every PR built on the default carries `Provisional on #NN`. The human overrules asynchronously by commenting; **silence past the window ratifies the default**.
- **Hard to reverse** (public API shape, data schema/migration, external commitments, money)? Don't guess: park the slice, pick the **next independent slice** from the tracker. **Nothing independent left?** Strengthen the blocked decision instead — `research_topic` the fork, spike on a throwaway branch (never merged) — then checkpoint and end the session cleanly. Don't invent roadmap scope to stay busy.
- Once the human decides (a comment on the issue *is* the decision): record it as `D-NN` in [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) Appendix A, reflect it in the ROADMAP, close the issue.

Other messages happen, but these three are the spine. Default to continuing the roadmap autonomously between them.

## Definition of done

Nothing is "done" until [`definition_of_done`](.claude/skills/definition_of_done/SKILL.md) passes its gates **with evidence** (output, not assertion): **build · tests · a11y parity (axe + keyboard) · token-only theming (no hardcoded design values) · adapter isolation (no 3p import outside its adapter) · provenance scan (US-origin + free, transitive tree) · liveness (it demonstrably does something end-to-end — inert hard-blocks, ambiguous soft-flags) · anti-patterns avoided · determinism (where required) · performance (profiled, not guessed) · milestone exit-criterion · backlog updated · no unticketed deferrals.** Skills live in [`.claude/skills/`](.claude/skills/).

## Working style — the policy the executing agent runs on

- **Ambitious destination, staged route.** Default to the most complete end-state, delivered through small, independently verifiable slices that each build *into* it. Prefer an easier-to-test intermediate **when it's a stepping stone, never as a scope cut**: a cheaper *first step* toward the same end-state is the default; a cheaper *final scope* requires an explicit `D-NN` decision. A slice is **coherent, not fragmented** — bundle tightly-coupled edits and the doc/ROADMAP updates they imply into one PR rather than splitting a single change across trivial PRs (`PROJECT_CONVENTIONS.md` › Right-sized slices).
- **Laziest sufficient code.** Ambition is about the *outcome*, not the line count — within a slice, write the least code that fully does the job. Before adding, climb the ladder and stop at the first rung that holds: does it need to exist at all (YAGNI), is it already in the codebase, the standard library, a native platform feature, an existing dependency, or a one-liner — only then write a minimal implementation. The best code is the code you never wrote. **Never** simplify away the non-negotiables, though: validation at trust boundaries, error / data-loss handling, security, accessibility, determinism where required, or anything the human explicitly asked for. And laziness *without first understanding the code it touches* is the dangerous kind — it ships a confident wrong fix. (`adversarial_review` carries the matching over-engineering lens; inspiration: the ponytail project.)
- **Ticket-first.** No slice without an issue; the branch and PR reference it. A TODO entering code must carry a ticket — `TODO(#NN)` — a naked TODO is a defect (gated by CI and `definition_of_done`).
- **Defer = file now.** The moment work is deferred or an idea worth keeping appears, file the issue (`track_followups`) — then continue. Promises in chat don't survive compaction; tickets do.
- **Checkpoint at every merge.** Update the Status line + ROADMAP state in the same breath as the merge. Compaction can then strike at any time and lose at most the in-flight slice.
- **One writing session per repo.** Concurrent sessions race on the Status line, ROADMAP state, and D-NN numbering. A second simultaneous session works read-only or in its own `git worktree` + branch — never two writers on one checkout.
- **Token discipline.** Grep the big indexes (`SECTIONS.json`) — never load them whole; `MANIFEST.json` is small by design: load it once, keep it resident. Read narrowly; delegate broad reads to read-only subagents and keep conclusions, not file dumps (cheap/fast models for mechanical sweeps, the strongest for adversarial lenses). Detail lives in skills (lazy-loaded), not in this file.
- **Follow the written system.** These docs and skills encode the judgment; prefer them over improvisation, and verify with evidence — never claim green without output. Don't re-litigate settled `D-NN` decisions. Never force-push or admin-merge — the gates exist to gate (`PROJECT_CONVENTIONS.md` › Merge policy).
- **Keep the library honest.** If you extend `textbooks/`, regenerate `SECTIONS.json` and run the audits (they exit non-zero; CI enforces them).
