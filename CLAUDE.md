# CLAUDE.md — Caelum

Caelum is an open-source, **US-origin-clean** Angular 22 component library — Angular Material + CDK with PrimeNG-level breadth, density, and parity — that teams leaving PrimeNG can adopt component-by-component the way they used PrimeNG. Built and tested almost entirely by Claude Code, on autopilot, with the human deferring on architecture and ambition calls. The library's canonical example throughout the docs is **Forge**, a demo admin console.

## Status — *the onboarding & compaction anchor; keep it current*

> The 10-line summary of "where are we" (ownership table below). Detail lives in [`docs/ROADMAP.md`](docs/ROADMAP.md) and the issue tracker — never here.

**As of:** 2026-07-02 · main @ `bb64d4d` · **repo LIVE** — [github.com/recon-research/caelum](https://github.com/recon-research/caelum) (public, `main` protected) · **Phase:** 0 — Foundation · **Milestone:** M0 ◐ (**M0-1 workspace DONE** PR #16; **M0-2 install + provenance scan DONE** #2 — **D-10 → D-11**, #21 closed; **M0-3 theming token bridge DONE** #3; **M0-4 provenance/adapter/size gates DONE** #4 — the M0-2 scan automated; **M0-5 Direct components batch 1 DONE** #5 → PR #30 — first `cae-*` components; **M0-6 configure_project — CI matrix + required checks DONE** #6; **Direct components batch 2 DONE** #26 — cae-radio/select/textarea/tabs/tooltip; **Direct components batch 3 DONE** #27 → PR #42 — cae-menu/stepper/tree, adversarially reviewed [8 findings fixed]; dialog/snackbar split to decision #39; **#28 per-component secondary entry points DONE** → PR #44 — split the monolith into tree-shakable `caelum/<name>` entry points [barrel 13.4 kB→742 B; `caelum/button` 1.7 kB], adversarially reviewed [0 MAJOR]; **#29 cae-input/textarea validation-error forwarding DONE** → PR #49 — MatFormFieldControl parity, adversarially reviewed [2 MAJOR + MINOR/NIT fixed]; **#47 cae-select error forwarding + radio/checkbox a11y hook DONE** → PR #52 — extends #29 to cae-select + the consumer-owned `ariaDescribedby` hook for the non-form-field controls, adversarially reviewed [3 MAJOR + MINOR/NIT fixed]; **#46 shared form-field-control base DONE** → PR #55 — extracted the byte-identical CVA + error-forwarding bridge from input/textarea/select into the abstract `CaeFormFieldControlBase` (new `caelum/form-field` entry point), adversarially reviewed [0 confirmed MAJOR; MINOR/NIT fixed]; **#36 cae-button tooltip-forwarding DONE** → PR #59 — the `tooltip`/`tooltipPosition` seam binds `MatTooltip` to cae-button's inner focusable `<button>` (keyboard/SR-accessible wrapper forwarding); menu-trigger forwarding split to #57, adversarially reviewed [0 BLOCKER/MAJOR; MINOR/NIT fixed]; **#40 cae-stepper linear (validity-gated) stepping DONE** → PR #62 — `cae-step` `stepControl` + `cae-stepper` `[linear]` with effect-driven `selectedIndex` reconciliation (a refused linear move re-emits so `[(selectedIndex)]` never desyncs); Forge adds a separate "Invite a teammate" linear demo, adversarially reviewed [reconciliation core SOLID; 2 MAJOR (2nd-formGroup viewChild ambiguity, invite-success focus/announce) + MINOR/NIT fixed, both MAJORs regression-tested]; **#57 cae-button menu-trigger forwarding DONE** → PR #64 — the sibling of #36's tooltip seam; a new `menuTriggerFor` input binds `MatMenuTrigger` to cae-button's inner focusable `<button>` (two-branch template — the trigger is opt-in so plain buttons carry no spurious `aria-expanded`); the panel crosses entry points via a type-only `CaeMenuPanelHost` seam in `caelum/shared` (no `caelum/button`→`caelum/menu` runtime dep), adversarially reviewed [0 MAJOR/BLOCKER; MINOR/NIT fixed]; **#58 cae-button `disabledInteractive` DONE** → PR #66 — an opt-in `disabledInteractive` input forwards `matButton`'s focusable-disabled mode to cae-button's inner `<button>` (drops the native `disabled` attr for `aria-disabled` so a disabled button stays focusable/hoverable/tabbable and can still surface its forwarded tooltip #36 / menu #57 — the `<p-button pTooltip disabled>` parity case); the action is NOT auto-suppressed (consumer guards `(click)`/`(ngSubmit)`; a bound `menuTriggerFor` stays openable → unbind to block); Forge's linear invite "Send invite" demos it (interactive-disabled + explanatory tooltip until a role is chosen), adversarially reviewed [0 BLOCKER/MAJOR; 1 MINOR + 3 NIT fixed — menu-trigger footgun doc, JSDoc guard-pattern reword, coercion spec strengthened, describedby comment]; **#68 cae-switch (Direct batch 4) DONE** → PR #69 — the first batch-4 Direct component: a thin `cae-switch` over `MatSlideToggle` (`p-toggleSwitch`/`p-inputSwitch` parity — COMPARISON row 42 → **`cae-switch`**, NOT `cae-slide-toggle`), reusing the cae-checkbox pattern verbatim (boolean CVA, not a `MatFormFieldControl` → the consumer-owned `ariaDescribedby` hook, no `<mat-error>`) plus a **`hideIcon`** passthrough; Forge binds it to a `notify` preference in the create-workspace form (a real CVA round-trip). Adversarially reviewed [4-lens: cva-forms/api-design/a11y/arch-packaging; 0 BLOCKER; **1 CONFIRMED MAJOR** — cae-switch always rendered Material's thumb glyph `p-toggleSwitch` never shows with no opt-out → `hideIcon` passthrough added (default `false` = faithful 1:1; a default-flip for pixel-parity is a separate decision); MINOR/NIT fixed — Forge narrative "batch 3"→batch 4, barrel comment; the `ariaLabel`/`ariaLabelledby` label-less seam split to **#70**]); **#73 cae-toggle-button + cae-select-button (Direct batch 4) DONE** → PR #74 — the `MatButtonToggle` pair (COMPARISON row 43): `p-togglebutton` → **`cae-toggle-button`** (boolean CVA over a STANDALONE `mat-button-toggle`; inner `<button role="button">` + **`aria-pressed`**, the WAI-ARIA toggle-button pattern; content-projected label) and `p-selectbutton` → **`cae-select-button`** (grouped CVA over `mat-button-toggle-group` with `@for` over `CaeSelectButtonOption[]`; single or **`multiple`** → value seam `string`|`string[]`; `vertical`; group named via `ariaLabel`/`ariaLabelledby` like cae-radio; `required` → `aria-required`; `hideSelectionIndicator`). **KEY:** `MatButtonToggle` (unlike checkbox/radio/slide-toggle) has NO `aria-describedby` input and its host is `role="presentation"`, so the consumer-owned describedby (#47) is forwarded onto the focusable inner `<button>`(s) via an **`afterRenderEffect`** (guaranteed post-render, reactive on options change). Forge adds a required `visibility` select-button + a boolean `pinned` toggle-button (Forge initial-budget warn 800→850 kB — `MatButtonToggleModule` is new to Forge). Adversarially reviewed [4-lens cva-forms/api-design/a11y/arch-packaging; 0 BLOCKER; **2 CONFIRMED MAJOR** (both cae-select-button: no `hideSelectionIndicator` opt-out for Material's selection checkmark — the #68 `hideIcon` precedent → added; no `required`/`aria-required` seam → added, cae-radio parity) + MINOR/NIT fixed (mode-aware `writeValue`, `effect`→`afterRenderEffect`, `multiple`-init-only doc); 0 refuted; on/off-label parity → **#75**, **#70** scope extended to both]; **#77 cae-accordion + cae-expansion-panel (Panel/layout Direct row) DONE** → PR #81 — the `MatExpansion` pair (COMPARISON row 89, Book 11): `p-accordion` → **`cae-accordion`** (a zero-logic container: `hostDirectives:[MatAccordion]` exposing `multiple`/`displayMode`/`hideToggle` + `<ng-content>`; **NO template re-stamping** — projected `cae-expansion-panel`s coordinate purely via DI: the inner `mat-expansion-panel`'s `inject(MAT_ACCORDION,{skipSelf:true})` walks up to the host through the declaration tree; single-expand = `UniqueSelectionDispatcher` keyed by the accordion's auto-unique id, and its auto-close **fires `expandedChange`** so `[(expanded)]` self-syncs with no reconciliation, unlike the linear stepper) and `p-accordionTab` → **`cae-expansion-panel`** (thin `mat-expansion-panel` wrapper: `title`/`description` header, `[(expanded)]`, `disabled`, `hideToggle`, `(opened)`/`(closed)`; native header a11y; not a form control → no CVA). Forge adds a single-expand FAQ (Forge 814→837 kB — `MatExpansionModule` new to Forge). Adversarially reviewed [4-lens di-coordination/a11y/api-design/arch-packaging; **0 BLOCKER, 0 MAJOR, 0 refuted**; MINOR/NIT applied — `multi`→**`multiple`** alias (PrimeNG + cae-select-button parity), inter-header arrow-key roving gap disclosed in JSDoc (WAI-ARIA-*optional*; → **#79** M4 real-browser verify), `expanded` controlled-binding footgun note, Book 01 §3.4→§3.3 citation fix; followups **#78** rich header / **#79** roving / **#80** parity extras (togglePosition/afterExpand/lazy body)]; **#83 cae-chip (data-display Direct row) DONE** → PR #86 — `p-chip` → **`cae-chip`** over a STANDALONE `MatChip` (new `caelum/chip`; MatChip defaults `role=null` + injects no chip-set → valid standalone): content-projected label, `removable` (default `false` = p-chip parity) → a remove `<button matChipRemove>` + `(removed)`, `disabled`, `removeAriaLabel`; the remove glyph is a **self-authored inline SVG "×"** (NOT `mat-icon` — Material Icons is a Google-CDN font Caelum avoids). Forge adds a removable tag row backed by a signal list (Forge warn budget 850→900 kB — `MatChipsModule` new to Forge, Forge nearing the 1mb ceiling → **#85**). Adversarially reviewed [4-lens a11y-focus/standalone-correctness/api-design/arch-packaging; **0 refuted**; **1 CONFIRMED BLOCKER** (the remove `<button matChipRemove>` rendered Material's default `tabindex="-1"` — only a `MatChipSet` FocusKeyManager promotes it, absent standalone → keyboard-inoperable, WCAG 2.1.1 fail; fixed with explicit `[tabIndex]="0"`, regression-tested since the specs only tested `.click()`) + **2 CONFIRMED MAJOR** (focus stranded on `<body>` + no announcement on removal → Forge live region + focus-move, `cae-chip` documents the standalone limitation, managed set with focus-redirect → **#84**; false "reachable by Tab" JSDoc → now true + honest); MINOR/NIT applied]; **#88 Direct display primitives (progress-bar/spinner/divider) DONE** → PR #89 — three thin NON-interactive Direct wrappers (Book 11, COMPARISON rows 141/142/94), each a tree-shakable entry point: **cae-progress-bar** (`p-progressbar`/MatProgressBar; value/bufferValue/mode + an **`ariaLabel`** seam since role=progressbar ships no accessible name), **cae-progress-spinner** (`p-progressspinner`/MatProgressSpinner; **defaults mode=`indeterminate`** — p-progressSpinner parity, a deliberate flip from Material's `determinate`; **`strokeWidth` 0=auto=`diameter/10`** computed in-template to sidestep Material's `v||0` setter that would collapse an explicit 0; diameter default 48 = deliberate Caelum default vs Material's 100), **cae-divider** (`p-divider`/MatDivider; vertical/inset; **host `display:contents`** so a vertical divider's inner `mat-divider` is hoisted as the flex item + stretches to row height — box styles on the wrapper are inert). Token-only (no `color` input). Forge gains a live wizard-progress strip (bar announced + a decorative `aria-hidden` spinner ring + a vertical divider, all driven by a `stepProgress()` computed on the step signal). Adversarially reviewed [4-lens a11y/api-design/layout-css/arch-packaging: **0 BLOCKER/MAJOR, 0 refuted**, 17 MINOR/NIT — applied: divider box-styling-inert doc, aria-hidden the decorative Forge divider, spinner aria-valuemin/max + strokeWidth-auto specs, dropped a hardcoded token-violating min-block-size, docstring parity notes; followups **#90** parity extras (divider label/type/align + progress-bar % label) / **#91** M4 real-browser-verify the `display:contents` vertical divider]; **#85 Forge @defer bundle budget DONE** → PR #94 — the four below-the-fold demo sections (structure `cae-tree` / reference `cae-tabs` / FAQ `cae-accordion` / tag-row `cae-chip`) each wrapped in **`@defer (on idle)`** split MatTree/MatTabs/MatExpansion/MatChips into lazy chunks (Forge initial **896→795 kB** raw / 170→157 kB transfer — 128 kB off the initial path); **`on idle` (not `on viewport`)** so deferred content is only *transiently* absent, never scroll-stranded for a SR/keyboard user; the `cae-tree` **value+type import → inline `type`** (a shared `import { CaeTree, CaeTreeNode }` decl counts as an eager use, defeating the split); warn budget re-tightened **900→850 kB** (1mb error kept) + a **defer-before-raise policy** recorded in PROJECT_CONVENTIONS (single-homed; fixes a stale 400/600 kB figure). Adversarially reviewed [4-lens defer-bundle/a11y/test-integrity/arch-budget; **1 CONFIRMED MAJOR, 0 refuted** — no regression guard for the deferral (an un-defer stays under the 1mb error → nothing red) → **boundary test** `getDeferBlocks().length===4` + absent-before/present-after added; MINOR/NIT applied — comment overclaim reworded, deliberate `@placeholder`/`@error` omission documented, budget policy recorded, raw-vs-gzip units noted] · **CI:** `full`+blocking; the heavy matrix is now **real + required**: `static gates` (node-free, runs the dependency-provenance gate) + `build & test (ubuntu-latest)`/`(windows-latest)` (setup-node@22 → `build:lib` [caelum + US-origin attestation + gzip size gate + exports-completeness gate] → `ng build forge` [budgets] → `ng test` [Vitest/jsdom]) + `format & lint` (`format:check` Prettier + adapter-isolation eslint), all gated by a stable **`heavy matrix gate`** aggregation check; **required checks = `static gates` + `heavy matrix gate`** (#32 — a skipped matrix job posts the *unexpanded* `build & test (${{ matrix.os }})` name, so per-leg names can't be required directly; the gate runs `if: always()` and fails if any heavy job failed, passes when they're skipped on doc-only PRs; size + attestation now CI-enforced, not just preflight)
**Done:** Onboarding (Mode A) — posture (public · full-blocking CI · review milestone-exits+risky · 48h); ARCHITECTURE (**D-01..D-11**) + ROADMAP (M0–M4); named **Caelum**/**Forge**; "US-origin" rule (**D-10**); team usage priority (tables/forms/dialog/stepper/tree/carousel up; **charts deferred**); D-07/08/09 endorsed; **20-book library COMPLETE** (`build_library`, Books 01–20; 2026-06-29 → 2026-07-01). **REPO-1 DONE (this session):** `git init` + public go-live — genesis commit `95a9330` (115 files) pushed to `main`; 10 tracker labels; `CI_POSTURE=full`; `PROJECT_BACKLOG.md` migrated → GitHub Issues **#1–#12** and deleted; root **README** rewritten Caelum-facing + **MIT `LICENSE`** added; **branch protection ON** (`main`: require PR + `static gates`, include-admins, no force-push/deletion). **README de-antagonized toward PrimeNG** (#13 → PR #14). **M0-1 DONE** (#1 → PR #16): Angular 22 workspace — library `caelum` (ng-packagr) + Forge app `forge` (`@angular/build:application`), **zoneless by default**, `cae` selector prefix, Vitest; `ng build` (both) + `ng test` green. Node 22 toolchain installed user-space at `~/nodejs` (durability → #15). Also tightened the proactive-compaction recommendation to carry the paste-able `/compact` block (#18 → PR #19). **M0-2 DONE (#2):** `@angular/material` + `@angular/cdk` + `@angular/aria` installed at **22.0.3** lockstep (plain `npm install`, no `ng add` — avoids auto-theming/font-CDN); Forge `matButton` build smoke green (`ng build` both + `ng test` green, 0 runtime vulns). First **transitive provenance scan** (D-05/D-10) ran via an adversarial ultracode workflow over the 15-package runtime tree — **license axis GREEN** (all MIT/BSD-2/Apache-2.0/0BSD, permissive+free); **origin GREEN for the shipped browser client** (`parse5`/`entities`/`@standard-schema/spec` are non-US but pulled by Angular itself and contribute **zero shipped browser bytes** — verified by `fesm2022` greps); the D-10 scoping fork (**#21**) was **decided by the human and recorded as D-11** — shipped-runtime scope + **allied-nation (UK/DE) fallback** (US-preferred, non-US avoided when a need can be met US-origin/self-built; Angular's closure accepted wholesale). Scan is GREEN under D-11. Report: `docs/provenance/M0-2-transitive-provenance-scan.md`. **M0-3 DONE (#3):** the theming token bridge — two-tier `--cae-*` token set (primitive palette → semantic role tokens) in `projects/caelum/styles/_tokens.scss`, and the bridge in `_theme.scss`: `mat.theme()` (theme-type defaults to `color-scheme`) + `mat.theme-overrides()` binding `--mat-sys-*` ← `var(--cae-*)` so Material controls and future `cae-*` components resolve identical values (verified in the built CSS: `--mat-sys-surface: var(--cae-surface-base)` overrides Material's own base). Light/dark via `light-dark()`+`color-scheme` (auto) with `:root[data-theme]` to force an arm (Book 04 §3.1–§3.3, §3.6). Shipped as raw SCSS **package assets** (`ng-package.json` → `assets`, verified in `dist/caelum/styles/`) so consumers apply it via `@use 'caelum/styles/theme'`; **Forge is the first consumer** — placeholder replaced with a token-only shell + light/dark toggle (proves the bridge end-to-end). Token-only (no hardcoded design values), builds green (both), tests green (forge **4/4**, caelum 1/1), prod Forge **202 kB** (well under budget), prettier-clean, 0 new deps. **M0-4 DONE (#4):** the gate suite — the M0-2 scan automated. Curated `provenance/allowlist.json` (D-11 source of truth: permissive-license sets + allied-nation origin list + the 15 vetted runtime packages + optional-peers `@angular/animations`/`zone.js` + the `rollup-plugin-dts` LGPL dev-exception, Book 16 §3.2). Four gates, all GREEN + negative-tested: (a) **dependency-provenance** `scripts/check_provenance.py` (pure-Python, node-free — reconciles `package-lock.json` vs the allowlist with the runtime-vs-dev split, Book 03 §3.3/§3.4; a new unvetted runtime dep / non-permissive license / non-allied origin FAILS — the forward ratchet) wired into CI **`static gates`** (real blocking check now) + preflight; (b) **adapter-isolation ESLint** `eslint.config.js` (`@typescript-eslint/no-restricted-imports` fencing `@tanstack/*`/`d3`/`lexical`/`primeng` to one explicit `*.adapter.ts` each, D-03/R6, Book 12 §3.3) wired into the real CI `lint` job; (c) **two size gates** — tightened Forge `angular.json` budgets (400/600 kB initial; Book 18 §3.1) + the bespoke shipped-library per-entry-point gate `scripts/check-lib-size.mjs` (zero-dep gzip, §3.2); (d) **US-origin attestation emitter** `scripts/emit-us-origin-attestation.mjs` → `us-origin.attestation.json` shipped IN the tarball (verified via `npm pack`), orthogonal to npm/SLSA build provenance (Book 19 §2.2/§3.5/§4(e)). The 22.0.4 patch bump pulled `zod`+`@standard-schema/spec` into the runtime tree (Angular's own closure — GREEN under D-11). Gates (b)–(d) run in preflight now; CI-matrix wiring + required-check promotion is #6. eslint toolchain added as **dev** deps (`angular-eslint@22` lockstep); builds+tests green, prettier-clean. **Adversarially reviewed (4-lens ultracode workflow) → hardened:** peer deps forced onto the strict runtime surface via a manifest-dependency-kind closure (not npm's `dev` flag — closes an M2 adapter-peer escape); dynamic-`import()`/`require()` added to the eslint fence + carve-out pinned to library source; a policy backstop rejects any forbidden-license/adversarial-nation edit to the allowlist; the emitter is closure-based + SPDX-expression-aware (gate↔attestation parity) + lists uninstalled optional peers; the §2 "Provenance purity" invariant reconciled to D-11. Every fix negative-tested. **M0-5 DONE (#5 → PR #30):** the first `cae-*` components — four thin standalone Direct wrappers over Material establishing the reusable patterns: `cae-button` (content projection + `variant`/`ariaLabel`), `cae-card` (title/subtitle + projection), `cae-checkbox` + `cae-input` (`ControlValueAccessor` form controls — the CVA value seam matching the `p-*` migration target, template-`disabled`⊕`setDisabledState` merge; `cae-input` forwards native attrs `autocomplete`/`name`/`readonly`/`maxlength`/`inputmode` + buffers IME composition, and defers validation-error forwarding to #29). All `OnPush` + signal inputs (**zoneless-compatible, provisional on #9** — commented on #9; ratify→D-NN), themed for free through the `--cae-*`/`--mat-sys-*` bridge (token-only, no per-component wiring), lib peers widened to `^22||^23` + `@angular/{cdk,forms,material}` added (all pre-vetted → provenance GREEN). Single `caelum` primary entry point; per-component secondary entry points deferred → **#28** (an "M0 packaging decision", Book 19 §3.2). **Forge rebuilt as a pure `cae-*` consumer** — a real create-workspace reactive form round-tripping through the CVAs end-to-end, with a persistent `aria-live` result region + focus move (the flagship liveness proof). **Adversarially reviewed (4-lens ultracode workflow — cva-forms/api-design/a11y/arch-consistency; 0 BLOCKER, 2 MAJOR + 4 MINOR fixed, each re-tested):** MAJOR native-attr pass-through gap (autofill/password-manager regression vs `pInputText`) + MAJOR Forge submit lost focus/announce → both fixed; MINORs — Material type leak (`CaeInputAppearance` alias), peer range, `number` type dropped from `CaeInputType`, citation §3.3→§3.1. Gates: lib+forge build green, **23 tests** (17 lib/6 forge), lint+prettier clean, lib entry **4.98 kB gzip** (61%), Forge **380 kB** (under budget), provenance GREEN, preflight green. Remaining Direct components → batch 2 **#26** + batch 3 **#27**. **M0-6 DONE (#6):** the CI hardening — the heavy matrix wired to real commands and promoted to required checks. `.github/workflows/ci.yml` `build & test` (ubuntu+windows) now runs `actions/setup-node@22` + `npm ci` → `npm run build:lib` (caelum + US-origin attestation + gzip size gate) → `npx ng build forge` (angular.json 400/600 kB budgets) → `npx ng test` (Vitest via `@angular/build:unit-test`, **jsdom** — no browser step needed); `format & lint` runs `npm run format:check` (new `format`/`format:check` npm scripts; Prettier scoped by a new **`.prettierignore`** — code only, since `textbooks`/`research`/`docs`/`.claude`/`.github`/root-prose carry their own audits) + `npm run lint`. **The M0-4 size + US-origin-attestation gates are now CI-enforced** (previously preflight-only, the #4→#6 gap closed). Headless-smoke stage **retired** (no run loop — Caelum is a client-side library, Forge a static SPA; build + tests are the operability proof). `scripts/preflight.{sh,ps1}` mirror the matrix and now report **0 skipped** with Node on PATH (build/test/format stages real; node-gated so they SKIP-not-fail without Node). Deliberate deviation from configure_project's template: `scripts/preflight.*` **kept exempt** from the TODO-hygiene gate — they *implement* the mirrored `todo hygiene` stage, so the literal words live in them by necessity (comment updated). **Branch protection promoted:** required checks → `static gates` + **`heavy matrix gate`** (an aggregation job over `build & test` [ubuntu+windows] + `format & lint`; was `static gates` only; enforce-admins + require-PR preserved; verify: `gh api repos/recon-research/caelum/branches/main/protection`). **Adversarial review MAJOR made real:** requiring the per-leg matrix names (`build & test (ubuntu-latest)`/`(windows-latest)`) deadlocked doc-only PRs — a skipped matrix job posts one check under the unexpanded `${{ matrix.os }}` name, so those contexts never satisfy → the `if: always()` **`heavy matrix gate`** is the fix (**#32** → PR #34: #34's first (doc-only) run reproduced the deadlock, its second (code) run delivered + verified the gate; required checks re-promoted to `static gates` + `heavy matrix gate`). All gates green: preflight PASS 0-skipped, 23 tests, lib 4980 B gzip, Forge 380 kB, provenance GREEN. **Direct components batch 2 DONE (#26):** five more Direct wrappers reusing #5's patterns — **cae-radio** (grouped options as `CaeRadioOption[]` data + CVA; **unique fallback group name** via a module counter so two unnamed groups don't collide through Material's global `UniqueSelectionDispatcher`; `ariaLabelledby` for a visible group label), **cae-select** (grouped `CaeSelectOption[]` CVA, single-select, touched on `focusout`), **cae-textarea** (cae-input's multi-line sibling — CVA + IME buffering + `name`/`maxlength`/`readonly`), **cae-tabs**/**cae-tab** (content-projection panels: `cae-tab` captures its content via `<ng-template #content><ng-content/></ng-template>`, `cae-tabs` collects them with `contentChildren` and stamps `mat-tab` bodies via `ngTemplateOutlet`; two-way `selectedIndex`/`selectedIndexChange`), and **caeTooltip** (attribute directive composing `MatTooltip` via `hostDirectives` input-aliasing — zero logic; JSDoc documents the **focusable-host a11y contract**). Shared **`CaeFormFieldAppearance`** type extracted to `lib/shared/appearance.ts` (input/select/textarea reference it — replaces the cross-file `CaeInputAppearance` import; package-neutral for #28). **Forge** demonstrates all five: the reactive form gains `plan` (radio)/`region` (select)/`description` (textarea) CVAs; `cae-tabs` organises the Design-tokens + In-this-batch reference panels; `caeTooltip` sits on a **focusable native header help button** (correct a11y pattern). **Adversarially reviewed (4-lens ultracode workflow — cva-forms/api-design/a11y/arch-tabs; 2 MAJOR + 2 MINOR + 2 NIT fixed, arch-tabs clean):** MAJOR radio empty-name cross-group collision + MAJOR caeTooltip keyboard/SR-inaccessible on the non-focusable `<cae-button>` host → both fixed (unique name; demo moved to a focusable element + JSDoc contract + **#36** filed for a cae-button tooltip-forwarding seam); MINOR select touched-only-on-open + MINOR `CaeInputAppearance` cross-file coupling; NIT radio double-label + NIT missing required marker. Each fix re-tested. Gates: lib+forge build green, **48 tests** (41 lib + 7 forge), lint/prettier clean, lib **9.0 kB gzip** (88% of a deliberately-raised 10 kB budget — the monolithic entry point grows per batch until #28 splits it), Forge **608 kB raw / 124 kB transfer** (angular.json budgets raised 650/850 kB), provenance GREEN (no new deps), preflight PASS 0-skipped. Remaining Direct components → batch 3 (#27). **#28 DONE (PR #44):** the packaging split — each `cae-*` component + `shared` is now a **self-contained secondary entry point** `projects/caelum/<name>/` (`ng-package.json`+`public-api.ts`+source+spec) published as `caelum/<name>`, mirroring Angular Material; the primary barrel `src/public-api.ts` re-exports each **by package name** (`export * from 'caelum/<name>'`) so `import … from 'caelum'` still works (additive — no `ng-update`). ng-packagr requires each entry point self-contained (source under its own folder — a `../src/lib` reach-out trips `TS6059 rootDir`) and each source file owned by exactly one entry point (the shared `CaeFormFieldAppearance` moved to `caelum/shared`, Material's `/core` pattern, so the barrel re-exports by name rather than re-declaring source). **Payoff:** barrel **13.4 kB → 742 B** (a thin re-export shim), `caelum/button` **1.7 kB** (was the whole 13 kB set); `size-budget.json` now has one tight row per component (barrel + `caelum-shared` deliberately tight — their size is itself an invariant). **Forge switched to per-component imports** (the real "pay only for what you import" consumer proof; `tsconfig` gained `caelum/*`; the unit-test `include` uses `../*/**/*.spec.ts` to find specs in the sibling folders — verified on windows-latest). New **`scripts/check-lib-exports.mjs`** (chained into `build:lib`): a **self-updating** gate that every entry-point folder is wired into BOTH the emitted `exports` map AND the barrel (negative-tested). **Adversarially reviewed (4-lens ultracode — packaging-exports/api-backcompat/treeshake-size/build-test-config; 0 BLOCKER, 0 confirmed MAJOR [one MAJOR refuted on verify]; MINOR/NIT fixed):** `PROJECT_CONVENTIONS.md` source-layout drift corrected + "adding a component" checklist; barrel/shared budgets tightened to policy; the non-hermetic hardcoded barrel unit spec replaced by the build-time exports gate. Gates: `build:lib` GREEN (14 entry points + attestation + size + exports), Forge 736 kB, **65 tests** (54 lib + 11 forge), lint/prettier clean, provenance GREEN (0 new deps), preflight PASS 0-skipped. **#29 DONE (PR #49):** validation-error forwarding — `cae-input`/`cae-textarea` now reflect the BOUND control's validity in their inner `mat-form-field` (`<mat-error>` + invalid styling + `aria-invalid`), the gap #5 deferred. Mechanism (Book 07 §3.3/§3.4): **self-inject `NgControl`** (drop the `NG_VALUE_ACCESSOR` provider) → **bridge** into the inner field via a per-control `ErrorStateMatcher` that delegates *timing* to the DI `ErrorStateMatcher` (Material's default = `invalid && (touched||submitted)`, byte-identical to a would-be `CaelumErrorStateMatcher` — so uniform library-wide + app-root-overridable, **no redundant class shipped**) → **recompute in `ngDoCheck`** against the live flags (Material's own pattern; `control.events` only nudges CD for programmatic changes in zoneless; resilient to `[formControl]` swaps + the `resetForm()` model-before-`submitted` staleness). New **`errorMessages`** input (`CaeErrorMessages` = validator-key → `string | (error)=>string`, in the type-only `caelum/shared`, re-exported by both controls) renders `<mat-error>`; `aria-invalid`/describedby come from Material (EXCEPT empty-required, where Material suppresses `aria-invalid` by design → map `required`). **Forge**: real per-field messages on name/email/password + `reset()` now clears `submitted`. **Adversarially reviewed (4-lens ultracode — cva-forms/error-reactivity/a11y/api-design; 11 findings, 2 CONFIRMED MAJOR + MINOR/NIT, all fixed):** MAJOR Forge reset() wall-of-errors (submitted never cleared → `resetForm()`; surfaced the deeper `ngDoCheck`-recompute fix) + MAJOR false `aria-invalid` a11y docstring; MINOR×3 orphaned-subscription-on-swap → `ngDoCheck` identity re-wire. Followups filed: **#46** shared form-field base, **#47** extend bridge to cae-select, **#48** rich-error escape hatch. Gates: `build:lib` GREEN (caelum-input 5373/6144, caelum-textarea 4748/5504 gzip; `caelum-shared` unchanged 451 = type-only), **73 tests** (61 lib + 12 forge), Forge 750 kB, lint/prettier clean, provenance GREEN (0 new deps), preflight PASS 0-skipped. **#47 DONE (PR #52):** the #29 error-forwarding bridge extended to **cae-select** (self-inject `NgControl`, per-control bridge `ErrorStateMatcher`, `errorMessages`→`<mat-error>`, `ngDoCheck`→`viewChild(MatSelect).updateErrorState()`; `MatSelect.ngDoCheck` only self-recomputes when it owns an `NgControl`, which the inner select never does — so the external poke is required; note **mat-select reflects `errorState`→`aria-invalid` unconditionally**, no empty-required suppression unlike matInput — documented+tested) + the **radio/checkbox decision** recorded: they're not `MatFormFieldControl`s, so they get a **consumer-owned error pattern** — an `ariaDescribedby` input forwarded onto each **focusable inner `<input>`** (Material's own seam; a description on the non-focusable `role=radiogroup` container isn't reliably announced), the consumer renders the message + points at it (a built-in `errorMessages` is reversible/additive → **#51**). **Forge** closes the plan/region/agree asymmetry: region (select) shows inline `<mat-error>`, plan (radio) + agree (checkbox `requiredTrue`) show consumer errors linked via `ariaDescribedby`; dropped the redundant `role=group` double-grouping. **Adversarially reviewed (4-lens ultracode + a fix-delta verifier; 3 CONFIRMED MAJOR + MINOR/NIT, all fixed+re-verified):** MAJOR region select shipped invalid-but-textless (mechanism wired, `errorMessages` map forgotten) + MAJOR radio describedby landed on the non-focusable radiogroup container + MAJOR checkbox had no hook despite the "radio/checkbox" JSDoc; MINOR/NIT — `CaeErrorMessages` docstring, `$any` bridges Material's `string`-typed `aria-describedby` input (null→attr absent), size budgets. Gates: `build:lib` GREEN (caelum-select 4940/5632, caelum-radio 3196/3712, caelum-checkbox 2401/2816 gzip), **80 tests** (67 lib + 13 forge), Forge 753 kB, lint/prettier clean, provenance GREEN (0 new deps), preflight PASS 0-skipped. **#46 DONE (PR #55):** the shared form-field-control base — the string-CVA seam + the #29/#47 validation-error-forwarding bridge (self-inject `NgControl` + per-control `ErrorStateMatcher` + `ngDoCheck` recompute + `errorMessages`→`<mat-error>`), which cae-input/textarea/select each carried **byte-for-byte**, extracted into ONE abstract `@Directive()` base **`CaeFormFieldControlBase`** published as the new tree-shakable secondary entry point **`caelum/form-field`**. Each control now `extends` it and keeps only its specifics (type/attrs/IME for input, `rows` for textarea, `options` for select) + a one-line `updateInnerErrorState()` seam over its own inner `MatInput`/`MatSelect`. Behaviour unchanged (all 72 prior control specs green; a new base spec tests the seam in isolation). Payoff: **−429/+44** across the three controls; per-entry gzip **shrinks** (input 5373→3011, select 4940→2620, textarea 4748→2678 B) into one **5073 B** base; a multi-control consumer ships one copy, not three. **Packaging gotcha found + fixed (would have broken CI):** ng-packagr's `.d.ts` flattener (rollup-plugin-dts) **tree-shakes an abstract `@Directive()` base out of the bundled typings** when it's re-exported from a sibling module in a *solo* entry point (ships an empty `export {}`, so dependents can't resolve its type) — a *clean* build (like CI) reproduces it, an incremental one masks it; the fix is to **declare the class in the entryFile (`public-api.ts`)**, which anchors it as a bundle root (documented inline). **Adversarially reviewed (4-lens ultracode — inheritance-correctness/behaviour-parity/api-surface/packaging; 0 confirmed MAJOR — a claimed single-control byte regression refuted as a gzip-additivity artifact; MINOR/NIT fixed):** renamed the base **`...Base`** (disambiguates Material's `MatFormFieldControl` + signals abstract), documented **experimental** for external subclassing (**#54** files the promote-vs-internal decision), reworded the CVA docstring, re-exported the shared types, reconciled the `size-budget` `$comment`. Gates: `build:lib` GREEN (caelum-form-field 5073/6144; input/select/textarea budgets retightened 3584/3072/3200), **87 tests** (74 lib + 13 forge), Forge 750 kB, lint/prettier clean, provenance GREEN (0 new deps), preflight PASS 0-skipped. **#36 DONE (PR #59):** the a11y forwarding seam (tooltip half) — `caeTooltip`/`matTooltip` attach to their host, so on the non-focusable `<cae-button>` wrapper a tooltip was pointer-only and its `aria-describedby` landed off the real control; cae-button now takes a **`tooltip`** (+ **`tooltipPosition`**) input that binds `MatTooltip` to its **inner `<button>`** (the natural `<p-button pTooltip>` → `<cae-button tooltip>` swap, keyboard- and SR-accessible). Empty `tooltip` (default) disables the directive (no listeners, no describedby). **`CaeTooltipPosition`** (a Caelum alias of Material's peer `TooltipPosition`) lives in **`caelum/shared`** (cross-component-type convention; avoids a barrel double-export trap), `import type` so it stays type-only. A tooltip on a *disabled* button won't display visually (native disabled swallows focus/hover) though describedby still reaches a screen reader — documented; `disabledInteractive` seam → **#58**. **Menu-trigger forwarding split to #57** — a genuine cross-entry-point blocker (`CaeMenu.panel` is `@internal`, stripped from `caelum/menu`'s typings, so `caelum/button` can't reach `menu.panel()`). **Forge** demonstrates the seam on the Theme button. **Clean build caught + fixed a real trap: the literal `@internal` in a class's JSDoc *is* a `stripInternal` marker — it had silently removed `CaeMenuTrigger` from the typings (would have broken CI) until reworded.** **Adversarially reviewed (4-lens ultracode — a11y-forwarding/api-design/packaging-build/arch-parity; 0 BLOCKER/MAJOR, MINOR/NIT applied: type→`caelum/shared`, disabled-tooltip JSDoc, `aria-describedby`-on-inner-button assertion, menu.ts wording):** two lenses independently flagged the disabled-button tooltip nuance (→ documented + #58); two flagged the `CaeTooltipPosition` home (→ moved to `caelum/shared`). Gates: `build:lib` GREEN (caelum-button 2513/3072 gzip; caelum-shared 451 unchanged = type-only), **92 tests** (79 lib + 13 forge), Forge 754 kB, lint/prettier clean, provenance GREEN (0 new deps), preflight PASS 0-skipped. **#40 DONE (PR #62):** linear (validity-gated) stepping — `cae-step` gains a **`stepControl`** seam; `cae-stepper` gains **`[linear]`** and drives Material's `selectedIndex` from an `effect` so a REFUSED linear move (which Material makes silently — no event) is reconciled: it reads the actual index back and re-emits, so `[(selectedIndex)]` never diverges from the rendered step. Reads/writes of Material's index are **`untracked`** so the effect depends only on the input + the `viewChild`, not Material's own index signal (which would fight user header clicks); `linear` off = behaviour-preserving (Book 05 §3.5). **Forge** adds a compact **"Invite a teammate"** linear demo (2 validity-gated steps), kept SEPARATE from the free-navigation create-workspace wizard so both modes show (count tests scoped to the wizard card). **Adversarially reviewed (4-lens ultracode — reconciliation-cd/linear-forms/a11y/arch-scope; the reconciliation core verified SOLID [loop-free, single-emit, untracked isolation]; 2 CONFIRMED MAJOR + MINOR/NIT fixed, both MAJORs regression-tested):** MAJOR the 2nd `[formGroup]` (invite) made `viewChild(FormGroupDirective)` ambiguous so "Create another" reset the WRONG form → query the wizard form by `#createFormDir`; MAJOR the invite-success swap stranded focus + announced unreliably → a persistent live region + focus-move effect mirroring the wizard; MINOR/NIT — Book 05 §3.5 citation (was Book 11), `selectedIndexChange` refused-move JSDoc, a DOM-driven `formGroupName`-through-projection test, scoped #29/#47 tests, well-behaved-consumer pre-check (prompt via live region, no refused-Next flicker). Filed **#61** (per-step `completed`/`hasError` override). **Gotcha:** ng-packagr's fesm2022 RETAINS JSDoc comments — docstrings ship as bundle bytes (caelum-stepper 3328→3841 gzip; budget 4608). Gates: **100 tests** (82 lib + 18 forge), Forge 760 kB, lint/prettier clean, provenance GREEN (0 new deps), preflight PASS 0-skipped. **#57 DONE (PR #64):** the a11y forwarding seam (menu half) — the sibling of #36's tooltip seam. `caeMenuTriggerFor` composes `MatMenuTrigger`, which puts `aria-haspopup`/`aria-expanded` + keyboard handling on its host, so on the non-focusable `<cae-button>` wrapper the menu was pointer-only with its ARIA on the wrong element. cae-button now takes a **`menuTriggerFor`** input binding `MatMenuTrigger` to its **inner `<button>`** (the `<p-menu>`+`<p-button>` → `<cae-menu>`+`<cae-button [menuTriggerFor]>` swap). Mechanism (mirrors the tooltip seam's peer-binding cost — no cae-menu runtime coupling): a new **type-only structural seam `CaeMenuPanelHost { getMenuPanel(): MatMenuPanel | undefined }`** in `caelum/shared` lets `caelum/button` name it via `import type` (erased) and NOT runtime-depend on `caelum/menu` (same reasoning that homes `CaeTooltipPosition` there); **`CaeMenu.getMenuPanel()`** wraps the (still `@internal`) `panel` viewChild, now **non-required** so it reads `undefined` before view-init instead of throwing; cae-button syncs the resolved panel into a local `@internal` signal via an **effect** (order-independent wiring + sidesteps a possible dev-mode `ExpressionChangedAfterChecked`; mirrors `CaeMenuTrigger`'s effect). **Two-branch template:** `MatMenuTrigger` binds `aria-expanded` UNCONDITIONALLY (`"false"` when closed), so the trigger is applied ONLY when a menu is bound — an always-present trigger would announce every plain button as a collapsed disclosure (verified against Material 22.0.4 source: null menu → `_openMenu` early-returns, `aria-haspopup` absent, `aria-expanded` unconditional). **Forge** swaps its native Actions button for `<cae-button [menuTriggerFor]="actionsMenu">`. **Adversarially reviewed (4-lens ultracode — a11y-forwarding/api-design/reactivity-cd/packaging-treeshake; 0 MAJOR/BLOCKER; MINOR/NIT fixed):** two-branch parity spec strengthened to assert variant/aria-label/disabled on the menu-branch button (was tooltip-only); the `menuPanel` JSDoc corrected (the effect's real justification, not the refuted "template read wouldn't track" claim — Angular *does* track signal reads in template-invoked methods); `@internal` on `menuPanel` closed a `MatMenuPanel` .d.ts leak. Gates: clean `build:lib` GREEN (caelum-button 3410/4096 gzip; caelum-menu 3244, caelum-shared 451 unchanged/type-only), **106 tests** (88 lib + 18 forge), Forge 760 kB, lint/prettier clean, provenance GREEN (0 new deps), preflight PASS 0-skipped. The #36 menu-trigger blocker (the `@internal` `CaeMenu.panel`) is resolved via the public `getMenuPanel()` seam.
**Library (COMPLETE — the onboarding/compaction anchor):** Vol I Foundations [01 *Angular 22 Architecture* · 02 *Reactivity & Forms* · 03 *Provenance & Licensing* · 04 *Theming Token Bridge*]; Vol II Building on Primitives [05 *CDK Primitives* · 06 *Angular Aria Headless* · 07 *Form Control Fundamentals* · 08 *Numeric/Mask Inputs* · 09 *Overlay & Menu* · 10 *Data Tables & Virtualization* · 11 *Layout/Panels/Media/Drag-Drop*]; Vol III The Adapter Layer [12 *Adapter Pattern* D-03 · 13 *Data Grid* — TanStack/`CaeGridAdapter` D-07 · 14 *Charts* — D3 SVG/`CaeChartAdapter` D-08 · 15 *Editor* — Lexical/`CaeEditorAdapter` CVA D-09]; Vol IV Quality [16 *Accessibility & Parity* — capability ledger + implementer/adversarial split; backs `verify_parity` · 17 *Testing* — **Vitest is v22's default runner** + CDK `ComponentHarness`; backs `add_test` · 18 *Performance* — the **two size gates** + tree-shaking/`@defer`/zoneless; backs `profile_subsystem`/`optimize_loop`]; Vol V Distribution & Adoption [19 *Packaging* — the **two orthogonal provenances** (npm/SLSA *build* vs the shipped *US-origin* attestation, Book 03 §2.3), `ng-packagr`+APF+`exports`(`types`/`default`, **not** `esm2022`)+secondary entry points, peer `^22||^23`+lockstep+`ng-update`; D-06 · 20 **Migration & Adoption** — the living PrimeNG→Caelum `p-*`→`cae-*` map (`reference/COMPARISON.md`), strangler-fig incremental adoption, the standalone `ng generate` codemods (distinct from Book 19's `ng-update`), the forward-only lint-ratchet fitness function (Book 12 §3.3 generalized) against adapter erosion]. + research notes `angular-22-{platform,testing,performance,packaging}`, `tanstack-table`, `visx-charts`, `lexical-editor`, `a11y-testing-tooling`. **Five audits green — 21 books/348 sections, refs 660/0, routing 82/82, links 144/0, research 0/0.** Books 18–20 (+ Book 20's `COMPARISON.md`) built via multi-agent workflows (ultracode): research fan-out → write → 4-lens adversarial-review fan-out.
**Next:** **M0 continues** — pick the next M0 slice: continue the **PrimeNG-parity Direct-component family** — batch 4 (cae-switch #68, cae-toggle-button/cae-select-button #73) + **accordion (#77)** + **chip (#83)** + **display primitives (progress-bar/spinner/divider, #88)** delivered; next candidates are more **panel/layout / data-display** Direct rows (toolbar/badge/avatar/skeleton — COMPARISON §Panel-layout + §Data-display), the richer **`cae-slider`**/**`cae-listbox`**/**`cae-autocomplete`** (Book 06/07/09), **#84** `cae-chip-set` (managed removable-chip list — focus redirect + roving nav), or **#70** the cross-control `ariaLabel`/`ariaLabelledby` label-less seam. **#85 Forge @defer bundle budget DONE** (PR #94 — Forge initial 896→795 kB via `@defer (on idle)` on the below-the-fold demo sections; warn re-tightened to 850 kB; defer-before-raise policy recorded). The next family that adds an EAGER control (cae-slider/listbox/autocomplete) has ~55 kB headroom before 850; below-the-fold demos must be deferred, not budget-raised. **Decisions all ratified 2026-07-02 → D-12/D-13/D-14/D-15** (#9/#10/#11/#39 closed; Appendix A + ROADMAP updated). **#12** AGENT_GUIDE §1.5 real trace (**now due** — use #5/#26/#27/#28/#29/#46/#47/#36/#40/#57/#58/#68/#73 as the worked example). Also: the **dialog/toast slice** is now **unblocked — D-15 = service passthrough** (`CaeDialog`/`CaeToast` over `MatDialog`/`MatSnackBar`; `cae-toast` naming) — buildable when scheduled, **#41** cae-tree keyboard-nav verification at M4, **#48** rich-error escape hatch (idea) / **#51** built-in errorMessages for radio/checkbox (idea, reversible) / **#54** formalize `CaeFormFieldControlBase` stability — promote to a documented extension API vs keep internal-experimental (idea, M1+) / **#61** cae-step per-step `completed`/`hasError` override for form-less gated steps (idea, reversible), **#7** python3 unify, **#8** LIB-DOCS, **#15** durable Node toolchain, **#33** CI-gate robustness followup (harden the `changes`-classifier-infra-failure path).
**Decisions — all resolved (2026-07-02); no open `decision` issues.** Ratified by the human on the recommended default, recorded in ARCHITECTURE Appendix A (#9/#10/#11/#39 closed): **D-12** zoneless-compatible invariant (#9 — now a §2 invariant) · **D-13** Material→Aria→CDK→bespoke reach-for ladder (#10 — refines D-02) · **D-14** charts → D3-direct (#11 — supersedes D-08's visx, which is React-bound; confirm exact modules + walk the tree at M2) · **D-15** cae-dialog/cae-toast = service passthrough over MatDialog/MatSnackBar (#39 — `CaeDialog`/`CaeToast`, `cae-toast` naming per COMPARISON; **unblocks the dialog/toast slice**). *(**#21 resolved 2026-07-01 → D-11**: D-10 refined to shipped-runtime scope + allied-nation UK/DE fallback. Books 15–20 produced no new D-NNs; the two-size-gates + US-origin-attestation emitter are M0 design tasks (issue #4), not D-NNs. Final transitive-provenance sign-off for grid/charts/editor at M2 now runs against D-11.)*
**Resume:** `main` @ `1161494` · repo **LIVE + protected** — required checks = `static gates` + **`heavy matrix gate`** (aggregation over `build & test` ubuntu+windows + `format & lint`; enforce-admins + require-PR on; per-leg matrix names are NOT required — they don't post when skipped, #32) · **REPO-1 + M0-1..M0-6 + Direct batch 2 (#26) + Direct batch 3 (#27 → PR #42) + #28 secondary entry points (PR #44) + #29 validation-error forwarding (PR #49) + #47 cae-select forwarding + radio/checkbox hook (PR #52) + #46 shared form-field base `CaeFormFieldControlBase` (PR #55) + #36 cae-button tooltip forwarding (PR #59) + #40 cae-stepper linear stepping (PR #62) + #57 cae-button menu-trigger forwarding (PR #64) + #58 cae-button disabledInteractive (PR #66) + #68 cae-switch (Direct batch 4, PR #69) + #73 cae-toggle-button/cae-select-button over MatButtonToggle (Direct batch 4, PR #74) + #77 cae-accordion/cae-expansion-panel over MatExpansion (Panel/layout, PR #81) + #83 cae-chip over MatChip (data-display, PR #86) + #88 cae-progress-bar/cae-progress-spinner/cae-divider over MatProgressBar/MatProgressSpinner/MatDivider (display primitives, PR #89) + #85 Forge @defer bundle budget (PR #94 — initial 896→795 kB) DONE.** Next action = pick the next M0 slice — continue the Direct-component family (a panel/layout / data-display row: toolbar/badge/avatar/skeleton; or the richer cae-slider/cae-listbox/cae-autocomplete), **#84** cae-chip-set (managed removable-chip list), or **#70** the cross-control ariaLabel/labelledby seam; the dialog/toast slice is unblocked (**D-15**). **#85** Forge @defer bundle budget DONE (PR #94, initial 795 kB); the four pending decisions are now **ratified → D-12/D-13/D-14/D-15** (#9/#10/#11/#39 closed). The cae-button a11y forwarding seams (#36 tooltip / #57 menu / #58 disabledInteractive) are complete; the **dialog/toast slice is unblocked (D-15 = service passthrough — `CaeDialog`/`CaeToast` over `MatDialog`/`MatSnackBar`, `cae-toast` naming)** — buildable when scheduled. **The `cae-*` Direct-component pattern (set by #5, extended by #26/#27):** thin standalone wrapper over Material, `OnPush` + signal inputs, theme-free via the bridge; form controls = CVA (value seam, touched on focusout/blur); grouped options as data (`Cae*Option[]`); native attrs forwarded on inner elements; directives via `hostDirectives` input-aliasing (caeTooltip) OR an instance-input + `effect` wiring (caeMenuTriggerFor takes the `CaeMenu` instance, not a Material type); content-projection via `<ng-template>`+`contentChildren` (cae-tabs/cae-stepper — but stepper steps stamp **eagerly**, unlike lazy tab bodies; **cae-stepper `[linear]` (#40)** = `cae-step` `stepControl` forwarded to `mat-step` + `selectedIndex` driven imperatively from an `effect` that reads Material's index back **`untracked`** and re-emits on a REFUSED linear move so `[(selectedIndex)]` never desyncs — a consumer should still pre-validate before advancing; **NOTE ng-packagr's fesm2022 retains JSDoc comments → docstrings ship as bundle bytes**); **structural `*matXxxDef` + `dataSource`/`childrenAccessor`** for MatTree (NOT `@for`); shared form-field types in `caelum/shared` (type-only entry point); **validation-error forwarding (#29):** self-inject `NgControl` (drop the `NG_VALUE_ACCESSOR` provider) + a per-control bridge `ErrorStateMatcher` delegating *timing* to the DI matcher + recompute in `ngDoCheck` (Material's pattern — resilient to control swaps + `resetForm()` staleness; `control.events` only nudges CD for programmatic zoneless changes) + an `errorMessages` map rendering `<mat-error>` — map `required` or an empty-required field is SR-silent (Material suppresses `aria-invalid` there). **Extended to cae-select by #47** (same bridge over the inner `mat-select`; but **mat-select reflects `errorState`→`aria-invalid` unconditionally** — no empty-required suppression). **Now extracted (#46) into the abstract `@Directive()` base `CaeFormFieldControlBase` (new `caelum/form-field` entry point):** input/textarea/select `extends` it for the identical CVA + bridge and add only their inner-control specifics + a one-line `updateInnerErrorState()` seam over their own `viewChild(MatInput|MatSelect)`. **Non-form-field controls (cae-radio/cae-checkbox/cae-switch/cae-toggle-button/cae-select-button) get NO `<mat-error>`** — they're not `MatFormFieldControl`s; instead an **`ariaDescribedby` input forwarded onto each focusable inner control** (an `<input>` for radio/checkbox; a **`<button role="switch">`** for cae-switch — M3's slide-toggle is a button, not an input), and the consumer renders + points at the message (Forge does; a built-in `errorMessages` for these is reversible/additive → #51). **For radio/checkbox/switch** Material exposes a native `aria-describedby` input (`$any(... || null)` bridges its `string` type so empty→attr-absent) — NOT the non-focusable `role=radiogroup` container, which isn't reliably announced. **For cae-toggle-button/cae-select-button (#73)** Material's `mat-button-toggle` has NO `aria-describedby` input AND its host is `role="presentation"`, so describedby is set onto the focusable inner `<button>`(s) via an **`afterRenderEffect`** (post-render, reactive on `options()`) — NOT declaratively. **cae-switch (#68)** = the cae-checkbox twin over `MatSlideToggle` (`p-toggleSwitch` parity) + a **`hideIcon`** passthrough (default `false`; set it for the icon-less p-toggleSwitch look — a default-flip for pixel-parity is a separate decision). **cae-toggle-button (#73)** = a boolean CVA over a STANDALONE `mat-button-toggle` (`p-togglebutton`; inner `<button role="button">` + **`aria-pressed`**, read `$event.source.checked`; content-projected label, state-independent → on/off-label parity #75). **cae-select-button (#73)** = the cae-radio twin over `mat-button-toggle-group` (`p-selectbutton`; `CaeSelectButtonOption[]`, `[value]`+`(change).value` = the GROUP value = `string` single / `string[]` `multiple`; `multiple` is read ONCE at Material's `ngOnInit` → set it statically; `writeValue` coerces null→mode-empty; `required`→`[attr.aria-required]` on the radiogroup/group host; `hideSelectionIndicator` passthrough for the checkmark). **cae-accordion + cae-expansion-panel (#77)** over `MatExpansion` (`p-accordion`; a NON-projection-re-stamping content family — unlike cae-tabs/cae-stepper): `cae-accordion` = a zero-logic container via **`hostDirectives:[{directive:MatAccordion, inputs:['multi: multiple','displayMode','hideToggle']}]`** + `<ng-content>` (the `multiple` alias = PrimeNG + cae-select-button parity), and projected `cae-expansion-panel`s coordinate purely via DI — the inner `mat-expansion-panel` does `inject(MAT_ACCORDION,{optional:true,skipSelf:true})` which walks UP the injector tree, and a content-projected child sees the projecting host's element-injector providers (declaration-tree DI, Book 01 §3.3), so `MatAccordion` on the host is reachable WITHOUT re-stamping; single-expand = `UniqueSelectionDispatcher` keyed by the accordion's auto-unique id (no manual group naming, unlike cae-radio), and its auto-close **fires `expandedChange`** so `[(expanded)]` self-syncs with NO reconciliation (contrast cae-stepper's silent linear refusal). `cae-expansion-panel` = a thin `mat-expansion-panel` wrapper (`title`/`description` header text, `[(expanded)]` two-way [prefer two-way — a one-way `[expanded)]` won't re-assert after a user toggle], `disabled`, `hideToggle` [OR-combined with the accordion's — panel default false never clobbers], `(opened)`/`(closed)`; `<ng-content>` body; not a form control → no CVA/`<mat-error>`). The optional WAI-ARIA inter-header arrow-key roving is NOT forwarded (Material's `@ContentChildren(MatExpansionPanelHeader)` doesn't cross the wrapper view boundary — disclosed in JSDoc; → **#79** M4 verify). **cae-chip (#83)** over a STANDALONE `MatChip` (`p-chip`; a display chip — MatChip defaults `role=null` + injects no chip-set, so no set/grid/listbox needed): content-projected label, `removable` (default `false` = p-chip parity) → a remove `<button matChipRemove>` + `(removed)`, `disabled`, `removeAriaLabel`. **KEY a11y GOTCHA (review-caught BLOCKER):** Material renders chip actions (incl. `matChipRemove`) with `tabindex="-1"` and only a `MatChipSet`'s FocusKeyManager promotes them to a roving `0` — a STANDALONE chip has no set, so the remove `<button>` is keyboard-INOPERABLE unless you bind **`[tabIndex]="0"`** on it (`MatChipContent.tabIndex` is a public input; `_getTabindex()` returns `null` when disabled, so `0` is safe). A standalone chip also does NOT redirect focus on removal (no set) — the CONSUMER moves focus + announces (Forge's tag row: a persistent `role=status` live region + a focus-move `effect`), matching PrimeNG's standalone `p-chip`; a managed set with built-in focus-redirect + roving nav → **#84** cae-chip-set. The remove glyph is a **self-authored inline SVG "×"** (stroked `currentColor`), NOT `mat-icon` (Material Icons is a Google-CDN font — avoid per the US-origin/no-CDN discipline; use inline SVG for any icon need). **Display primitives (#88)** = three NON-interactive Direct wrappers (no CVA, no form control), standalone-component imports: **cae-progress-bar**/**cae-progress-spinner** wrap MatProgressBar/MatProgressSpinner (both host `role=progressbar` + aria-valuemin/max/now but NO built-in accessible name → expose **`ariaLabel`** forwarded as `[attr.aria-label]` on the inner element, empty→absent; `numberAttribute` on value/bufferValue/diameter, wrapper does NOT clamp — Material clamps internally); **cae-progress-spinner defaults mode=`indeterminate`** (p-progressSpinner parity — a deliberate flip from Material's `determinate` default) and **`strokeWidth` 0=auto** via `[strokeWidth]="strokeWidth() || diameter()/10"` in-template (Material's `v||0` setter would collapse an explicit 0; diameter default 48 = deliberate Caelum default vs Material 100); **cae-divider** wraps MatDivider (vertical/inset; role=separator + auto aria-orientation) with **`:host{display:contents}`** so the wrapper adds no box — the inner `mat-divider` is hoisted as the flex item and a vertical divider stretches to row height (a wrapping block would collapse to 0) BUT box styles (margin/padding/border/background) on `<cae-divider>` are inert → space via layout gap. NO `color` input on any (token-bridge theming). Decorative duplicates (Forge's spinner ring echoing the bar) get `aria-hidden` on an ancestor so role=progressbar isn't double-announced; a purely-decorative divider gets `aria-hidden` too (it'd otherwise announce a separator to silent content). Parity gaps → #90 (divider label/type/align + progress-bar % label); `display:contents` real-browser verify → #91 (M4, like #79/#41). **Material types kept out of the public bindable API via `@internal` + tsconfig `stripInternal`** (cae-menu `panel`) — NOTE `private`/`protected` do NOT strip a member's *type* from the emitted `.d.ts` (the bridge matcher's `ErrorStateMatcher` type IS emitted; harmless — `@angular/material` is a peer dep, so it resolves); **each `cae-*` ships as its own tree-shakable secondary entry point `caelum/<name>` (#28)** — a self-contained folder `projects/caelum/<name>/` (ng-packagr requires each entry point self-contained + each source file single-owned; shared types in `caelum/shared`), the barrel `src/public-api.ts` re-exports each by package name, and `scripts/check-lib-exports.mjs` (in `build:lib`) gates that every folder is wired into the exports map + barrel. **Gotcha (#46):** an abstract-`@Directive()` base alone in a solo entry point (`caelum/form-field`) must be **DECLARED in its `public-api.ts` entryFile, not re-exported from a sibling `.ts`** — ng-packagr's `.d.ts` flattener (rollup-plugin-dts) tree-shakes a re-exported abstract base out of the bundled typings (ships empty `export {}` → dependents can't resolve its type); a clean/CI build reproduces it, an incremental build masks it. **a11y contracts:** caeTooltip/caeMenuTriggerFor apply to **focusable hosts only** — for the `<cae-button>` wrapper, its own `tooltip`/`menuTriggerFor` inputs forward `MatTooltip`/`MatMenuTrigger` to the inner focusable `<button>` (**#36 + #57 DONE**; the `CaeTooltipPosition` alias + the structural `CaeMenuPanelHost` seam live in `caelum/shared` [type-only], so `caelum/button` never runtime-depends on `caelum/tooltip`/`caelum/menu`; `menuTriggerFor`'s trigger is opt-in via a **two-branch template** so plain buttons carry no spurious `aria-expanded`; cae-button's **`disabledInteractive`** input (#58) keeps a *disabled* wrapper focusable/hoverable/tabbable — Material drops the native `disabled` attr for `aria-disabled` — so the forwarded tooltip/menu still surface (the `<p-button pTooltip disabled>` parity case); the action is NOT auto-suppressed (guard `(click)`/`(ngSubmit)`; a bound `menuTriggerFor` stays openable while interactive-disabled → unbind it, `[menuTriggerFor]="busy ? undefined : menu"`, to block); **`CaeMenu.getMenuPanel()`** is the public panel seam that unblocked #36's `@internal`-`panel` blocker); **cae-tree follows the WAI-ARIA roving-tabindex pattern** (plain-text labels + CDK `(activation)`, NOT a button per node — verify arrow-key nav in a real browser at M4, #41). **Every slice on a public API / a11y is adversarially reviewed** (4-lens ultracode workflow — batch 3's caught 3 MAJORs). **All dependency vetting runs against D-11 + the automated gate** — `provenance/allowlist.json` (source of truth) + `scripts/check_provenance.py`; **adding a runtime dep REQUIRES an allowlist row or CI (`static gates`) fails** (the forward ratchet). **CI is real+required** — a clean local `bash scripts/preflight.sh` (**0 skipped** with Node on PATH) means CI should be green; CI/preflight commands live in `.github/workflows/ci.yml` + `scripts/preflight.{sh,ps1}` (mirrors — change one, change the others). **Toolchain:** Node 22 at `~/nodejs` — prefix `PATH="$HOME/nodejs/bin:$PATH"` (session-only; do NOT edit shell profiles — the classifier blocks it). verify: `gh issue list` (open #7, #8, #12, #15, #33, #41, #48, #51, #54, #61, #70, #72, #75, #78, #79, #80, #84, #90, #91; #1–#6/#9/#10/#11/#13/#18/#21/#26/#27/#28/#29/#32/#36/#39/#40/#46/#47/#57/#58/#68/#73/#77/#83/#85/#88 closed) · `gh pr list` · `PATH="$HOME/nodejs/bin:$PATH" npm ci && npm run build:lib && npx ng build forge && CI=true npx ng test` (**181 pass** — 156 lib + 25 forge) · `python3 scripts/check_provenance.py` (GREEN) · `PATH=... npm run format:check && npm run lint` · `PATH=... bash scripts/preflight.sh` (PASS 0-skipped); components live in per-component secondary-entry-point folders `projects/caelum/{accordion,button,card,checkbox,chip,divider,input,progress-bar,progress-spinner,radio,select,select-button,textarea,tabs,toggle-button,tooltip,menu,stepper,tree,switch,form-field,shared}/` (each published as `caelum/<name>`; `form-field` = the shared `CaeFormFieldControlBase`, `shared` = type-only; barrel at `projects/caelum/src/public-api.ts`), Forge in `projects/forge/src/app/`; theming bridge in `projects/caelum/styles/`; provenance gate + allowlist in `provenance/`; library green: `cd textbooks && python3 tools/_gen_sections.py` (21 books/348) + `python3 tools/_audit_refs.py` (refs 660/0, routing 82/82, links 144/0); research audit from repo root.

## Read these first (in order)

1. [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) — the shape, the invariants, and the decision log (`D-01..`, Appendix A).
2. [docs/ROADMAP.md](docs/ROADMAP.md) — milestones `M0..`, each with goal / exit-criterion / status; the live plan.
3. [PROJECT_CONVENTIONS.md](PROJECT_CONVENTIONS.md) — paths, commands, stack, tracker conventions. **Every skill reads this.**
4. [textbooks/AGENT_GUIDE.md](textbooks/AGENT_GUIDE.md) — the build loop for turning the library into shipped work.

## Source of truth — docs are caches

Truth order: **git/CI &gt; issue tracker &gt; docs &gt; chat memory.** On any mismatch, fix the doc to match reality *before* working — cheap now, poisonous later. **Single-home each fact** — policy here, project mechanics/config in `PROJECT_CONVENTIONS.md`, procedure in the owning skill, operating reference in `docs/`; every other mention is a one-line cross-link, never a second copy (a fact written twice drifts — `README.md` › the single-home rule). Carve-out: **ARCHITECTURE invariants / `D-NN` rows are commitments, not caches** — code contradicting one is a defect or a supersede-candidate (file `bug` or `decision`; never edit the log to match drift). *(The pre-repo carve-out — `PROJECT_BACKLOG.md` as the interim tracker — was retired at go-live 2026-07-01; the tracker is now GitHub Issues.)* Ownership (who updates what, when):

| Artifact | When it's written |
|---|---|
| **Status block** (above) | one line at **every merge** (the merge-time checkpoint); fully at `prepare_compaction`; verified by `onboard` |
| **docs/ROADMAP.md** | the moment a slice/milestone changes state |
| **docs/ARCHITECTURE.md** | only when a `D-NN` decision lands |
| **Issue tracker** | continuously — **defer = file now**, never "I'll note it later" |

## The reference library — use it

[`textbooks/`](textbooks/) is this project's RAG knowledge library (built per-domain; see [textbooks/LIBRARY_SEED.md](textbooks/LIBRARY_SEED.md)). When planning or implementing:

- Route topics via [`textbooks/MANIFEST.json`](textbooks/MANIFEST.json) (`topic_to_books`, `rag_hints`); follow the loop in [`textbooks/AGENT_GUIDE.md`](textbooks/AGENT_GUIDE.md).
- **Verify every `Book NN §X` citation against [`textbooks/SECTIONS.json`](textbooks/SECTIONS.json) before asserting it** — grep it, don't load it whole. The library is audited; don't invent sections.
- Pre-mortem with `textbooks/reference/ANTI_PATTERNS.md` + `SYMPTOMS.md`; resolve architectural forks with `DECISION_TREES.md`.
- **Frontier topics** live in [`research/`](research/) — survey notes (every claim a **real fetched URL + accessed date + tier**), pre-registered experiments (`EXP-NN`), and paper-style reports (`RR-NN`) that ground `D-NN` decisions like book sections do. **Cite as `research/notes/<file>.md` / `RR-NN`, never as a `Book §`** — different trust models; notes stale ~2 quarters. Discipline + audit: [research/README.md](research/README.md).

## How we work — the daily loop

This project runs on autopilot. Day-to-day, the human's input is mostly three moves:

### 1. "Welcome back, let's onboard and continue" → run [`onboard`](.claude/skills/onboard/SKILL.md)
Preflight → anchors → reconcile docs vs tracker → **read the decision answers** (issue comments) + digest → resume the [build loop](textbooks/AGENT_GUIDE.md). The skill owns the detail, including the edge cases (dirty tree, red main, dead `gh`).

### 2. "Let's prepare for compaction" → run [`prepare_compaction`](.claude/skills/prepare_compaction/SKILL.md)
Rewrite + stamp Status → **verify its claims against the tracker** → update ROADMAP/ARCHITECTURE → sweep deferrals into tickets → clean tree, pushed via the checkpoint path → emit a ready-to-paste `/compact` command. If merge-time checkpointing happened all along, this is verification, not archaeology. **The agent raises this proactively** — at a clean checkpoint (just merged, tree pushed, no in-flight slice) after a heavy session, it recommends compacting rather than letting the window force an unfocused auto-compaction, **and carries the ready-to-paste `/compact` command in that same recommendation** (not a bare "want me to prepare it?"); the skill owns the timing signals.

### 3. A decision is needed → surface a fork; don't guess, don't stall
Architecture and scope are ~1× leverage (the human's judgment). When a real fork appears:
- **File it as an issue labeled `decision`** (template provided): 2–4 options, each with its trade-off and the `Book NN §X` / `DECISION_TREES Dn` behind it, **recommended default first** (per the policy below).
- **Reversible fork?** Proceed **provisionally** on the recommended default and keep working — the issue states the objection window; every PR built on the default carries `Provisional on #NN`. The human overrules asynchronously by commenting; **silence past the window ratifies the default**.
- **Hard to reverse** (public API shape, data schema/migration, external commitments, money)? Don't guess: park the slice, pick the **next independent slice** from the tracker. **Nothing independent left?** Strengthen the blocked decision instead — `research_topic` the fork, spike on a throwaway branch (never merged) — then checkpoint and end the session cleanly. Don't invent roadmap scope to stay busy.
- Once the human decides (a comment on the issue *is* the decision): record it as `D-NN` in [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md) Appendix A, reflect it in the ROADMAP, close the issue.

Other messages happen, but these three are the spine. Default to continuing the roadmap autonomously between them.

## Definition of done

Nothing is "done" until [`definition_of_done`](.claude/skills/definition_of_done/SKILL.md) passes its gates **with evidence** (output, not assertion): **build · tests · a11y parity (axe + keyboard) · token-only theming (no hardcoded design values) · adapter isolation (no 3p import outside its adapter) · provenance scan (US-origin + free, transitive tree) · liveness (it demonstrably does something end-to-end — inert hard-blocks, ambiguous soft-flags) · anti-patterns avoided · determinism (where required) · performance (profiled, not guessed) · milestone exit-criterion · backlog updated · no unticketed deferrals.** Skills live in [`.claude/skills/`](.claude/skills/).

## Working style — the policy the executing agent runs on

- **Ambitious destination, staged route.** Default to the most complete end-state, delivered through small, independently verifiable slices that each build *into* it. Prefer an easier-to-test intermediate **when it's a stepping stone, never as a scope cut**: a cheaper *first step* toward the same end-state is the default; a cheaper *final scope* requires an explicit `D-NN` decision. A slice is **coherent, not fragmented** — bundle tightly-coupled edits and the doc/ROADMAP updates they imply into one PR rather than splitting a single change across trivial PRs (`PROJECT_CONVENTIONS.md` › Right-sized slices).
- **Laziest sufficient code.** Ambition is about the *outcome*, not the line count — within a slice, write the least code that fully does the job. Before adding, climb the ladder and stop at the first rung that holds: does it need to exist at all (YAGNI), is it already in the codebase, the standard library, a native platform feature, an existing dependency, or a one-liner — only then write a minimal implementation. The best code is the code you never wrote. **Never** simplify away the non-negotiables, though: validation at trust boundaries, error / data-loss handling, security, accessibility, determinism where required, or anything the human explicitly asked for. And laziness *without first understanding the code it touches* is the dangerous kind — it ships a confident wrong fix. (`adversarial_review` carries the matching over-engineering lens; inspiration: the ponytail project.)
- **Ticket-first.** No slice without an issue; the branch and PR reference it. A TODO entering code must carry a ticket — `TODO(#NN)` — a naked TODO is a defect (gated by CI and `definition_of_done`).
- **Defer = file now.** The moment work is deferred or an idea worth keeping appears, file the issue (`track_followups`) — then continue. Promises in chat don't survive compaction; tickets do.
- **Checkpoint at every merge.** Update the Status line + ROADMAP state in the same breath as the merge. Compaction can then strike at any time and lose at most the in-flight slice.
- **One writing session per repo.** Concurrent sessions race on the Status line, ROADMAP state, and D-NN numbering. A second simultaneous session works read-only or in its own `git worktree` + branch — never two writers on one checkout.
- **Token discipline.** Grep the big indexes (`SECTIONS.json`) — never load them whole; `MANIFEST.json` is small by design: load it once, keep it resident. Read narrowly; delegate broad reads to read-only subagents and keep conclusions, not file dumps (cheap/fast models for mechanical sweeps, the strongest for adversarial lenses). Detail lives in skills (lazy-loaded), not in this file.
- **Follow the written system.** These docs and skills encode the judgment; prefer them over improvisation, and verify with evidence — never claim green without output. Don't re-litigate settled `D-NN` decisions. Never force-push or admin-merge — the gates exist to gate (`PROJECT_CONVENTIONS.md` › Merge policy).
- **Keep the library honest.** If you extend `textbooks/`, regenerate `SECTIONS.json` and run the audits (they exit non-zero; CI enforces them).
