# TEMPLATE_VERSION — provenance stamp (one `key: value` per line).
# Written by `onboard` (Mode A) right after the template is copied; re-stamped by
# `update_from_template` after every sync. `update_from_template` diffs the upstream
# template from `sha` to its HEAD to know what changed — without this stamp, every
# template update is hand archaeology.
#
# NOTE (2026-07-17, #518): sync d04440a→79fdfbe (8 commits). REFLUX-HEAVY — most
# upstream commits fix intakes Caelum itself filed (pyxis#287/#288/#289, all CLOSED),
# and Caelum's PR #507 (a7823dd) had independently fixed the SAME encoding + banner
# defects locally (its comments cite #503/#489). This sync CONVERGES Caelum's local
# form to pyxis's canonical #296/#297/#299 so future syncs stay clean.
# WHOLESALE (16 machinery files): inject_rule_reminders (#290 AskUserQuestion elicitation
# matcher); session_start_banner (#297 classify()+--selftest corpus — SUPERSEDES Caelum's
# equivalent #507 inline regex fix; identical regex, adds the corpus the new gate runs);
# research_topic SKILL (#313 worker-contract steps 3-5); update_from_template SKILL (#294/#295
# 4th/5th mirrored-constant note); understand_intent SKILL (NEW, #290); decision.md (#290
# consequence framing); metrics.py (#296 encoding + #295 CHECKPOINT_PREFIXES=("checkpoint/",));
# slice_telemetry + _audit_research + _audit_refs + _gen_sections + audit_docs + audit_repo_links
# + audit_staleness + _audit_links (#296 stdout-guard convergence); _audit_routing (#293 count-
# anchor tolerates markdown markers). BY HAND (structural divergence — Caelum bits kept):
# audit_ops_config.py (+import subprocess, +cp1252 guard, +SELFTEST_HOOKS/check_hook_selftests
# #297; KEPT Caelum STAGE_MAP + PREFLIGHT_SHELLS + run_if_node/Invoke-StageIfNode stage regex
# #15); preflight.sh + preflight.ps1 (#299 python3 sentinel inserted; KEPT Caelum run_if_node/
# test_ci bodies). CATALOG: understand_intent registered — README 31→32, MANIFEST skills[],
# on-disk dir; routing audit 32==32. CONTENT: PROJECT_CONVENTIONS #296 encoding-discipline
# bullet; AUTOMATION inject_rule_reminders AskUserQuestion row. SETTINGS: AskUserQuestion hook
# matcher + condensed $comment substance (mechanical, #242); allow/deny UNTOUCHED.
# #485 RESOLVED by this sync: Caelum's checkpoint branches already adhere to checkpoint/<date>
# (drift corrected — #517/#515/#513/#508/#505/#487), so CHECKPOINT_PREFIXES=("checkpoint/",)
# makes the receipts tripwire LIVE; old docs/checkpoint-* PRs self-heal out of the 40-PR window.
# DEVIATIONS — not taken: (1) research/notes/intent-elicitation.md + agent-project-systems.md
# — pyxis-DOMAIN (agent-process research, not Caelum's Angular research domain); understand_intent's
# grounding citation dangles as a code-span (no audit break) → followup #519 to reconcile. (2)
# research/MANIFEST.json routing for those notes — not taken (notes not imported). (3) settings.json
# bare-`python` slice_telemetry grant twins (#314, D-210 dual-spelling) — GRANT, SURFACED to owner
# not applied; Caelum invokes via python3 (already granted) so the bare twin is likely unneeded.
# No upstream FINDING this sync — Caelum's prior reports (pyxis#287/#288/#289) closed as these fixes.
#
# NOTE (2026-07-16, #484): sync 5e8dbc0→d04440a (4 commits). Taken WHOLESALE (all five
# byte-identical to baseline, ZERO Caelum machinery deviations): inject_rule_reminders.py
# v2 (#275 first-push -u/--set-upstream re-key, prefix-independent — was inert on non-slice/
# branches; #279 path-aware git-commit rules from the staged index + gh-issue-close routing),
# ship_pr step-0 claim grep widened to the issue-number invariant (#275), _audit_routing.py
# README skill-count regex anchored on "<N> skills total" (#274 — Caelum README already read
# "31 skills total", audit stays green), update_from_template SKILL steps 3/5 (#276 debt-size
# fork + structural-divergence hand-port note; #277 D-210 dual-spelling surface note),
# metrics.py expects_receipt checkpoint-exempt classifier + flat-shape pin (#269 — Caelum's
# OWN field data motivated it, intake #268). BY HAND: settings.json $comment hook-behavior
# substance ported into Caelum's condensed deviation (allow/deny byte-identical, #242
# mechanical); docs/AUTOMATION.md inject row (identical baseline → replaced). docs/METRICS.md
# REGENERATED from Caelum's local ledger. No deviations withheld — all 4 upstream commits
# applied. FINDING (→ pyxis inbox + #485): the #269 expects_receipt exemption HARDCODES the
# "checkpoint/" prefix (same latent class as the #275 slice/ fix). Caelum's convention IS
# checkpoint/<date>, but recent practice DRIFTED to docs/checkpoint-* → the exemption matched
# ZERO Caelum PRs, leaving 3 historical checkpoint PRs (#478/#480/#483, transient/self-heal)
# flagged beside the 1 GENUINE receipt-less slice #467. Reconciliation = adhere to
# checkpoint/<date> (#485); metrics.py kept WHOLESALE (its mirror value for Caelum's
# checkpoint fact equals upstream — no deviation).
#
# NOTE (2026-07-15, #474): sync b637016→5e8dbc0 (3 commits). Taken WHOLESALE:
# inject_rule_reminders.py (new gh-pr-create + slice-push rules routing to ship_pr's
# owed steps, #266 — from a live Caelum field failure) + _audit_routing.py README
# skill-count guard (#264). Applied BY HAND onto project-mirrored files:
# audit_ops_config.py check 6 (bare-python audit, D-210/#262) + metrics.py
# receipts-coverage tripwire (#266). python3 sweep of the D-210 re-patch's misses:
# preflight.ps1 (10 sites) + metrics_report SKILL (2). Content ports: AUTOMATION hook
# row, README audit-assert clause + Caelum's own count (31, not pyxis's 32), settings
# $comment inventory sentence (grants UNTOUCHED, #242 mechanical). docs/METRICS.md
# REGENERATED locally (never pyxis numbers) — the new tripwire at once flagged Caelum's
# own #465/#467/#468/#470/#471/#473 as receipt-less (the field failure the guard exists
# for; fix = the installed JIT routing, never backfilled receipts). No deviations
# withheld — all 3 upstream commits fully applied.
#
# NOTE (2026-07-15, #463): sync 12f3608→b637016 (11 commits). Taken: deep_brief
# skill (#239) + metrics_report skill + MANIFEST skills[] (catalog now 31); two
# new PreToolUse hooks — block_chained_merge (#253, blocks a checks-watch chained
# into gh pr merge) + inject_rule_reminders (#252, non-blocking rule injection at
# commit/merge/push-main) — wired in settings.json (MECHANICAL hooks + $comment
# only; allow/deny UNTOUCHED, so a single mechanical commit per the new #242 rule);
# slice_telemetry.py (#255/#256) + metrics.py wholesale (per-slice cost & pace /
# guard-hits / preflight-times) + the fail-open slice_telemetry call in BOTH
# preflight shells (mirror kept, 13/13 stages); content ports — Self-Reporting
# Oracle universal anti-pattern (#244/#248) + SYMPTOMS row, PROJECT_CONVENTIONS
# Guard-provenance (#253) + Cost-receipts (#255) bullets, AUTOMATION §1 two hook
# rows + §2 two-commit settings hygiene (#242), MODE_A clean-baseline gate (#243);
# docs/METRICS.md REGENERATED from Caelum's local ledger. DEVIATIONS — not taken:
# (1) design_review skill + SEXTANT mobile-register Compose exemplars/goldens
# (#251) — still deferred #298 (jsdom can't render; gated on #240/M4); (2) pyxis
# docs/METRICS numbers (regenerated locally, not copied); (3) README #240
# PROJECT_BACKLOG de-link — N/A, Caelum already removed all refs at go-live; (4)
# heavy-gate name kept `heavy matrix gate` (#407 deviation held); (5) metrics.py
# python3 comment spelling re-applied (D-210). Upstream finding (reported to
# inbox): pyxis README skill-count line still reads "30 skills" though 32 are on
# disk — same count-drift this sync fixed locally.
#
# NOTE (2026-07-14, #407): sync 4193b6d→12f3608. Taken: audit_docs width-cap
# converged to upstream canonical (#232, superseding Caelum's local #402 impl);
# audit_staleness.py (#222, new, warn-only) + wiring; heavy-gate #206 aggregation
# (classify-died guard) + audit_ops_config check 5 (if-mirror) + PREFLIGHT_SHELLS
# (D-218, two-shell); block_naked_todos \n fix (#207); harvest_downstream skill
# (#148, inert — Caelum has no downstreams, registry knob n/a); skill/doc wording
# ports. DEVIATIONS — not taken: (1) `design_review` skill + capture harness + canon
# still deferred (#298, gated on #240/M4 capture infra; jsdom can't render); (2)
# EXP-05 + pyxis docs/METRICS numbers (pyxis-domain); (3) settings.json PowerShell
# `python3` allowlist entries (Linux-only project; no side-effect allow-widening —
# surfaced to owner); (4) heavy-gate DISPLAY name kept `heavy matrix gate` (not
# upstream `heavy gate`) so the branch-protection required check stays stable —
# owner may align later with a protection update.
#
# NOTE (2026-07-09, #297): the prior sync #259/#260 advanced the ops template
# b263b94→dfd6b47 on disk but MISSED the re-stamp — this `sha` field still read
# b263b94 while disk was already at dfd6b47. True baseline for #297 was dfd6b47.
# DEVIATION — not taken that sync: the `design_review` skill + canon (upstream
# D-137) is deferred (#298) to a slice gated on the #240/M4 screenshot-capture runner
# (jsdom can't render, so its pixel-evidence gate would be permanently degraded);
# re-evaluate at that milestone. Also not taken: pyxis-domain content
# (research/banked/register-game-engine, EXP-03/04/05, docs/METRICS numbers).
source: https://github.com/recon-research/pyxis (upstream identified + first synced 2026-07-08, template-sync #245)
sha: 79fdfbe69d4acea904048cd204e58275b7c043f2 (template sync d04440a→79fdfbe, #518; synced 2026-07-17)
baseline: 276a2d6 (reconstructed — the copy-day HEAD; the original copy recorded no sha, see #245)
copied: 2026-06-25
last_update: 2026-07-17
