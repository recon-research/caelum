# Changelog — Caelum Knowledge Library

All notable changes to the library. Newest first. Add an entry whenever you add/rename/restructure a book, reference doc, or skill, or change counts.

## [Unreleased]

### Template state
- Scaffolded from the project template: `MANIFEST.json` / `SECTIONS.json` / `ROUTING_EVAL.json` skeletons, the four `tools/` audit scripts, the `reference/` and `vision/` doc templates, and `books/00_TEMPLATE.md`.
- All four audits pass green on the skeleton (0 ref misses, all routing cases pass, 0 broken links).

### Outline approved (2026-06-29)
- Domain locked: **Caelum** — a US-origin-clean Angular 22 Material + CDK component library (example system **Forge**). Onboarding produced `docs/ARCHITECTURE.md` (D-01..D-09) and `docs/ROADMAP.md`.
- **20-book / 5-volume outline approved** and recorded in [`LIBRARY_OUTLINE.md`](LIBRARY_OUTLINE.md) (the durable build spec). MANIFEST/SECTIONS still in template state — books not written yet.
- **Next:** `build_library` → research-ground Angular 22 specifics → write Book 01, add its MANIFEST entry, regenerate SECTIONS, keep the four audits green; proceed in volume order.

## [0.21] — 2026-07-01 — migration & adoption (Volume V COMPLETE · **THE 20-BOOK LIBRARY IS COMPLETE**)

### Added
- **Book 20 — Migration & Adoption** (Volume V, **Book 2 — the FINAL book; CLOSES Volume V AND completes the 20-book Caelum library**): the last mile publishing (Book 19) exists to serve — how a team actually LEAVES PrimeNG for Caelum, component by component, without a rewrite. Realizes **D-06** (Caelum is an app-agnostic library a team ADOPTS — its authors never see the consuming app) and generalizes **D-03**/Book 12's adapter isolation from one foreign library to a whole one being removed. **The boundary with Book 19 §3.4** (stated in both directions): ng-update migrations upgrade a team ALREADY ON Caelum across Caelum MAJORS (same-package, version-gated); the p-*→cae-* codemods here move a team FROM PrimeNG (cross-package) — so the from-PrimeNG transform is a **standalone `ng generate caelum:migrate-from-primeng` Schematic, NOT an ng-update migration** (the load-bearing conclusion, grounded in the real Schematics-surface definitions). **The spine:** a UI-library migration is a **STRANGLER FIG** (Fowler; Azure's 4-phase facade→incremental-shift→decommission→remove-facade; Kinney's frontend specialization) — the new system germinates beside the old, behavior moves one vertical slice at a time, both COEXIST, the old host is decommissioned once nothing depends on it. Four moves: **(1) the living map** (reference/COMPARISON.md, seeded from brief §3: p-* → cae-* → Material/CDK primitive → effort tier Direct/Compose/Build/Adapter; a MAPPED row is NOT a migrated component — Book 16 §2.1 — done only at adversarial-passed); **(2) coexistence** (two libraries in one app; theming collisions + the CDK overlay-container z-index conflict + bundle bloat, all tamed by driving BOTH Caelum/Material AND residual PrimeNG from the one token bridge, Book 04; MEASURE the overlap with Book 18's two size gates); **(3) the codemods** (the stable-since-Angular-6 `@angular-devkit/schematics` API; TS imports via the TypeScript COMPILER API — what Material/CDK's internal update-tool uses, NOT ts-morph, and update-tool is NOT public API so imitate-don't-import — + template selectors via template-AST/guarded-regex; a codemod does the MECHANICAL renames and leaves a TODO(caelum-migrate) MARKER at every semantic prop/event remap a human must decide); **(4) convergence** (a forward-only architectural fitness function — Ford/Parsons/Kua's "unit test for architecture" — ESLint no-restricted-imports forbidding NEW primeng/* imports [Book 12 §3.3's "rule that defends itself" generalized to a whole library] wrapped in a RATCHET whose falling count is the PrimeNG-REMOVAL burndown, *necessary-not-sufficient* for parity; the honest limit — no-restricted-imports catches TS imports but NOT <p-*> template selectors, needing a second @angular-eslint template leg). R7 correctly attributed to the **ROADMAP scope note** (NOT a brief label — the brief assumes the team greps its own app; Caelum being app-agnostic ships the usage-mapping guide instead). **No new frontier research note** (the Schematics API is stable-architecture, the methodology canonical — Fowler/Azure/Ford/ESLint/angular.dev cited in §8; v22 specifics verify-at-M0). (+17 sections)
- **`reference/COMPARISON.md`** (new domain reference doc — the first of the LIB-DOCS reference set filled): the **canonical LIVING PrimeNG→Caelum component map** — every p-* to its cae-* equivalent, the Material/CDK primitive under it, and the effort tier — seeded from brief §3 + the real ~95-component PrimeNG showcase inventory, with the source-selector versioning table (Dropdown→Select, Calendar→DatePicker, Sidebar→Drawer, OverlayPanel→Popover, InputSwitch→ToggleSwitch, TabView→Tabs, VirtualScroller→Scroller) and a "how to use" (grep your app first, seed the ledger, run the codemods, hold the line forward). Registered in MANIFEST reference_docs.

### Changed
- MANIFEST: `total_books` stays **20** (`total_volumes` 6); Volume V `volumes[]` → **COMPLETE** (books [19,20]); full `book_20` entry + `topic_to_books` (18 migration/adoption routes) + `rag_hints` (7) + `reference_docs` COMPARISON entry; `coverage_gaps` — flipped **PrimeNG→Caelum migration & adoption → covered (Book 20)** and rewrote the description to **THE 20-BOOK CAELUM LIBRARY IS COMPLETE**; `last_updated` 2026-07-01.
- ROUTING_EVAL: +4 cases for Book 20 (migrate component-by-component; the p-*→cae-* map/comparison; write a rename codemod/schematic; the no-restricted-imports ratchet fitness function) → **82 cases** (no routing regression).
- **Book 20 built via multi-agent workflow (ultracode):** a **5-facet ground+research fan-out** (brief map extraction · adapter-erosion grounding + exact citable §ids · Angular-22 schematics/codemods · the real PrimeNG p-* inventory · strangler-fig/fitness-function methodology — all fetched 2026-07-01), then I wrote Book 20 + COMPARISON.md + wiring, then a **4-lens adversarial-review fan-out** (fabrication/citations+R7 · the Book 19 boundary + strangler-fig soundness · technical/codemod-sample correctness · consistency/COMPARISON-accuracy+over-engineering). Applied **2 MUST-FIX** — the strangler-fig **facade mis-mapping** (§2.1 called the cae-* surface "the facade" but §3.6 removed a *different* facade; recast: a UI migration has no runtime routing facade, the coexistence layer is the facade analogue, the cae-* surface is the new-system abstraction) and the **missing rich-text-editor Adapter row** in COMPARISON (one of the three headline gaps, Book 15 — added) — plus **SHOULD-FIXes** (the burndown measures PrimeNG-*removal* not parity → qualified necessary-not-sufficient across §1/§2.3/§3.5/§3.6; the ledger chain aligned to Book 16 §2.2's state names with mapped/renamed as Book-20 pre-states; the §4 codemod sample no longer hardcodes p-table→cae-data-grid as a mechanical rename [it's a marker-only human call]; the niche-tail contradiction split into conditional-Build vs out-of-scope; the dropped p-mention row added) and **NITs** (renameTag longest-first/boundary guard; inline-template caveat; two-not-three schematic surfaces in the §4 caption; ~95-vs-brief-~90 reconciled; selectorless "stabilized"→"advancing"; single-home the rename list to COMPARISON). Both fabrication + boundary verdicts confirmed the citation graph, R7 attribution, and Book 19 boundary **SOLID** ("tried to break, could not").
- SECTIONS regenerated: **21 books / 348 sections** (incl. the book_00 template; Books 01–20 curriculum). Docs synced: `LIBRARY_OUTLINE.md` Progress (**20/20 — LIBRARY COMPLETE**), `README.md`, `CLAUDE.md` Status, `docs/ROADMAP.md` (M0), `PROJECT_BACKLOG.md` (LIB-1 done).

### Audits
- All five green: refs **660 checked / 0 misses**, routing **82/82**, links **144 / 0 broken**, sections regenerated (**21 books / 348 sections**), research audit **0 errors / 0 warnings**.

## [0.20] — 2026-06-30 — packaging, versioning & distribution (Volume V OPENS)

### Added
- **Book 19 — Packaging, Versioning & Distribution** (Volume V, **Book 1 — opens the Distribution & Adoption volume**): the **how-to-publish** to Book 18's **why-the-shape-matters** (same why/how boundary as 16↔17 and 17↔18 — Book 18 §3.3 owns *why* a tree-shakable secondary-entry-point package is worth shipping, this book owns *how* to build/version/ship it). **The keystone:** a published package carries **two orthogonal provenances** — npm/SLSA **build-origin** provenance (which repo/commit/CI job produced the tarball, via OIDC + Sigstore/Rekor) vs Caelum's **US-origin** attestation (who authored/owns the code + its transitive tree, D-05/D-10, Book 03 §2.3); **neither implies the other** (neither necessary nor sufficient for the other), so a release ships **both** — the build provenance recorded in the registry/transparency log, the US-origin manifest **inside the tarball**; "we have npm provenance so we're US-origin-clean" is the trap. Five moves: **(1) build** — the v22 library builder **`@angular/build:ng-packagr`** (always AOT, always **partial compilation**) → the **Angular Package Format** (FESM2022 + bundled `types/` + a generated `package.json`); `ng build <lib> --configuration production` → `dist/` **is** the package. **(2) consumer contract** — the **`exports`** map keys each entry point by `types`/`default` **only** (the research caught that **`esm2022` is NOT a v22 condition** — a blog myth), and **one secondary entry point per component/adapter** (a subfolder with its own `ng-package.json`, Material's shape) is the package-level meaning of "adopt component-by-component"; a `sass` condition exposes Book 04's theme assets. **(3) dependencies** — `@angular/*` as **`peerDependencies`** not `dependencies` (else a duplicate-Angular-instance break) with the **observed** `^22.0.0 || ^23.0.0` multi-major range (angular.dev prescribes none; it's CDK/Material's convention). **(4) versioning** — Angular **semver** (breaking only in a major, ≥2-major/≥12-month deprecation window) + a **lockstep Caelum-major-per-Angular-major** + an **`ng-update`** migration collection (distinct from Book 20's `p-*`→`cae-*` codemods). **(5) distribution** — publish from **GitHub Actions via npm Trusted Publishing** (OIDC `id-token: write`, no stored token) → provenance auto-generated for the public repo/package = **SLSA Build Level 2**, Sigstore-signed, npm badge; **separately** emit the US-origin attestation into the tarball. Release checklist wires into `definition_of_done`'s provenance gate; the shipped-library size gate (Book 18 §3.2) + the US-origin-attestation emitter are **M0-4** design tasks. Realizes **D-06**. (+17 sections)

### Added — research
- **`research/notes/angular-22-packaging.md`** (new frontier note; routed in `research/MANIFEST.json`): gathered via a **5-facet research fan-out** (ng-packagr build flow · APF `exports`/secondary entry points · peer deps/lockstep · semver/deprecation policy · npm provenance/SLSA) fetched this session from angular.dev (v22.0.4 footers), the ng-packagr repo/schema, the angular-cli spec, docs.npmjs.com, and slsa.dev. Directly grounded: `@angular/build:ng-packagr` (always AOT + `"compilationMode": "partial"`), APF output (FESM2022, bundled `types/`, `"type": "module"`, `"sideEffects": false`, the `exports` map keyed `types`/`default`, secondary entry points via a subfolder `ng-package.json`), ng-packagr 22.0.0 MIT (main `22.1.0-next.2`), the `^22.0.0 || ^23.0.0` peer convention + Material pinning CDK at `22.0.2`, Angular semver + 6mo-Active/12mo-LTS + the ≥2-major deprecation window, the `ng-update` field schema (from the angular-cli spec), and npm Trusted Publishing/`--provenance` → SLSA Build L2 via Sigstore (Fulcio + Rekor). **Fabrication traps caught:** `esm2022` is not a v22 `exports` key; angular.dev prescribes no peer-range syntax; the `ng-update` schema is from an undated CLI-spec page — all flagged verify-at-M0. **Keystone:** build-origin provenance is **orthogonal** to the US-origin claim.

### Changed
- MANIFEST: `total_books` 19→20, **`total_volumes` 5→6** (adds **Volume V — Distribution & Adoption**, `volumes[]` entry `books: [19, 20]`); `book_19` entry + `topic_to_books` routes (packaging/versioning/semver/distribution/ng-packagr/exports map/peer dependencies/ng-update/npm provenance/slsa/trusted publishing/sigstore/us-origin attestation) + `rag_hints` (publishing/distribution, ng-packagr/APF, secondary entry points/exports map, peer deps/lockstep, semver/deprecation, ng-update, provenance/SLSA/trusted publishing, shipping the US-origin attestation); `coverage_gaps` — flipped **packaging/versioning/distribution → covered (Book 19)** and split out **PrimeNG→Caelum migration/adoption → missing (Book 20)**; description updated to **Volume V OPEN; only Book 20 remains**.
- ROUTING_EVAL: +4 cases for Book 19 (publish/distribute with ng-packagr + APF; peer deps + lockstep + ng-update; npm provenance + trusted publishing + SLSA; shipping the US-origin attestation alongside build provenance) → **78 cases** (no routing regression).
- **Adversarial review (ultracode, 4-lens read-only fan-out):** fabrication/citations · why/how boundary + the two-provenance keystone · technical correctness of the §4 config samples · consistency/wiring. Applied **1 MUST-FIX** — the provenance↔US-origin relation is **orthogonal** ("neither necessary nor sufficient"), not "necessary but not sufficient" (the latter negates the book's own orthogonality example); propagated to the note + MANIFEST — plus **SHOULD-FIXes** (build provenance lives in the registry/Rekor log, *not* the tarball — only the US-origin manifest ships in it; the §4 `dist/package.json` needs a `repository` field or provenance hard-fails; the workflow needs a recent npm for OIDC; the `ng-package.json` `$schema` path must match the nested `dest`) and **NITs** ("ledger"→"manifest/record"; `ng update caelum` unscoped; `./package.json` string-form export; `platform-browser` peer). Both fabrication + boundary verdicts: **free of fabrication, all citations resolve, boundary clean.**
- SECTIONS regenerated: **20 books / 331 sections**. Docs synced: `LIBRARY_OUTLINE.md` Progress (19/20 written, **Vol V OPEN**, Book 20 next), `README.md`, `CLAUDE.md` Status, `docs/ROADMAP.md` (M0), `PROJECT_BACKLOG.md` (LIB-1).

### Audits
- All five green: refs **605 checked / 0 misses**, routing **78/78**, links **136 / 0 broken**, sections regenerated (**20 books / 331 sections**), research audit **0 errors / 0 warnings**.

## [0.19] — 2026-06-30 — performance & bundle budgets (Volume IV COMPLETE)

### Added
- **Book 18 — Performance & Bundle Budgets** (Volume IV, **Book 3 — completes the Quality volume**): the **performance** dimension of "is done," governed by the project rule *profiled, not guessed* — so performance is a **budgeted, gated** dimension (set the number, measure, **fail CI** when crossed, like Book 16's axe gate), and every quantitative claim is a candidate *experiment*, not a memorized figure. Four levers. **(1) Bundle-size gates — the distinctive Caelum insight: TWO gates, not one.** `angular.json` **budgets** (types `initial`/`anyComponentStyle`/`bundle`/…, thresholds `maximumWarning`/`maximumError` + `baseline`+`%`, the gate = *warning proceeds but error FAILS `ng build`*, defaults initial 500kb/1mb + anyComponentStyle 2kb/4kb) gate the **Forge application** build — but budgets are an *application-build* feature and do **not** measure `ng-packagr` **library** output, so the shipped `cae-*` package needs its **own** per-secondary-entry-point size gate (`source-map-explorer`/`size-limit` on the dist), because a library is not an application (Book 01 §2.2); conflating them is the trap. **(2) Tree-shaking as a *packaging* discipline** decided at build time: ship APF/**FESM2022** with **`"sideEffects": false`** (the highest-leverage `package.json` line), **one secondary entry point per component/adapter** (the literal "adopt component-by-component" mechanism — importing `cae-select` never pulls `cae-data-grid`, the shape Material uses), partial compilation, and tree-shakable `provide*()`/`providedIn` services (already Book 12 §3.4's adapter-DI shape). **(3) Lazy loading:** `@defer` deferrable views split **standalone** deps into separate chunks (triggers `on viewport`/`interaction`/`idle`/`hover`/`timer`/`when` + `prefetch`; `@placeholder`/`@loading`/`@error`) — the right tool for Caelum's **heaviest** artifacts, the chart/editor/grid adapters (Books 13–15); plus standalone lazy routes for Forge screens. **(4) Change-detection cost:** zoneless + `OnPush` + signals (the v22 default, Book 01 §3.2) means a `cae-*` re-checks only when its own signals change, **proven** via the Angular DevTools profiler's "Change detection" checkbox (skipped `OnPush` subtrees **grey out**); virtual scroll (Book 10 §3.3, the R1 lever) bounds the DOM independent of dataset size (fixed `itemSize` shippable; autosize experimental) — so grid-at-scale is where Book 10's R1 decision gets its *evidence*. Profiling instruments (all dev-tier): the Angular DevTools Profiler + Chrome DevTools `ng.enableProfiling()`; the discipline is profile-first-optimize-the-measured-hotspot-re-profile — never an unprofiled hunch (the **premature-optimization** guard, backing `optimize_loop`/`profile_subsystem`). Six-leg perf checklist; `definition_of_done`'s perf gate enforces legs 1–2 + 5–6. Boundaries kept clean: Book 17 owns the test *tooling* and handed grid-at-scale *budgets* here (Book 17 §5); Book 19 owns *how* to publish the APF, this book owns *why* (bundle cost). Names but doesn't cover (so silence isn't mistaken for coverage): incremental hydration (`@defer` `hydrate`), runtime Web-Vitals/Lighthouse-CI gates, router preloading, `NgOptimizedImage`. (+17 sections)

### Added — research
- **`research/notes/angular-22-performance.md`** (new frontier note; routed in `research/MANIFEST.json`): gathered via a **6-facet research fan-out** (bundle budgets · tree-shaking/APF · zoneless perf · analysis tooling · grid-at-scale · lazy/@defer) with a synthesis + **fabrication-audit** pass, then first-hand re-fetches of the load-bearing claims. Directly fetched from angular.dev (v22.0.4): the `budgets` model (types/thresholds/gate/defaults), the Angular Package Format (`FESM2022`, `"sideEffects": false`, partial compilation, secondary entry points, `ng-packagr` AOT, tree-shakable providers), `@defer` (standalone-chunk split, triggers, sub-blocks), the DevTools Profiler (bar chart / flame graph / "Change detection" greying / JSON export), and the dev-tier analysis toolchain (`@angular/build` 22.0.4 MIT esbuild 0.28.1/Vite 7.3.5/Rollup 4.60.2; `ng build --stats-json` → esbuild analyzer as the zero-dep first-party path; `source-map-explorer` 2.5.3 Apache-2.0 builder-agnostic; `webpack-bundle-analyzer` webpack-only = fit mismatch). Zoneless/OnPush facts cross-referenced from `angular-22-platform.md`. **Key structural insight:** `angular.json` budgets gate the *application*, not the `ng-packagr` *library* — the shipped-package gate is a separate M0-4 CI mechanism. Caveats flag honestly: **no memorized "zoneless saves N KB"** (measure it — the zone.js tarball size ≠ shipped polyfill); virtual-scroll constants + ng-new generated budgets read from main branch (verify at tag); `@defer` stability stated behaviorally; SSR/incremental-hydration + router-preloading + Web-Vitals under-covered.

### Changed
- MANIFEST: `total_books` 18→19 (`total_volumes` unchanged at 5); Volume IV `volumes[]` entry → `books: [16, 17, 18]` and marked **COMPLETE**; `book_18` entry + `topic_to_books`/`rag_hints` routes (performance, bundle budgets/size gate, tree-shaking/sideEffects, secondary entry points → shared book_18/book_19, lazy loading/@defer, change-detection perf → shared book_18/book_01, virtual-scroll/grid-at-scale → shared book_18/book_10, profiling, source-map-explorer, premature optimization); `coverage_gaps` — flipped **performance & bundle budgets → covered (Book 18)**, description updated to **Volume IV COMPLETE (3/3); only Volume V remains**.
- ROUTING_EVAL: +4 cases for Book 18 (angular.json bundle-size budget that fails the build; sideEffects+secondary-entry-point tree-shaking; @defer lazy loading a heavy chart/editor; profiling change detection + virtual-scroll at scale) → **74 cases** (no routing regression this pass).
- SECTIONS regenerated: **19 books / 314 sections**. Docs synced: `LIBRARY_OUTLINE.md` Progress (18/20 written, **Vol IV COMPLETE 3/3**, Vol V next), `README.md`, `CLAUDE.md` Status, `docs/ROADMAP.md` (M0), `PROJECT_BACKLOG.md` (LIB-1).

### Audits
- All five green: refs **574 checked / 0 misses**, routing **74/74**, links **134 / 0 broken**, sections regenerated (19 books / 314 sections), research audit **0 errors / 0 warnings**.

## [0.18] — 2026-06-30 — testing strategy & tooling (Volume IV 2/3)

### Added
- **Book 17 — Testing Strategy & Tooling** (Volume IV, **Book 2**): the **tooling that executes Book 16's verification** and the tests that reach past a11y. Boundary kept clean — Book 16 owns *what must be verified and why* (the ledger, pre-committed scenarios, the three a11y layers); Book 17 owns *the runner, the harness depth, the visual-regression build, and CI wiring* (visual-regression is a *dimension* in Book 16 §3.5, a *build* here). Spine: **(1)** the test pyramid is **bent for a component library** into a "trophy" — a fat **middle** of component tests, because Caelum's value is behavior at the DOM and most defects are *interaction* defects only visible when a component is mounted and driven (Book 16 §2.1's "looks done" trap, in test form). **(2)** A test is a pre-committed parity scenario (Book 16 §3.1) made **executable** — Book 16 decides *what* + which parity dimension; Book 17 decides *which layer* expresses each clause (validator→unit, keyboard→harness, light/dark/density→snapshot); the ledger states (Book 16 §2.2) are *earned* by these tests passing. **(3)** The **frontier fact**: Angular 22's **default unit-test runner is now Vitest** (via `@angular/build:unit-test` — `tsconfig.spec.json`, `::development`), with **Karma a supported-but-legacy alternative, not removed** (correcting "Karma is dead"); the *stable* path is greenfield, so **Caelum is born on Vitest** and never walks the *experimental* Karma-migration path. Default env is Node + **jsdom** (fast, no real layout); **Vitest Browser Mode** (Playwright provider, opt-in) is the escape hatch for layout/focus/zoneless cases. **Four layers:** unit (pure logic — signals Book 02, the R3 format/parse locale matrix Book 08, validators Book 07 §3.2; TestBed only when DI is needed) → **component-via-harness** (the bulk + the concrete build behind Book 16 §3.3 — a custom `CaeXHarness` per component driving semantic ops via `TestbedHarnessEnvironment` + `documentRootLoader` for overlays Book 09 §3.6, testing interaction not coordinates so it survives refactors, runner-agnostic + portable across jsdom and e2e, awaiting `whenStable`/`forceStabilize` for zoneless settling Book 01 §3.2; also asserts the four CVA states Book 07 §2.2/§3.1) → integration (a Forge screen, adapter **fallback** provider swapped in via `provideCaelumGrid` for determinism *and* to prove the neutrality contract, Book 12 §3.2) → visual/e2e (Playwright). **Visual regression + golden discipline:** Playwright snapshots across light/dark/RTL/parity-density (Book 04 §3.3/§3.4/§3.6) with baselines committed + reviewed like code (never auto-accepted in CI), flake killed at the source (disable animations, pin viewport/fonts, mask volatile regions, *small* tolerance), a harness assertion **preferred** to a snapshot for semantic properties, and **`@axe-core/playwright` on the same browser run** for real-layout a11y (Book 16 §3.2). **Provenance tie-back:** the whole toolchain (Vitest, jsdom, Playwright, `@axe-core/playwright`, axe-core) is **dev-tier** — in `devDependencies`, *excluded from the shipped-tree scan* (Book 03 §3.3/§3.4; the M0-4 gate implements the runtime-vs-dev split, axe's MPL the worked example). Seven-leg test-authoring checklist; `definition_of_done` runs legs 1–4 + 6 as its build/test gate. **Backs the `add_test` skill.** Honest status: **no test infra yet** (M0 not started) — runner choice *provisional pending M0*. (+17 sections)

### Added — research
- **`research/notes/angular-22-testing.md`** (new frontier note; routed in `research/MANIFEST.json`): settles the runner question Book 16 deferred. Directly fetched from `angular.dev` (served as **v22.0.4**): **Vitest is the default runner for new projects** via the `@angular/build:unit-test` builder (`tsconfig.spec.json`, `::development`); **Karma is still supported** as a legacy alternative (*not removed* in v22); **migrating an existing Karma project is explicitly experimental** (the `refactor-jasmine-vitest` schematic is experimental/partial) — so greenfield Caelum is *born on Vitest*. Default env Node + jsdom (happy-dom swappable); Vitest Browser Mode (Playwright/WebdriverIO provider) is the real-browser opt-in. Provenance (all dev-tier): Vitest **4.1.9** MIT (antfu/VoidZero), `@playwright/test` **1.61.1** Apache-2.0/Microsoft/US — governed by Book 03 §3.3's runtime-vs-dev split. Caveats flag honestly: the jsdom/browser-mode specifics are WebSearch-derived (verify flag names against angular.dev/guide/testing/components), and the **library (ng-packagr) vs application test-wiring is an open verify-at-M0 question**.

### Changed
- MANIFEST: `total_books` 17→18 (`total_volumes` unchanged at 5); Volume IV `volumes[]` entry → `books: [16, 17]`; `book_17` entry + `topic_to_books`/`rag_hints` routes (testing strategy, test pyramid, Vitest/Karma runner, component test harness → now shared book_17/book_16, unit/component/integration tests, Playwright, visual regression, golden discipline, test determinism, CI wiring, add_test); `coverage_gaps` — **split** the old combined "testing & performance" entry into **testing strategy & tooling → covered (Book 17)** and **performance & bundle budgets → still missing (Book 18)**, description updated to **Volume IV OPEN (2/3)**.
- ROUTING_EVAL: +4 cases for Book 17 (the Angular 22 Vitest-vs-Karma default runner; writing a custom CDK `ComponentHarness`; Playwright visual-regression + golden baselines light/dark; the test pyramid + CI wiring) → **70 cases** (no routing regression this pass).
- SECTIONS regenerated: **18 books / 297 sections**. Docs synced: `LIBRARY_OUTLINE.md` Progress (17/20 written, **Vol IV 2/3**, Book 18 next), `README.md`, `CLAUDE.md` Status, `docs/ROADMAP.md` (M0), `PROJECT_BACKLOG.md` (LIB-1).

### Audits
- All five green: refs **549 checked / 0 misses**, routing **70/70**, links **131 / 0 broken**, sections regenerated (18 books / 297 sections), research audit **0 errors / 0 warnings**.

## [0.17] — 2026-06-30 — accessibility & parity verification (Volume IV OPENS)

### Added
- **Book 16 — Accessibility & Parity Verification** (Volume IV, **Book 1 — opens the Quality volume**): the verification **engine** for the whole library. Volumes I–III built the components and adapters, each ending with a *parity leg* (Book 09 §3.6, 10 §3.6, 11 §3.5/§3.6, 13 §3.6, 14 §3.4, 15 §3.4/§3.5); this book turns the standing promise **"looks done" is not "is done"** into a repeatable procedure. Spine (`brief §6`/`§7`): **(1) "looks the same" ≠ "is the same"** — visual parity is the *dangerous* moment because keyboard/SR/edge-cases stay unverified, so a component is presumed **not** at parity until a procedure proves it; **(2) the capability ledger** tracks each component through earned states (`untouched → scaffolded → implementer-passed → adversarial-passed`), nothing ships below `adversarial-passed` (ROADMAP M4), and a code change **resets** the state (Book 03 §3.4 re-vet discipline applied to a11y); **(3) pre-committed parity scenarios** — the functional/a11y/visual bar is authored *before* "done," derived from the `p-*` behavior, so it can't silently lower to meet the implementation; **(4) the implementer-vs-adversarial split** — the builder does the implementer pass, a *separate* party mandated to falsify does the adversarial pass (the `adversarial_review` two-party discipline; one agent doing both inherits the implementer's blind spot). Three verification **layers**: **axe-core** (broad + shallow, necessary-not-sufficient, a 0-violation merge gate run in **both themes + parity density**, never the proof); **keyboard** operability via the **CDK `ComponentHarness`** (`TestbedHarnessEnvironment` + `documentRootLoader` for overlays — drives Tab/arrows/Enter/Escape, asserts focus order + dismissal + focus restoration, no unintended trap); and **screen-reader semantics + the manual adversarial pass** (meaningful names, announced state changes via `LiveAnnouncer` — what no engine catches, made a named *ledgered* step). Three parity **dimensions**: functional, accessible, visual (light/dark/RTL + density R4 + token-only, Book 04 §3.4/§3.6). Carries a load-bearing **provenance lesson**: **axe-core is MPL-2.0** — a weak/file-level *copyleft* license Book 03 §2.2 would flag for a runtime dep — yet admitted because it is a **dev/test tool never shipped**, so the copyleft never reaches Caelum's source (Book 03 §3.3's runtime-vs-dev distinction made concrete). Closes with the seven-leg verification checklist; **backs the `verify_parity` skill** and `definition_of_done`'s a11y gate (axe + keyboard, adversarial pass for risky/milestone work); the ledger is the **M4 exit**. Visual-regression is a *dimension* here but a *build* in Book 17; SR automation is the immature frontier layer (kept manual + ledgered). (+17 sections)

### Added — research
- **`research/notes/a11y-testing-tooling.md`** (new frontier note; routed in `research/MANIFEST.json`): the a11y/parity-verification *tooling* grounding for Book 16 — **axe-core 4.12.1 is MPL-2.0** (weak/file-level **copyleft**, *not* permissive), Deque/US, zero runtime + zero peer deps, admissible **only as a dev/test tool** (the runtime-vs-dev distinction, Book 03 §3.3); `@axe-core/playwright` 4.12.1 (MPL-2.0; dep `axe-core`, peer `playwright-core >= 1.0.0`); the Angular CDK component harness (`TestbedHarnessEnvironment` / `SeleniumWebDriverHarnessEnvironment` / `documentRootLoader`) confirmed current in the v22 docs. Caveats flag honestly: the WCAG 2.2 tag coverage + the automated-catch proportion were **not** re-sourced (flagged, not asserted), and the test-*runner* choice is Book 17's. The capability-ledger methodology is a Caelum artifact (`brief §6/§7`), not a tool feature.

### Changed
- MANIFEST: `total_books` 16→17, **`total_volumes` 4→5** (added the **Volume IV — Quality** entry to `volumes[]`); `book_16` entry + `topic_to_books`/`rag_hints` routes (accessibility/parity verification, capability ledger, pre-committed scenarios, axe-core, keyboard operability, screen-reader testing, component test harness, WCAG 2.2, visual parity); `coverage_gaps` — **split** the old "Volume IV" entry into **accessibility & parity verification → covered (Book 16)** and the **rest of Volume IV (testing strategy; performance & bundle budgets) → still missing (Books 17–18)**, description updated to **Volume IV OPEN (Book 16 done)**.
- ROUTING_EVAL: +4 cases for Book 16 (verify a11y/PrimeNG parity not just visually; the capability ledger + implementer-vs-adversarial pass; axe + keyboard via the CDK harness; pre-committed scenarios + SR testing before done) → **66 cases** (no routing regression this pass).
- SECTIONS regenerated: **17 books / 280 sections**. Docs synced: `LIBRARY_OUTLINE.md` Progress (16/20 written, **Vol IV open 1/3**, Book 17 next), `README.md`, `CLAUDE.md` Status, `docs/ROADMAP.md` (M0), `PROJECT_BACKLOG.md` (LIB-1).

### Audits
- All five green: refs **480 checked / 0 misses**, routing **66/66**, links **128 / 0 broken**, sections regenerated (17 books / 280 sections), research audit **0 errors / 0 warnings**.

## [0.16] — 2026-06-30 — the rich-text editor adapter (Volume III COMPLETE)

### Added
- **Book 15 — Rich-Text Editor Adapter** (Volume III, **Book 4 — the third *concrete* adapter, and the one that COMPLETES the volume**): Book 12's pattern (and the Book 13/14 templates) applied to the **rich-text editor**, the third gap Angular Material fills **not at all**. The decisive research finding is the **favorable mirror of the visx result** (Book 14 §2.3): where visx's render core was React-bound, **Lexical's core engine carries no React dependency at all** — core `lexical` is a "dependency-free text editor engine" (sole dep `@lexical/internal`, a zero-dep MIT leaf) that works "independently of any framework or library", and the headless behavior modules (`@lexical/rich-text`/`@lexical/history`/`@lexical/utils`) are React-free too; React is quarantined entirely in the separately-published `@lexical/react` binding the Angular adapter never imports. So unlike D-08, **D-09 stands as-is — no refinement** — and Lexical is consumable in Angular as a *real* adapter. The load-bearing lesson (from the opposite direction to Book 14): the neutral interface made the framework-binding question cost nothing **either way**. Structure is Book 12's: a neutral **`CaeEditorAdapter`** whose **value is a Caelum `CaeDoc`** (opaque/serializable, *not* `EditorState` — no vendor type leaks), a single **`editor.adapter.ts`** membrane over `lexical`/`@lexical/*` (`createEditor`/`setRootElement` onto the component's `contenteditable` + `mergeRegister(registerRichText, registerHistory)`, bridging `registerUpdateListener` → a value signal), fenced by ESLint `no-restricted-imports` **plus the editor-specific clause that `@lexical/react` stays out of `package.json` entirely** so the React peer can't leak (R6); DI swap via **`CAE_EDITOR`** + **`provideCaelumEditor()`**. Two dimensions are **new** to this book. **Form control:** an editor *is* a form field, so `cae-editor` implements **`ControlValueAccessor`** + `mat-form-field` integration + the four honest states, exchanging the neutral `CaeDoc` (Book 07 §3.1/§3.4; Book 06 §3.4) — the heaviest instance of the Book 07 control contract. **A11y in two component-owned places:** the `contenteditable` surface (accessible name + `LiveAnnouncer`, Book 05 §3.2) and a **roving-tabindex toolbar** (`aria-pressed` active marks — Aria toolbar pattern Book 06 §2.2/§3.1, CDK `FocusKeyManager` fallback). The `contenteditable` is themed end-to-end through `--cae-*` tokens (Lexical's `theme` maps node types to Caelum class names; no vendor stylesheet — Book 04 §3.6). **"Size the need":** the editor is **ROADMAP cut order #1** (heaviest gap, narrowest usage), so the no-engine **`PlainContentEditableAdapter`** (contenteditable + CDK toolbar + thin command layer) is often the *preferred* first ship, a selectable provider not a degraded mode (Book 12 §6; `brief §4`). Closes with the **eight-leg editor checklist**. Implements **D-09** — **deferred for the build** (cut order #1) but written in volume order, **COMPLETING Volume III** (the adapter layer). (+17 sections)

### Added — research
- **`research/notes/lexical-editor.md`** (new frontier note; routed in `research/MANIFEST.json`): the editor grounding for D-09 — the **favorable mirror** finding that core `lexical` **0.46.0** (MIT, facebook/lexical) declares **no `react`/`react-dom` dep or peer** (sole dep `@lexical/internal`, a zero-dep MIT leaf), and the headless modules `@lexical/rich-text`/`@lexical/history`/`@lexical/utils` are React-free too; the React peer is quarantined in the separately-published `@lexical/react` binding. The vanilla API surface (`createEditor`/`setRootElement`/`registerRichText`/`registerHistory`/`mergeRegister`/`editor.update`/`registerUpdateListener`). The cleanest single-origin Meta/MIT tree of the three gaps; Yjs flagged as the one new external library for collaboration; final sign-off at M2 — all sourced + tiered + dated (npm registry directly fetched; the `lexical.dev` SPA's deep pages search-sourced, flagged in Caveats).

### Changed
- MANIFEST: `total_books` 15→16; Volume III `books` [12,13,14]→[12,13,14,15] and the volume marked **COMPLETE**; `book_15` entry + `topic_to_books`/`rag_hints` routes (rich-text editor, lexical, contenteditable, `CaeEditorAdapter`, editor-as-form-control, framework-agnostic core, editor toolbar/accessibility; the gap hint now includes book_15); `coverage_gaps` — the rich-text-editor entry flipped **missing → covered (Book 15)**, description updated to **Volume III ALL COMPLETE** (Books 12–15).
- ROUTING_EVAL: +4 cases for Book 15 (neutral `CaeEditorAdapter` single membrane; Lexical-core-framework-agnostic-or-react-bound; editor-as-form-control via CVA with a neutral value; roving-tabindex toolbar + the contenteditable fallback) → **62 cases**. Trimmed generic provenance/transitive wording from the `book_15` MANIFEST entry so the pure-provenance routing query stays on **book_03** (the same near-miss the routing audit caught for book_14 — fixed before green).
- SECTIONS regenerated: **16 books / 263 sections**. Docs synced: `LIBRARY_OUTLINE.md` Progress (15/20 written, **Vol III 4/4 COMPLETE**, Book 16 next), `README.md`, `CLAUDE.md` Status, `docs/ROADMAP.md` (M0), `PROJECT_BACKLOG.md` (LIB-1).

### Audits
- All five green: refs **428 checked / 0 misses**, routing **62/62**, links **126 / 0 broken**, sections regenerated (16 books / 263 sections), research audit **0 errors / 0 warnings**.

## [0.15] — 2026-06-30 — the charts adapter (and a D-08 refinement)

### Added
- **Book 14 — Charts Adapter** (Volume III, **Book 3 — the second *concrete* adapter**): Book 12's pattern (and Book 13's grid template) applied to **charts**, the one gap Angular Material fills **not at all** (a pure third-party case from zero). The decisive research finding: **D-08 endorsed visx, but visx is React-bound** — a hard `react ^18||^19` peer on `@visx/shape`, so it is structurally unavailable to an Angular library. The visx *approach* (render-it-yourself viz primitives over D3) is right; the visx *package* is React-only — so the Angular instantiation depends on **D3's framework-agnostic modules directly** (`d3-scale`/`d3-shape`, ISC, Mike Bostock/US — visx's own substrate). A **refinement of D-08, not a reversal**, filed as `DEC-CHARTS-LIB` for the M2 sign-off — and it cost almost nothing precisely because the neutral interface never named visx (the neutrality test earning its keep before a chart is drawn). Render-it-yourself is the ideal token-bridge fit: D3 emits only numbers + SVG path strings (no DOM, no CSS), so `cae-chart` draws `<path>`/`<rect>`/`<text>` itself with `--cae-*` tokens + a `--cae-chart-series-*` ramp (Book 04 §3.6; Book 13 §3.3). Structure is Book 12's: neutral `CaeChartAdapter` (no `d3` type leaks), a single `chart.adapter.ts` membrane over `d3-*` fenced by ESLint (R6), DI swap via `CAE_CHART` + `provideCaelumChart()`. Two things bite harder than the grid: **a11y** — a chart is not just a picture, so the component carries an accessible name + text summary + a visually-hidden **data-table alternative** + keyboard for interactive points (Book 16); and **"size the need"** (`brief §9.3`) — charts are deferred and the d3 tree is bushier, so the **zero-dependency hand-rolled-SVG fallback** (a sparkline is one `<polyline>`) is often the *preferred* impl for small needs, made a selectable provider by DI (Book 03; Book 12 §6). SVG is the default render target (token-themable, accessible, serializable for SSR); canvas/WebGL is a future large-series mode that loses theming + a11y unless mirrored. Implements **D-08** — **deferred for the build** (team not using charts yet) but written in volume order. (+17 sections)

### Added — research
- **`research/notes/visx-charts.md`** (new frontier note; routed in `research/MANIFEST.json`): the charts grounding for D-08 — the visx-is-React-bound finding (`@visx/shape` hard `react` peer; MIT/Airbnb), and the framework-agnostic D3 alternative (`d3-scale` 4.0.2 + `d3-shape` 3.2.0, **ISC**, Mike Bostock/US, no React peer — scales + arc/line/area generators emitting only numbers + path strings). Feasibility path: D-08 refinement (D3-direct for Angular), the bushier d3 tree vs the lazier hand-rolled path, and the M2 sign-off — all sourced + tiered + dated.

### Changed
- MANIFEST: `total_books` 14→15; Volume III `books` [12,13]→[12,13,14] (grid+charts done; editor in progress); `book_14` entry + `topic_to_books`/`rag_hints` routes (charts, data visualization, visx, d3/d3-scale/d3-shape, render-it-yourself, svg charts, chart accessibility, `CaeChartAdapter`; the gap hint now includes book_14); `coverage_gaps` — **split** the "remaining concrete adapters" entry into the **charts adapter → covered (Book 14)** and the **remaining concrete adapter rich-text editor → still missing (Book 15)**, description updated to **Volume III 3/4**.
- ROUTING_EVAL: +4 cases for Book 14 (neutral `CaeChartAdapter` + token SVG; why-visx-is-React-bound / d3-direct; render-it-yourself + the a11y data-table alternative; size-the-need vs the d3 tree) → **58 cases**. Trimmed generic provenance/transitive wording from the `book_14` MANIFEST entry so the pure-provenance routing query stays on **book_03** (a near-miss the routing audit caught — fixed before green).
- SECTIONS regenerated: **15 books / 246 sections**. Docs synced: `LIBRARY_OUTLINE.md` Progress (14/20, Vol III 3/4, Book 15 next), `README.md`, `CLAUDE.md` Status, `docs/ROADMAP.md` (M0), `PROJECT_BACKLOG.md` (LIB-1 + new **DEC-CHARTS-LIB** for the M2 D-08 refinement).

### Audits
- All five green: refs **349 checked / 0 misses**, routing **58/58**, links **124 / 0 broken**, sections regenerated (15 books / 246 sections), research audit **0 errors / 0 warnings**.

## [0.14] — 2026-06-30 — the first concrete adapter (data grid)

### Added
- **Book 13 — Data Grid Adapter** (Volume III, **Book 2 — the first *concrete* adapter**): Book 12's pattern applied, unchanged, to the advanced data grid — the gap Book 10 §3.5 drew the line at (**server-side everything, grouping/aggregation, column resize/reorder, virtual rows at scale, export** — an *engine*, not a widget, so it sits on the ladder's bottom rung). The engine is **TanStack Table**, chosen because it is **headless — it renders no DOM**: it supplies row models + state + API, so `cae-data-grid` draws every cell itself with `--cae-*` Material markup (Book 04 §3.6) and there is **no vendor stylesheet to fight** (headless is the ideal adapter shape; a batteries-included grid shipping CSS would be worse). Structure is Book 12's, instantiated: a **neutral `CaeGridAdapter` interface** (`CaeColumn<T>`/`CaeSort`/`CaeRowGroup`/`CaeGridDataRequest`, no vendor `ColumnDef` leak — the *satisfiable-two-ways* test, Book 12 §3.1); a **single `grid.adapter.ts`** as the only `@tanstack/*` import (maps `CaeColumn`→`ColumnDef`, composes the opt-in row models, reads rows back into a `viewRows` signal), fenced by the ESLint `no-restricted-imports` rule against erosion (R6, Book 12 §3.2–§3.3); **DI swap** via `CAE_GRID` + `provideCaelumGrid()` (Book 12 §3.4; Book 09 §3.1 shape); and a **`MatTable` + `cdk-virtual-scroll` fallback** (`MatGridAdapter`) on the same token (`brief §4`; Book 10 §3.1/§3.3) — the provenance escape hatch and the neutrality proof, honest that it is a *floor*, not full column-virtualization parity. Covers rendering cells through the token bridge with `cdk-virtual-scroll` (the R1 lever, Book 10 §3.3) and the **thin-vs-thick** fork (own the render vs adopt `FlexRender` — Caelum defaults to depending on the zero-dep `@tanstack/table-core` directly, Book 12 §5); **server-side everything** via the `DataSource` seam (Book 10 §3.1) as `CaeGridDataRequest`/`applyServerResult`, never a full client load; grouping/expansion/**TreeTable** (Book 10 §3.4) as modes of one engine behind neutral state (`role=treegrid`); DI + fallback + **export** (a neutral capability, not a second vendor lock-in). Closes with the **seven-leg grid-adapter checklist** (the concrete analogue of Book 12 §3.6 + Book 10 §3.6). A11y via `role=grid`/`treegrid` + `aria-sort` + `LiveAnnouncer` (Book 05 §3.2); the **Aria Grid pattern** (Book 06 §2.2) supplies the keyboard model, **not** the data engine (Book 10 §3.5's boundary). Implements **D-07**; all TanStack version/API/provenance facts route through **`research/notes/tanstack-table.md`** (cited as a research note, **never a `Book §`**), final transitive-provenance + origin sign-off **defers to M2**. (+17 sections)

### Added — research
- **`research/notes/tanstack-table.md`** (new frontier note; routed in `research/MANIFEST.json`): web-sourced, tiered version & provenance grounding for D-07 / Book 13 — `@tanstack/table-core` **8.21.3** (MIT, **zero deps**) + `@tanstack/angular-table` **8.21.4** (MIT; deps `tslib` + exact-pinned `table-core`; peer `@angular/core >=17`, signal-based `FlexRender`); **headless = renders no DOM**; the core API surface (`ColumnDef`, table instance, opt-in row models); and the clean **two-package, two-permissive-license (MIT + 0BSD `tslib`/Microsoft), US-maintained** runtime tree — with the `tanstack.com`-SPA-403 limits and the origin/transitive sign-off (M2) flagged honestly in Caveats.

### Changed
- MANIFEST: `total_books` 13→14; Volume III `books` [12]→[12,13] (grid done; charts/editor in progress); `book_13` entry + `topic_to_books`/`rag_hints` routes (tanstack table, headless grid, neutral grid interface, `CaeGridAdapter`, server-side grid, grouping/aggregation, column virtualization, virtual rows, grid export, FlexRender; the grid-vs-table and grid-adapter hints now include book_13); `coverage_gaps` — **split** the "three concrete adapters → missing" entry into the **data grid adapter → covered (Book 13)** and the **remaining concrete adapters charts/editor → still missing (Books 14–15)**, and updated the description to record **Volume III 2/4**.
- ROUTING_EVAL: +4 cases for Book 13 (headless TanStack grid behind a neutral interface; server-side grouping/virtual-rows/export; headless-so-render-cells-with-tokens; swap TanStack↔MatTable behind `CAE_GRID`) → **54 cases**.
- SECTIONS regenerated: **14 books / 229 sections**. Docs synced: `LIBRARY_OUTLINE.md` Progress (13/20, Vol III 2/4, Book 14 next), `README.md`, `CLAUDE.md` Status, `docs/ROADMAP.md` (M0), `PROJECT_BACKLOG.md` (LIB-1).

### Audits
- All five green: refs **306 checked / 0 misses**, routing **54/54**, links **122 / 0 broken**, sections regenerated (14 books / 229 sections), research audit **0 errors / 0 warnings**.

## [0.13] — 2026-06-30 — Volume III opens (the adapter layer)

### Added
- **Book 12 — The Adapter Pattern** (Volume III, **Book 1 — opens the adapter layer**): the single discipline that makes depending on a foreign library safe, applied identically to the three genuine gaps (Books 13–15). Core thesis — **the adapter is the bottom rung of the reach-for ladder** (Material→Aria→CDK→bespoke→**adapter**, Book 01 §3.4 / Book 06 §3.1): reached only for an **engine-sized** gap (data grid / charts / editor, `brief §4`) an existing battle-tested MIT US-origin library solves better than a bespoke CDK build, and only after Book 03's admit/reject gate clears. The method is **interface-first**: design a **neutral, app-owned interface** in Caelum's vocabulary (`cae-*`, signal IO, `--cae-*` tokens, domain-typed values) and make the library satisfy *it* — the ports-and-adapters/hexagonal inversion (and GoF *Adapter*); **never leak a vendor type** across the boundary (neutrality test: *satisfiable two ways*, by the library AND the documented fallback — `MatTable`/`contenteditable`). A **single adapter file** is the only code in the whole library that imports the foreign package (a membrane: vendor types inside, Caelum types outside). That one rule ("only the adapter touches it") pays off three ways (`brief §2.1`): **provenance is one file** + the lib's transitive tree (R5; Book 03 §3.2), a failed-revet/acquired lib is **swapped at one file not N call sites**, and dependency accounting stays honest. It is fragile — it dies the first "just this once" direct import (**adapter erosion, R6**) — so it is enforced **mechanically** with ESLint `no-restricted-imports` scoping the package to its adapter file (CI-failing, self-explaining, one allow-listed path), backed by the review lens + the provenance CI gate (Book 03 §3.4). Wired through DI as an `InjectionToken` provided by `provideCaelumX()` (Book 09 §3.1 shape; behavior-by-injection, Book 01 §3.3), so "swap the library" = "change one provider" and the fallback is a first-class selectable impl. The foreign render path is themed through `--cae-*` tokens (Book 04 §3.6) — the candidates are all **headless/render-it-yourself** precisely so the render stays inside the token bridge. Closes with the **adapter-authoring checklist** (neutral / single-membrane / DI-swappable / token-themed / provenance-signed-off / a11y-parity) as this book's discipline leg, the bottom-rung analogue of Book 05 §3.6. Implements **D-03**; concrete adapters are TanStack/D-07 (Book 13), visx/D-08 deferred (Book 14), Lexical/D-09 (Book 15); final transitive-provenance sign-off at M2. (+16 sections)

### Changed
- MANIFEST: `total_books` 12→13; **`total_volumes` 3→4 — added the Volume III object to `volumes[]`** (the adapter layer, books 12–15, in progress); `book_12` entry + `topic_to_books`/`rag_hints` routes (adapter pattern/layer/isolation/erosion, neutral & app-owned interface, ports-and-adapters/hexagonal, no-restricted-imports, swappable dependency, injection token/provide function, headless library, the three gaps / data-grid-chart-editor adapter); `coverage_gaps` — **split** the old "three adapters" entry into the **adapter pattern → covered (Book 12)** and the **three concrete adapters → still missing (Books 13–15 pending)**, and updated the `coverage_gaps` description to record **Volume III has opened**.
- ROUTING_EVAL: +4 cases for Book 12 (wrap a 3p lib behind a neutral adapter; enforce isolation with no-restricted-imports; swappable behind an injection token + provide function; interface-first + token-theme the foreign render) → **50 cases**.
- SECTIONS regenerated: **13 books / 212 sections**. Docs synced: `LIBRARY_OUTLINE.md` Progress (12/20, Vol III 1/4, Book 13 next), `README.md`, `CLAUDE.md` Status, `docs/ROADMAP.md` (M0), `PROJECT_BACKLOG.md` (LIB-1).

### Audits
- All five green: refs **236 checked / 0 misses**, routing **50/50**, links **120 / 0 broken**, sections regenerated (13 books / 212 sections), research audit **0 errors / 0 warnings**.

## [0.12] — 2026-06-30 — Volume II complete

### Added
- **Book 11 — Layout, Panels, Media & Drag-Drop** (Volume II — **the book that completes it**): the structural/visual remainder. Core thesis — **the laziness ladder lived**: accordion (`MatExpansionPanel`), card (`MatCard`), tabs (`MatTabGroup`), stepper (`MatStepper`), toolbar (`MatToolbar`), divider (`MatDivider`) are **Direct** Material ports — Caelum's job is to **token-skin them for density/parity** (Book 04 §3.6) and verify the a11y Material ships, **not rebuild** them (the `pDraggable`→CDK / `FocusTrap`→`cdkTrapFocus` / `Ripple`→`matRipple` tail is the same one-line-reach lesson). The genuine builds cluster around **one engine** — `@angular/cdk/drag-drop` (Book 05 §3.4) backs **OrderList** (reorder), **PickList** (two connected drop lists), **FileUpload** (drop target + `HttpClient`, validate type/size at the trust boundary, no first-party uploader → build US-clean), **Carousel** (drag-to-advance over a stepper index), and **Splitter** (draggable, keyboard-resizable `role=separator`) — built once on the CDK, never a foreign drag library (Book 03). The **media family** (a team priority) layers the overlay shell (Book 09 §3.1) for Galleria/Image-preview lightboxes, with large galleries virtualizing (Book 10 §3.3); Carousel's "or vetted US lib if heavy" is a live build-vs-vet fork through the Book 03 gate. The cluster's parity leg — **the most-skipped requirement**: a **keyboard path for every drag** (drag-only fails a11y + parity), `LiveAnnouncer` move announcements (Book 05 §3.2), RTL axes via `Directionality`, drag state in signals under zoneless (Book 01 §3.2), drop validation + virtualization. The honest gaps: Splitter (Build-M) and ScrollPanel (Build-S, as lazy as native overflow + token scrollbars allows). (+17 sections)

### Changed
- MANIFEST: `total_books` 11→12; Volume II books [5,6,7,8,9,10]→[5,6,7,8,9,10,11], marked **COMPLETE**; `book_11` entry + `topic_to_books`/`rag_hints` routes (accordion/card/tabs/stepper/toolbar, splitter/scrollpanel/orderlist/picklist/fileupload, drag-drop, carousel/galleria/image-preview); `coverage_gaps` — "layout/panels/media/drag-drop" flipped missing→**covered** (Book 11), and the `coverage_gaps` description updated to record **Volumes I & II both complete** (the full component-level curriculum authored); the open items are now Volume III (the adapters), IV (quality), V (distribution).
- README + LIBRARY_OUTLINE stats and the Status block updated to **11/20 and "Volume II complete (7/7)"**; Next = Book 12 (The Adapter Pattern), opening Volume III.

### Verified
- Five audits green: 12 books / 195 sections; refs 208 checked / 0 misses; routing **46/46**; links 0 broken; research audit 0 errors / 0 warnings.

## [0.11] — 2026-06-30

### Added
- **Book 10 — Data Tables & Virtualization** (Volume II): the highest-leverage component family (tables top the usage signal). **`MatTable` is a styled skin over the headless `CdkTable`** (Book 05 §3.5); most real tables are a **Compose** of `MatSort` (+ `LiveAnnouncer`/`aria-sort`, Book 05 §3.2) + `MatPaginator` + sticky rows/columns + expandable detail rows (brief §3 data rows). Spine is the **R1 scar — decide grid-vs-table *by row count*, per screen** (brief §8): hundreds of client-side rows → `MatTable` + `MatTableDataSource`; thousands rendered → `MatTable` + `cdk-virtual-scroll` (the R1 lever, Book 05 §3.3); tens of thousands / server-owned / grouping / resize-reorder / export → the **grid adapter** (Book 13, TanStack behind a neutral interface). The **`DataSource<T>` seam** (`connect`/`disconnect`) decides client-side (in-memory) vs server-side (one page per sort/page/filter change) — and *where the data lives* is the silent half (virtualizing the render is useless if the backend shipped 50k rows). Covers virtual-scroll + tables (fixed-height happy path, the sticky-header-survival + variable-height seams, virtualize-OR-paginate), **TreeTable** (`MatTable` over `@angular/cdk/tree`, `role=treegrid` — the *data* cousin of Book 09 §3.5 TreeSelect: TreeTable displays the hierarchy, TreeSelect resolves a value from it), and the grid-adapter line (isolated + provenance-vetted Book 03; the Aria Grid *pattern* of Book 06 §2.2 is a keyboard model, **not** a data grid). Closes with the table-authoring checklist (R1 recorded, data source matches reality, stable `trackBy` identity, a11y role/`aria-sort`/`LiveAnnouncer`, token-only styling Book 04 §3.6, performance profiled at realistic scale Book 18 with OnPush+signal CD Book 01 §3.2). (+17 sections)

### Changed
- MANIFEST: `total_books` 10→11; Volume II books [5,6,7,8,9]→[5,6,7,8,9,10]; `book_10` entry + `topic_to_books`/`rag_hints` routes (MatTable sort/paginate/sticky/expandable, grid-vs-table by row count + large tables, server-side/virtual-scroll, TreeTable); `coverage_gaps` split — "data tables & virtualization" flipped missing→**covered** (Book 10), while "layout/panels/media/drag-drop (Book 11)" remains **missing**.
- README + LIBRARY_OUTLINE stats and the Status block updated to 10/20 and "Volume II 6/7."

### Verified
- Five audits green: 11 books / 178 sections; refs 165 checked / 0 misses; routing **43/43**; links 0 broken; research audit 0 errors / 0 warnings.

## [0.10] — 2026-06-30

### Added
- **Book 09 — Overlay & Menu Components** (Volume II): the floating-UI tier — OverlayPanel/Popover, ConfirmPopup, ConfirmDialog, the menu family (context/tiered/panel/menubar), Breadcrumb, and the value-bearing pickers CascadeSelect, TreeSelect, Mention — all built on the **CDK overlay substrate** (Book 05 §3.1), never hand-positioned `absolute` divs. Spine is the **trust-model split**: a **command overlay** (OverlayPanel, ConfirmPopup, ConfirmDialog, any menu, Breadcrumb) shows transient content or triggers an action and returns nothing to a form (**no CVA**); a **value-bearing overlay** (CascadeSelect, TreeSelect, Mention) is a *form control whose option UI floats* and wears the full Book 07 contract (CVA + `MatFormFieldControl` + library `ErrorStateMatcher`), emitting the **model** (leaf/node-ids/reference), never the display text. Every component runs the same **five-beat lifecycle** (open → position/flip → focus trap-or-rove → dismiss on `Escape`+outside-click → restore focus to trigger), enforced once by a **shared overlay shell** (position presets + scroll/backdrop policy as behavioral DI per Book 01 §3.3; token-only surface per Book 04 §3.6). Menus climb the **reach-for ladder** (Book 06 §3.1): `MatMenu` (Direct) + Menubar → Angular Aria menu (net-new, opens via `cdkConnectedOverlay`, Book 06 §3.2) → CDK Menu (`cdkContextMenuTriggerFor` / nested, Book 05 §3.5); PanelMenu (`MatExpansionPanel`, Book 11) and Breadcrumb (semantic nav + `aria-current`) are inline navigation, not overlays. ConfirmDialog is a build-once `CaeConfirmService` over `MatDialog` (`role=alertdialog`, safe-default focus). Closes with the **overlay-authoring checklist** (dismissal/focus/positioning/roles/token-surface + the value boundary) as this book's parity leg. (+17 sections)

### Changed
- MANIFEST: `total_books` 9→10; Volume II books [5,6,7,8]→[5,6,7,8,9]; `book_09` entry + `topic_to_books`/`rag_hints` routes (overlay/popover/confirm, context/tiered/panel menu + breadcrumb, CascadeSelect/TreeSelect/Mention, anchored overlay components); `coverage_gaps` split — "overlay & menu components" flipped missing→**covered** (Book 09), while "the rest of the component families (data tables, layout/media — Books 10-11)" remains **missing**.
- README + LIBRARY_OUTLINE stats and the Status block updated to 9/20 and "Volume II 5/7."

### Verified
- Five audits green: 10 books / 161 sections; refs 133 checked / 0 misses; routing **39/39**; links 0 broken; research audit 0 errors / 0 warnings.

## [0.9] — 2026-06-30

### Added
- **Book 08 — Numeric, Mask & Specialized Inputs** (Volume II): the directive-heavy parity tier on the Book 07 control contract — InputNumber, InputMask, KeyFilter, InputOtp, Password+meter. Core thesis: **most are format/parse *directives* on a native `matInput`** (composition over a new control, inheriting Material's label/error/a11y), promoted to a thin `cae-*` component only when the DOM shape itself differs (OTP's *N* cells, Password's meter). Spine is the **model/view split (the R3 scar)**: model the *typed* value (a `number`, the unmasked digits, the combined code), display a *localized* formatted string, bridge them with the CVA (`writeValue` formats in, a keystroke parses out) so the form only sees the model — built on the platform's **`Intl.NumberFormat`, not a foreign mask library** (provenance Book 03 + fidelity). Covers caret preservation, `0`-vs-empty/`null`, structural validity via `NG_VALIDATORS`, `beforeinput`-not-`keydown` filtering (IME-safe; key-filtering is *not* validation), OTP roving `FocusKeyManager` focus + paste-spread + `autocomplete=one-time-code`, and the **advisory** token-styled strength meter over a real `ValidatorFn` (scoring lib gated on the Book 03 admit/reject). Closes with the **locale-matrix round-trip test** (en-US/de-DE/RTL + paste + negatives) as the non-negotiable parity gate. (+17 sections)

### Changed
- MANIFEST: `total_books` 8→9; Volume II books [5,6,7]→[5,6,7,8]; `book_08` entry + `topic_to_books`/`rag_hints` routes (numeric/currency formatting, mask/keyfilter/OTP, password meter); `coverage_gaps` split — "numeric, mask & specialized inputs" flipped missing→**covered** (Book 08), while "the rest of the component families (Books 09-11)" remains **missing**.
- README + LIBRARY_OUTLINE stats and the Status block updated to 8/20 and "Volume II 4/7."

### Verified
- Five audits green: 9 books / 144 sections; refs 93 checked / 0 misses; routing **35/35**; links 0 broken; research audit 0 errors / 0 warnings.

## [0.8] — 2026-06-30

### Added
- **Book 07 — Form Control Fundamentals** (Volume II): the control-author's complete contract — every text-like `cae-*` control implements **`ControlValueAccessor`** (the forms-API-agnostic value seam, Book 02 §3.4) **and `MatFormFieldControl`** (the presentation seam into `mat-form-field`, Book 02 §3.6) **together**. Spine: the separation of three responsibilities — value transport (the control), validity *policy* (the form's `ValidatorFn`s), presentation/error-timing (`mat-form-field` + **one library-wide `ErrorStateMatcher`**) — and the **four states a control must render honestly** (value/disabled/touched/errorState) with the contract member behind each. Covers the CVA contract in full (`writeValue` never re-emits `onChange`; `null`/`''`/`0` distinct — value fidelity, R3); validation (the form owns policy; a control may contribute only **structural** validity via the `Validator` interface + `NG_VALIDATORS`, never business rules; cross-field on the group; async via `resource()`/`rxResource()`); error state & timing (`ErrorStateMatcher` default *invalid && (touched || submitted)*, one matcher library-wide for parity R4, `errorState`→`aria-invalid`); the `MatFormFieldControl` surface (`stateChanges` discipline, `setDescribedByIds`→`aria-describedby`); and the **PrimeNG FloatLabel/IconField/InputGroup mapping** (FloatLabel/IconField are `mat-form-field` features; **InputGroup is the honest gap** → a thin `cae-input-group` compose). Closes with the **forms leg of the four-legged stool** (Book 05 §3.6) as a control-authoring checklist. (+17 sections)

### Changed
- MANIFEST: `total_books` 7→8; Volume II books [5,6]→[5,6,7]; `book_07` entry + `topic_to_books`/`rag_hints` routes (form-control authoring, validation, error-state, `MatFormFieldControl`, FloatLabel/IconField/InputGroup); `coverage_gaps` split — "form control fundamentals" flipped missing→**covered** (Book 07), while "the rest of the component families (Books 08-11)" remains **missing**.
- README + LIBRARY_OUTLINE stats and the Status block updated to 7/20 and "Volume II 3/7."

### Verified
- Five audits green: 8 books / 127 sections; refs 78 checked / 0 misses; routing **32/32**; links 0 broken; research audit 0 errors / 0 warnings.

## [0.7] — 2026-06-30

### Added
- **Book 06 — Angular Aria Headless Primitives** (Volume II): Angular's new-in-v22 set of **12** first-party headless WAI-ARIA patterns (Autocomplete/Listbox/Select/Multiselect/Combobox; Menu/Menubar/Toolbar; Accordion/Tabs/Tree/Grid) — directives that supply keyboard/ARIA/focus/screen-reader behavior while you supply DOM + CSS + logic. Core contribution: the **reach-for ladder made precise** (Material → Aria → CDK → bespoke), plus the load-bearing clarification that **Aria stands *on* the CDK, not beside it** (an Aria menu opens through `cdkConnectedOverlay`) — so "Aria vs CDK" is a choice only at the pattern layer, never the infrastructure layer. Headless patterns style off ARIA attributes (`[aria-selected]`) with `--cae-*` tokens; value-bearing patterns wear a `ControlValueAccessor` outside / `[formField]` inside; the posture is **prefer-Aria-but-keep-the-CDK-fallback-live** (Aria is the youngest layer). Refines D-02 (the Material→Aria→CDK ladder, `DEC-ARIA-LADDER`). (+17 sections)
- **Research:** extended `research/notes/angular-22-platform.md` with the **directive-level Angular Aria API surface** — the per-pattern `@angular/aria/<x>` imports + `ng*` attribute selectors; the Listbox inputs (`multi`/`orientation`/`selectionMode`/`wrap`/`typeaheadDelay`) and its `Signal<V[]>` two-way `[(value)]`; the Menu-on-`cdkConnectedOverlay` layering; `[formField]` form integration — all newly fetched and sourced (accessed 2026-06-30), grounding Book 06's frontier claims.

### Changed
- MANIFEST: `total_books` 6→7; Volume II books [5]→[5,6]; `book_06` entry + `topic_to_books`/`rag_hints` routes; `coverage_gaps` split — "Angular Aria headless patterns" flipped missing→**covered** (Book 06), while "the component families (Books 07-11)" remains **missing**.
- README + LIBRARY_OUTLINE stats and the Status block updated to 6/20 and "Volume II 2/7."

### Fixed
- Corrected the Angular Aria pattern count **13 → 12** across the library (research note, Books 01 & 05, CLAUDE Status, ROADMAP, PROJECT_BACKLOG). The Aria overview lists exactly twelve named patterns; the research note's *prose* had miscounted its own (correct) twelve-item list as "13" and the error propagated. Caught by Book 06's research-grounding re-fetch — a small vindication of the rule that frontier counts get verified against a fresh source, not recalled.

### Verified
- Five audits green: 7 books / 110 sections; refs 57 checked / 0 misses; routing **29/29**; links 0 broken; research audit 0 errors / 0 warnings.

## [0.6] — 2026-06-30

### Added
- **Book 05 — CDK Primitives** (Volume II, first book of *Building on Primitives*): the Angular CDK as Caelum's primary build substrate — the headless behavior + accessibility layer Material itself is built on, supplying behavior with **no visual opinion**. Covers the floating-UI substrate (Overlay + Portal); the `a11y` package as the **parity engine** (FocusTrap/FocusMonitor/LiveAnnouncer/InteractivityChecker + the ListKeyManager/FocusKeyManager/ActiveDescendantKeyManager family behind every list-like widget); layout/scrolling incl. `cdk-virtual-scroll` as the **R1 scale lever** (grid-vs-table by row count); `drag-drop` behind OrderList/PickList/FileUpload/Carousel/Splitter; and the structural directives (CdkMenu/CdkListbox/CdkStepper/CdkTree/CdkTable). Lands the **four-legged-stool** method — CDK behavior + token design (Book 04) + signal CD (Book 01) + CVA forms (Book 02) — that every Volume II component follows, and names where CDK pattern directives overlap Angular Aria (Book 06) vs where CDK infrastructure has no Aria replacement. Grounds D-02. **Opens Volume II.** (+17 sections)

### Changed
- MANIFEST: `total_books` 5→6; `total_volumes` 2→3 (added **Volume II — Building on Primitives**, books [5]); `book_05` entry + `topic_to_books`/`rag_hints` routes; `coverage_gaps` split — "CDK primitives" flipped missing→**covered** (Book 05), while "Angular Aria patterns and the component families (Books 06-11)" remains **missing**; `last_updated` + gaps `description` stamped 2026-06-30.
- README + LIBRARY_OUTLINE stats and the Status block updated to 5/20 and "Volume II begun (1/7)."

### Verified
- Five audits green: 6 books / 93 sections; refs 38 checked / 0 misses; routing **26/26**; links 0 broken; research audit 0 errors / 0 warnings.

## [0.5] — 2026-06-29

### Added
- **Book 04 — The Theming Token Bridge** (Volume I): the bridge that makes Caelum's two render paths (Angular Material components + CDK/Aria-built components) look like one design system. A single semantic `--cae-*` CSS-custom-property token set as the **source of design truth**, with Material 22's `--mat-sys-*` system tokens **bound from it** via `mat.theme()`/`mat.theme-overrides()` (platform note); built components read semantic tokens **only** (the token-only invariant — no hardcoded design values); light/dark via `light-dark()` or a theme selector; **density as the PrimeNG-parity lever** (R4); RTL via logical properties + CDK `bidi`. Implements D-04. **Completes Volume I (Foundations, 4/4).** (+17 sections)

### Changed
- MANIFEST: `total_books` 4→5; `book_04` entry + `topic_to_books`/`rag_hints` routes; Volume I books [1,2,3]→[1,2,3,4]; `coverage_gaps` "theming token bridge" flipped partial→**covered**; gaps `description` updated (Volume I complete; Volumes II–V remain).
- README + LIBRARY_OUTLINE stats and the Status block updated to 4/20 and "Volume I complete."

### Verified
- Five audits green: 5 books / 76 sections; refs 19 checked / 0 misses; routing **23/23**; links 0 broken; research audit 0 errors / 0 warnings.

## [0.4] — 2026-06-29

### Added
- **Book 03 — Provenance & Licensing Discipline** (Volume I): the discipline behind Caelum's two hard rules. The license taxonomy (permissive ADMIT vs copyleft/source-available/none REJECT — **free ≠ permissive**, and PrimeUI-style source-available is the trap that drove the team off PrimeNG); the five-check **admit/reject gate** (necessity · license · origin · transitive · isolation); auditing the **transitive tree** with `npm ls` + a license scan (the rule is on the tree, not the package — R5); the runtime-vs-dev split; continuous CI enforcement + lockfile discipline + re-vet-on-upgrade; and the admit/reject **ledger** (`reference/PROVENANCE.md`). License is CI-mechanical; **origin needs a human**. Grounds D-05/D-10. (+16 sections)

### Changed
- MANIFEST: `total_books` 3→4; `book_03` entry + `topic_to_books`/`rag_hints` routes; Volume I books [1,2]→[1,2,3]; `coverage_gaps` "provenance & licensing auditing" flipped partial→**covered**; stale gaps `description` updated to Books 01–03.
- README + LIBRARY_OUTLINE stats and the Status block updated to 3/20.

### Verified
- Five audits green: 4 books / 59 sections; refs 13 checked / 0 misses; routing **20/20**; links 0 broken; research audit 0 errors / 0 warnings.

## [0.3] — 2026-06-29

### Added
- **Book 02 — Reactivity & Forms in Angular 22** (Volume I): signals (signal/computed/effect/linkedSignal/untracked) and the rules that keep them honest; signal-based component IO; reactive forms; and **`ControlValueAccessor` as the forms-API-agnostic bridge** every Caelum control implements (works under classic Reactive Forms AND Signal Forms). The library's first real cross-book citations (Book 01 §3.2/§3.3). (+17 sections)

### Changed
- MANIFEST: `total_books` 2→3; `book_02` entry + `topic_to_books`/`rag_hints` routes; Volume I books [1]→[1,2]; `coverage_gaps` "reactivity & forms internals" flipped to covered.
- README + LIBRARY_OUTLINE stats and the Status block updated to 2/20.

### Verified
- Four textbook audits green: 3 books / 43 sections; refs 7 checked / 0 misses; routing **17/17**; links 0 broken.

## [0.2] — 2026-06-29

### Added
- **Book 01 — Angular 22 Architecture for a Component Library** (Volume I): standalone components as the unit of distribution; zoneless + OnPush change detection a library can rely on; signals & DI; the Material→Aria→CDK reach-for ladder; the Caelum+Forge workspace shape; version lockstep. (+16 sections)
- **Research note** `research/notes/angular-22-platform.md` — web-sourced, tiered, dated version specifics for Angular 22 / Material 22 / CDK 22 / Angular Aria (release 2026-06-03; Node/TS/RxJS floors; zoneless+OnPush defaults and the stable `provideZonelessChangeDetection()`; stable signal/resource/Signal-Forms APIs; CVA-still-the-bridge; the MIT + Google provenance of the core stack). Grounds Book 01's frontier claims; cited as `research/notes/...`, never as a `Book §`.

### Changed
- MANIFEST lifted out of template state: identity filled (series_title; system_name=Forge; primary_assumptions); `total_books` 1→2, `total_volumes` 1→2; `book_01` entry + `topic_to_books`/`rag_hints` routes; `coverage_gaps` rewritten honestly (Books 02–20 still missing/partial).
- README + CHANGELOG identity/stats updated off the template.

### Verified
- All four textbook audits green: 2 books / 26 sections; refs 0 misses; routing **14/14**; links 0 broken. Research-layer audit green (0 errors, 0 warnings).

<!--
Entry format going forward:

## [0.2] — YYYY-MM-DD
### Added
- Book NN — Title (Volume V): one-line summary. (+N sections)
### Changed
- ...
### Fixed
- Citation/routing/link audit fixes.
-->
