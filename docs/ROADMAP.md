# ROADMAP — Caelum

The live plan. Milestone order follows the brief's phased build order ([`_intake/` §5](../_intake/primeng-to-material-migration-brief.md)), adapted to Caelum's scope: an **app-agnostic, public** component library (not an in-place app migration — `D-06`). Onboarding drafted it; every session updates the status of the slice it touched. The `CLAUDE.md` Status block is the 10-line summary of this file.

**Status legend:** ☐ todo · ◐ in progress · ☑ done · ⊘ blocked · ✂ cut

> **Scope note (R7).** With no access to the consuming app we can't grep real `p-*` usage (brief §9.1) — but the team gave a usage signal directly (below). We build the full parity surface, ranked by that signal then by general PrimeNG frequency, and ship an adoption/usage-mapping guide so the team maps their own usage on adoption.
>
> **Team usage priority (2026-06-29, from the team via Connor):** highest dependence — **tables, forms, buttons, dynamic dialog, steppers, tree view, image carousel**. **Charts are not used now** (likely future). Build order is ranked by this; the **charts adapter (D-08) and editor adapter (D-09) are deprioritized** (see Cut order + D-08).

## Milestones

Each milestone has a **goal**, **slices**, a verifiable **exit criterion**, a **leverage** note, and **status**. Slices are tracked as [GitHub Issues](https://github.com/recon-research/caelum/issues) (the repo went live 2026-07-01; the M0 slices are issues #1–#8).

### M0 — Foundation & theming token bridge
- **Goal:** An Angular 22 workspace with the Caelum library + the **Forge** demo console, Material + CDK wired, the theming token bridge established, provenance/adapter ESLint guards in place, and the *Direct* form/button/panel/overlay/menu components ported.
- **Slices:** ☑ Angular 22 workspace (lib `caelum` + app `forge`) — #1, PR #16 (zoneless, ng-packagr lib + `@angular/build:application` app, Vitest, `cae` prefix) · ☑ Material + CDK + Angular Aria install + provenance scan — #2 (Material/CDK/Aria @ **22.0.3** lockstep; Forge `matButton` build smoke green; **license axis GREEN**, **origin GREEN** — ratified as **D-11** (#21 closed: shipped-runtime scope + allied-nation UK/DE fallback; Angular's closure accepted); full report `docs/provenance/M0-2-transitive-provenance-scan.md`) · ☑ theming token bridge (color/surface/spacing/radius/type/focus, light+dark) — #3 (two-tier `--cae-*` tokens + `mat.theme()`/`mat.theme-overrides()` bridge binding `--mat-sys-*` ← `--cae-*`; `light-dark()`+`color-scheme` auto with `[data-theme]` override; shipped as SCSS package assets; Forge consumes it end-to-end with a light/dark toggle; token-only, builds+tests green, prod 202 kB) · ☑ ESLint `no-restricted-imports` + dependency-provenance CI gate + two size gates + US-origin attestation — #4 (curated `provenance/allowlist.json` (D-11 source of truth); node-free `check_provenance.py` runtime-vs-dev gate wired into CI **`static gates`** — real blocking check now; `eslint.config.js` adapter fence (D-03, one explicit `*.adapter.ts` each) wired into the real `lint` job; zero-dep `check-lib-size.mjs` per-entry-point library gate + tightened Forge `angular.json` budgets; `emit-us-origin-attestation.mjs` → `us-origin.attestation.json` shipped in the tarball; all gates GREEN, negative-tested; gates (b)–(d) run in preflight, CI-matrix wiring + required-check promotion deferred to #6) · ☑ Direct components batch 1 — #5 (PR #30): four thin standalone `cae-*` wrappers over Material establishing the reusable patterns — `cae-button` (content projection), `cae-card` (title/subtitle + projection), `cae-checkbox` + `cae-input` (`ControlValueAccessor` form controls, the CVA value seam matching the `p-*` migration target; `cae-input` also forwards native attrs `autocomplete`/`name`/`readonly`/`maxlength`/`inputmode` + buffers IME composition); all `OnPush` + signal inputs (zoneless-compatible, **provisional on #9**), themed for free through the `--cae-*`/`--mat-sys-*` bridge (token-only), single `caelum` primary entry point (per-component secondary entry points → #28). **Forge screen rebuilt as a pure `cae-*` consumer** — a real reactive form (create-workspace) round-tripping through the CVAs end-to-end, with a persistent `aria-live` result region + focus move. Adversarially reviewed (4-lens ultracode workflow; 2 MAJOR + MINORs fixed, each re-tested). Gates: lib+forge build green, **23 tests** (17 lib + 6 forge), lint/prettier clean, size 4.98 kB gzip (61%), provenance GREEN, preflight green. Remaining Direct components → batch 2 (#26) + batch 3 (#27) · ☑ configure_project — CI matrix + required checks — #6: wired the real heavy CI matrix — `build & test` (setup-node@22 + `npm ci` → `npm run build:lib` [caelum + US-origin attestation + gzip size gate] → `ng build forge` [budgets] → `ng test` [Vitest/jsdom, no browser]) on ubuntu+windows, and `format & lint` (`npm run format:check` [Prettier scoped by a new `.prettierignore` — code only; prose/generated trees have their own audits] + `npm run lint`). The M0-4 gates that ran only in preflight (size + US-origin attestation) are now CI-enforced. Preflight scripts (sh+ps1) mirror it — **0 skipped** with Node on PATH; the headless-smoke stage retired (no run loop — static SPA + library). Required checks promoted to `static gates` + a stable **`heavy matrix gate`** aggregation job (over `build & test` ubuntu+windows + `format & lint`) — the per-leg matrix names can't be required directly (a skipped matrix job posts the unexpanded `${{ matrix.os }}` name, deadlocking doc-only PRs; **#32**, found live on PR #34, fixed by the `if: always()` gate). `scripts/preflight.*` kept exempt from the TODO-hygiene gate (they implement the mirrored `todo hygiene` stage). · ☑ Direct components batch 2 — #26: five more Direct wrappers reusing #5's patterns — `cae-radio` (grouped options as data + CVA; unique fallback group name so unnamed groups don't collide via Material's global dispatcher; `ariaLabelledby`), `cae-select` (grouped options CVA, touched on `focusout`), `cae-textarea` (input's multi-line sibling — CVA + IME buffering + native attrs), `cae-tabs`/`cae-tab` (content-projection panels via `<ng-template>`+`contentChildren`, two-way `selectedIndex`), and `caeTooltip` (attribute directive composing `MatTooltip` via `hostDirectives` input-aliasing; documented focusable-host a11y contract). Shared `CaeFormFieldAppearance` type extracted (input/select/textarea) — package-neutral for #28. **Forge** demonstrates all five: form gains `plan` (radio)/`region` (select)/`description` (textarea) CVAs, `cae-tabs` organises the reference panels, `caeTooltip` on a focusable header help button. Adversarially reviewed (4-lens ultracode workflow — cva-forms/api-design/a11y/arch-tabs; 2 MAJOR [radio-name collision, tooltip keyboard-inaccessibility] + 2 MINOR + 2 NIT fixed, each re-tested; arch-tabs clean). Gates: lib+forge build green, **48 tests** (41 lib + 7 forge), lint/prettier clean, lib 9.0 kB gzip (88% of a deliberately-raised 10 kB budget — monolithic entry point grows per batch until #28), Forge 608 kB raw / 124 kB transfer (budgets raised), provenance GREEN, preflight PASS 0-skipped. Follow-ups filed: **#36** (cae-button tooltip-forwarding seam). Remaining Direct components → batch 3 (#27). · ☑ Direct components batch 3 — #27 (PR #42): three **component-based** Direct wrappers reusing #5/#26 — `cae-menu` (+ `caeMenuTriggerFor`: data-driven `mat-menu`; the trigger composes `MatMenuTrigger` and takes the `CaeMenu` instance directly via an `effect`, so no Material type leaks into the consumer — `panel` is `@internal` + tsconfig `stripInternal`; focusable-host a11y contract shared with #36), `cae-stepper`/`cae-step` (content-projection wizard mirroring cae-tabs; two-way `selectedIndex`; **steps stamp eagerly** unlike lazy tab bodies; form controls declared inside projected steps bind to the ancestor FormGroup — ControlContainer resolves through projection), `cae-tree` (nested `CaeTreeNode[]` via Material's modern `dataSource` + `childrenAccessor`, deprecated `TreeControl` avoided; **WAI-ARIA roving-tabindex** pattern — plain-text labels + CDK `(activation)`, collapsed subtrees hidden via CSS). **Forge** rebuilt: header Actions `cae-menu` (functionally fills/resets the form), the create-workspace form as a **3-step `cae-stepper` wizard**, a `cae-tree` workspace structure with a polite live region + first-invalid-step jump on bad submit. Adversarially reviewed (4-lens ultracode — a11y/api-design/projection-reactivity/arch; **3 MAJOR** [tree roving-tabindex broken by a button-per-node → plain-text + activation; Forge invalid-submit dropped focus/announced nothing → jump-to-first-invalid + announce; stepper `linear` hollow with no stepControl seam → dropped] + 4 MINOR + 1 NIT, each re-tested). Dialog/snackbar (the service-based public-API fork) split to decision **#39**. Gates: lib+forge build green, **65 tests** (54 lib + 11 forge), lint/prettier clean, lib 13.4 kB gzip (94% of a raised 14 kB batch budget), Forge 746 kB raw / 149 kB transfer (budgets raised 800/1mb), provenance GREEN (no new deps), preflight PASS 0-skipped. Follow-ups filed: **#40** (linear stepper), **#41** (tree keyboard verification at M4). Remaining Direct-component families → new slices as scoped. · ☑ per-component secondary entry points — #28 (PR #44): split the monolithic `caelum` entry point into per-component tree-shakable **secondary entry points** (`caelum/button`, …, plus `caelum/shared`), mirroring Angular Material (Book 19 §3.2). Additive — `import … from 'caelum'` still works (the barrel re-exports each by package name); no `ng-update`. Each component is a self-contained folder `projects/caelum/<name>/` (ng-packagr's `rootDir` constraint + single-source-ownership drove moving the shared type to `caelum/shared`). Payoff: barrel **13.4 kB → 742 B** (thin shim), `caelum/button` **1.7 kB** (was the whole set). `size-budget.json` now one tight row per component; Forge switched to per-component imports (consumer liveness proof); new `scripts/check-lib-exports.mjs` (self-updating, in `build:lib`) gates the folder↔exports-map↔barrel wiring. Adversarially reviewed (4-lens — 0 BLOCKER/MAJOR; MINOR/NIT fixed: conventions drift, budgets tightened, non-hermetic barrel spec → build-time gate). Gates: 65 tests, provenance GREEN (0 new deps), preflight PASS 0-skipped. · ☑ validation-error forwarding — #29 (PR #49): `cae-input`/`cae-textarea` now reflect the BOUND control's validity in their inner `mat-form-field` (`<mat-error>` + invalid styling + `aria-invalid`), the gap #5 deferred (Book 07 §3.3/§3.4). Self-inject `NgControl` (drop the `NG_VALUE_ACCESSOR` provider) → bridge into the inner field via a per-control `ErrorStateMatcher` delegating *timing* to the DI matcher (uniform library-wide + app-root-overridable; no redundant `CaelumErrorStateMatcher` shipped since Material's default is identical) → recompute in `ngDoCheck` against the live touched/submitted flags (Material's own pattern; `control.events` only nudges CD for programmatic zoneless changes; resilient to `[formControl]` swaps + the `resetForm()` model-before-`submitted` staleness). New `errorMessages` input (`CaeErrorMessages` = validator-key → `string | (error)=>string`, in the type-only `caelum/shared`). **Forge**: per-field messages on name/email/password + `reset()` clears `submitted`. Adversarially reviewed (4-lens ultracode — cva-forms/error-reactivity/a11y/api-design; 11 findings, 2 CONFIRMED MAJOR [Forge reset wall-of-errors → `resetForm()`+the `ngDoCheck` recompute; false `aria-invalid` a11y docstring] + MINOR×3 orphaned-subscription-on-swap → `ngDoCheck` identity re-wire + NITs, all fixed). Follow-ups filed: **#46** shared form-field base, **#47** extend bridge to cae-select, **#48** rich-error escape hatch. Gates: **73 tests** (61 lib + 12 forge), lib caelum-input 5373/6144 + caelum-textarea 4748/5504 gzip, Forge 750 kB, provenance GREEN (0 new deps), preflight PASS 0-skipped. · ☑ cae-select error forwarding + radio/checkbox a11y hook — #47 (PR #52): extended the #29 bridge to **cae-select** (self-inject `NgControl`, per-control bridge `ErrorStateMatcher`, `errorMessages`→`<mat-error>`, `ngDoCheck`→`viewChild(MatSelect).updateErrorState()`; **mat-select reflects `errorState`→`aria-invalid` unconditionally**, no empty-required suppression unlike matInput — documented+tested) and **decided the radio/checkbox story**: they're not `MatFormFieldControl`s, so no `<mat-error>` — instead a **consumer-owned error pattern**, an `ariaDescribedby` input forwarded onto each **focusable inner `<input>`** (Material's own seam; a description on the non-focusable `role=radiogroup` container isn't reliably announced), the consumer renders + points at the message (a built-in `errorMessages` for these is reversible/additive → **#51**). **Forge** closes the plan/region/agree asymmetry: region (select) shows inline `<mat-error>`, plan (radio) + agree (checkbox `requiredTrue`) show consumer errors linked via `ariaDescribedby`; dropped the redundant `role=group` double-grouping. Adversarially reviewed (4-lens ultracode + a fix-delta verifier; **3 CONFIRMED MAJOR** [region select shipped invalid-but-textless — mechanism wired, `errorMessages` map forgotten; radio describedby on the non-focusable radiogroup container; checkbox had no hook despite the "radio/checkbox" JSDoc] + MINOR/NIT, all fixed + re-verified). Gates: **80 tests** (67 lib + 13 forge), lib caelum-select 4940/5632 + caelum-radio 3196/3712 + caelum-checkbox 2401/2816 gzip, Forge 753 kB, provenance GREEN (0 new deps), preflight PASS 0-skipped. · ☑ shared form-field-control base — #46 (PR #55): extracted the byte-identical string-CVA seam + the #29/#47 validation-error-forwarding bridge (self-inject `NgControl` + per-control `ErrorStateMatcher` + `ngDoCheck` recompute + `errorMessages`→`<mat-error>`) that `cae-input`/`cae-textarea`/`cae-select` each carried verbatim into ONE abstract `@Directive()` base **`CaeFormFieldControlBase`**, published as the new tree-shakable secondary entry point **`caelum/form-field`**. Each control now `extends` it and keeps only its specifics (type/attrs/IME for input, `rows` for textarea, `options` for select) + a one-line `updateInnerErrorState()` seam over its own inner `MatInput`/`MatSelect`. Behaviour unchanged (all 72 prior control specs green; a new base spec tests the seam in isolation). Payoff: **−429/+44** across the three controls; per-entry gzip **shrinks** (input 5373→3011, select 4940→2620, textarea 4748→2678 B) into one **5073 B** base. **Packaging gotcha found + fixed (would have broken CI):** ng-packagr's `.d.ts` flattener (rollup-plugin-dts) tree-shakes an abstract `@Directive()` base out of the bundled typings when it's re-exported from a sibling module in a solo entry point (ships empty `export {}`); a clean/CI build reproduces it, incremental masks it — the fix is to DECLARE the class in the entryFile (`public-api.ts`). Adversarially reviewed (4-lens ultracode — inheritance-correctness/behaviour-parity/api-surface/packaging; **0 confirmed MAJOR** — a claimed single-control byte regression refuted as a gzip-additivity artifact; MINOR/NIT fixed: renamed the base `…Base` to disambiguate Material's `MatFormFieldControl` + signal abstract, documented **experimental** for external subclassing [**#54**], reworded/consistency fixes). Gates: **87 tests** (74 lib + 13 forge), lib caelum-form-field 5073/6144 (+ input/select/textarea budgets retightened 3584/3072/3200), Forge 750 kB, provenance GREEN (0 new deps), preflight PASS 0-skipped. Follow-up filed: **#54** (formalize the base's stability contract). · ☑ cae-button tooltip forwarding — #36 (PR #59): the a11y forwarding seam (tooltip half). `caeTooltip`/`matTooltip` attach to their host; on the non-focusable `<cae-button>` wrapper a tooltip was pointer-only and its `aria-describedby` landed off the real control. cae-button now takes a **`tooltip`** (+ **`tooltipPosition`**) input binding `MatTooltip` to its **inner `<button>`** — the natural `<p-button pTooltip>` → `<cae-button tooltip>` swap, keyboard- and SR-accessible; empty `tooltip` disables it. `CaeTooltipPosition` (a Caelum alias of Material's peer `TooltipPosition`) lives in `caelum/shared` (`import type`), matching the cross-component-type convention and avoiding a barrel double-export trap. Tooltip on a *disabled* button won't display visually (native disabled swallows focus/hover) though describedby still reaches a screen reader — documented; `disabledInteractive` → **#58**. **Menu-trigger forwarding split to #57** (real cross-entry-point blocker: `CaeMenu.panel` is `@internal`, stripped from `caelum/menu` typings). Forge demonstrates it on the Theme button. A clean build caught + fixed a real trap — the literal `@internal` inside a class JSDoc is a `stripInternal` marker that had silently dropped `CaeMenuTrigger` from the typings. Adversarially reviewed (4-lens ultracode — a11y-forwarding/api-design/packaging-build/arch-parity; 0 BLOCKER/MAJOR; MINOR/NIT applied). Gates: `build:lib` GREEN (caelum-button 2513/3072 gzip), **92 tests** (79 lib + 13 forge), Forge 754 kB, lint/prettier clean, provenance GREEN (0 new deps), preflight PASS 0-skipped. Follow-ups filed: **#57** (menu-trigger forwarding), **#58** (`disabledInteractive`). · ☑ cae-stepper linear (validity-gated) stepping — #40 (PR #62): `cae-step` gains a **`stepControl`** seam; `cae-stepper` gains **`[linear]`** and drives Material's `selectedIndex` from an `effect` so a REFUSED linear move (which Material makes silently — no event) is reconciled: it reads the actual index back and re-emits, so `[(selectedIndex)]` never diverges from the rendered step. Reads/writes of Material's index are **`untracked`** so the effect depends only on the input + the `viewChild`, not Material's own index signal (which would fight user header clicks); `linear` off = behaviour-preserving (Book 05 §3.5). **Forge** adds a compact **"Invite a teammate"** linear demo (2 validity-gated steps) kept separate from the free-navigation create-workspace wizard so both modes show. Adversarially reviewed (4-lens ultracode; reconciliation core verified SOLID; **2 CONFIRMED MAJOR** [2nd-`[formGroup]` `viewChild(FormGroupDirective)` ambiguity → query wizard form by `#createFormDir`; invite-success focus/announce → persistent live region + focus effect] + MINOR/NIT fixed, both MAJORs regression-tested). Gotcha: ng-packagr's fesm2022 retains JSDoc comments (docstrings ship as bundle bytes; caelum-stepper 3328→3841 gzip). Gates: **100 tests** (82 lib + 18 forge), Forge 760 kB, provenance GREEN (0 new deps), preflight PASS 0-skipped. Follow-up filed: **#61** (per-step `completed`/`hasError` override). · ☑ cae-button menu-trigger forwarding — #57 (PR #64): the sibling of #36's tooltip seam — `cae-button` gains a **`menuTriggerFor`** input binding `MatMenuTrigger` to its inner focusable `<button>` (was pointer-only + ARIA-misplaced on the non-focusable wrapper). A **two-branch template** applies the trigger only when a menu is bound (MatMenuTrigger binds `aria-expanded` unconditionally, so an always-present trigger would announce every plain button as a collapsed disclosure — verified against Material 22.0.4). The panel crosses entry points via a **type-only `CaeMenuPanelHost`** seam in `caelum/shared` (no `caelum/button`→`caelum/menu` runtime dep); **`CaeMenu.getMenuPanel()`** (over the now non-required `@internal` `panel`) unblocked #36's `@internal`-`panel` blocker; cae-button syncs the resolved panel into a local `@internal` signal via an effect (order-independent + sidesteps a dev-mode ExpressionChanged). **Forge** swaps its native Actions button for `<cae-button [menuTriggerFor]="actionsMenu">`. Adversarially reviewed (4-lens ultracode — a11y-forwarding/api-design/reactivity-cd/packaging-treeshake; **0 MAJOR/BLOCKER**; MINOR/NIT fixed: two-branch parity spec strengthened to assert variant/aria-label/disabled on the menu branch, `menuPanel` JSDoc corrected [the effect's real justification], `@internal` closed a `MatMenuPanel` .d.ts leak). Gates: **106 tests** (88 lib + 18 forge), lib caelum-button 3410/4096 gzip, Forge 760 kB, lint/prettier clean, provenance GREEN (0 new deps), preflight PASS 0-skipped. Follow-up filed then done: **#58** (`disabledInteractive`). · ☑ cae-button `disabledInteractive` — #58 (PR #66): the third cae-button a11y forwarding seam (after #36 tooltip, #57 menu). A native *disabled* `<button>` swallows focus + pointer events, so #36's tooltip (and #57's menu) can't surface on a disabled button. An opt-in **`disabledInteractive`** input (booleanAttribute, default off) forwards `matButton`'s focusable-disabled mode to the inner `<button>` (in both template branches): Material drops the native `disabled` attribute for `aria-disabled="true"`, so the button stays focusable/hoverable/tabbable while AT still announces it disabled — the `<p-button pTooltip disabled>` parity case. The action is NOT auto-suppressed (Material's contract) — the JSDoc names the guards ((click)/the enclosing form's (ngSubmit); a bound `menuTriggerFor` stays openable → unbind to block). **Forge** demos it on the linear invite wizard's "Send invite" submit (interactive-disabled + explanatory tooltip until a role is chosen; `sendInvite()` stays the guard; tooltip drops once valid). Adversarially reviewed (4-lens ultracode — a11y-forwarding/api-design/reactivity-two-branch/arch-parity-packaging; **0 BLOCKER/MAJOR**; 1 MINOR + 3 NIT fixed: menu-trigger-openable footgun documented, JSDoc guard-pattern reworded, coercion spec strengthened to assert cae-button's own transform, an overstated describedby comment). Gates: **111 tests** (92 lib + 19 forge), lib caelum-button 3822/4608 gzip (budget bumped for real growth), Forge 760 kB, lint/prettier clean, provenance GREEN (0 new deps), preflight PASS 0-skipped. · ☑ cae-switch (Direct components batch 4) — #68 (PR #69): the first batch-4 Direct component — a thin `cae-switch` over `MatSlideToggle` (`p-toggleSwitch`/`p-inputSwitch` parity; **COMPARISON row 42 → `cae-switch`**, not `cae-slide-toggle`). MatSlideToggle is a boolean `ControlValueAccessor` and NOT a `MatFormFieldControl` (structurally the MatCheckbox twin), so cae-switch reuses the cae-checkbox pattern verbatim: `OnPush` + signal state, boolean CVA (touched on `focusout`, `setDisabledState` ⊕ coerced template `disabled`), content-projected label, `required`/`labelPosition` passthrough, and the consumer-owned `ariaDescribedby` hook (#47) forwarded onto the focusable inner **`<button role="switch">`** (M3's slide-toggle is a button, not an input) — no built-in `<mat-error>`. A **`hideIcon`** passthrough (default `false`) opts into the icon-less p-toggleSwitch look. **Forge** binds it to a new `notify` boolean preference in the create-workspace form (a real CVA round-trip through the same `getRawValue` payload). Adversarially reviewed (4-lens ultracode — cva-forms/api-design/a11y/arch-packaging; **0 BLOCKER; 1 CONFIRMED MAJOR** [cae-switch always rendered Material's thumb glyph `p-toggleSwitch` never shows, no opt-out → `hideIcon` passthrough added; a default-flip for pixel-parity is a separate decision] + MINOR/NIT fixed [Forge narrative "batch 3"→batch 4; barrel comment]). Gates: **122 tests** (102 lib + 20 forge), lib caelum-switch 2596/3072 gzip, Forge 787 kB (< 800 kB warn), lint/prettier clean, provenance GREEN (0 new deps), preflight PASS 0-skipped. Follow-up filed: **#70** (cross-control `ariaLabel`/`ariaLabelledby` label-less seam). · ☑ cae-toggle-button + cae-select-button (Direct components batch 4) — #73 (PR #74): the `MatButtonToggle` pair (COMPARISON row 43). `p-togglebutton` → **`cae-toggle-button`** — a boolean `ControlValueAccessor` over a STANDALONE `mat-button-toggle`; the cae-switch/cae-checkbox twin rendered as a button (inner `<button role="button">` + **`aria-pressed`**, read `$event.source.checked`; content-projected label; `disabled` merge; consumer-owned `ariaDescribedby` #47). `p-selectbutton` → **`cae-select-button`** — the cae-radio twin: a grouped CVA over `mat-button-toggle-group` with `@for` over `CaeSelectButtonOption[]`, single or **`multiple`** (value seam `string` | `string[]`; `writeValue` coerces null→mode-empty; `multiple` is read ONCE at Material's `ngOnInit` → set statically), `vertical`, group named via `ariaLabel`/`ariaLabelledby`, **`required`** → `[attr.aria-required]` (cae-radio parity), and **`hideSelectionIndicator`** for Material's checkmark. **KEY seam:** `mat-button-toggle` (unlike checkbox/radio/slide-toggle) exposes NO `aria-describedby` input and its host is `role="presentation"`, so describedby is forwarded onto the focusable inner `<button>`(s) via an **`afterRenderEffect`** (post-render, reactive on `options()`), not declaratively. Each ships as its own tree-shakable secondary entry point (`caelum/toggle-button`, `caelum/select-button`). **Forge** adds a required `visibility` select-button (consumer-owned error pattern) + a boolean `pinned` toggle-button — real CVA round-trips; Forge initial-budget warn 800→850 kB (`MatButtonToggleModule` new to Forge). Adversarially reviewed (4-lens ultracode — cva-forms/api-design/a11y/arch-packaging; **0 BLOCKER; 2 CONFIRMED MAJOR** [both cae-select-button: no `hideSelectionIndicator` opt-out for Material's selection checkmark, the #68 `hideIcon` precedent → added; no `required`/`aria-required` seam → added] + MINOR/NIT fixed [mode-aware `writeValue`, `effect`→`afterRenderEffect`, `multiple`-init-only doc, Forge describedby test]; 0 refuted). Gates: **146 tests** (125 lib + 21 forge), lib caelum-toggle-button 2768/3456 + caelum-select-button 3686/4608 gzip, Forge 814 kB (< 850 kB warn), lint/prettier clean, provenance GREEN (0 new deps), preflight PASS 0-skipped. Follow-ups filed: **#75** (cae-toggle-button on/off-label parity), **#70** scope extended to both. · ☑ cae-accordion + cae-expansion-panel (Panel/layout Direct row) — #77 (PR #81): the `MatExpansion` pair (COMPARISON row 89, Book 11), a new tree-shakable entry point `caelum/accordion`. `p-accordion` → **`cae-accordion`** — a zero-logic container: **`hostDirectives:[{directive:MatAccordion, inputs:['multi: multiple','displayMode','hideToggle']}]`** + `<ng-content>` (the `multiple` alias = PrimeNG + cae-select-button parity). **No template re-stamping** (unlike cae-tabs/cae-stepper): projected `cae-expansion-panel`s coordinate purely via DI — the inner `mat-expansion-panel` does `inject(MAT_ACCORDION,{optional:true,skipSelf:true})`, walking UP the injector tree, and a content-projected child sees the projecting host's element-injector providers (declaration-tree DI, Book 01 §3.3), so `MatAccordion` on the host is reachable without re-stamping. Single-expand = `UniqueSelectionDispatcher` keyed by the accordion's auto-unique id (no manual group naming, unlike cae-radio), and its auto-close **fires `expandedChange`** so `[(expanded)]` self-syncs with no reconciliation (contrast cae-stepper's silent linear refusal). `p-accordionTab`/`p-accordion-panel` → **`cae-expansion-panel`** — a thin `mat-expansion-panel` wrapper: `title`/`description` header text, `[(expanded)]` two-way (prefer two-way — a one-way `[expanded]` won't re-assert after a user toggle), `disabled`, `hideToggle` (OR-combined with the accordion's — a panel default of false never clobbers), `(opened)`/`(closed)`; `<ng-content>` body; not a form control → no CVA/`<mat-error>`. **Forge** adds a single-expand FAQ accordion in the reference area (liveness proof; Forge 814→837 kB — `MatExpansionModule` new to Forge). Adversarially reviewed (4-lens ultracode — di-coordination/a11y/api-design/arch-packaging; **0 BLOCKER, 0 MAJOR, 0 refuted**; MINOR/NIT applied: `multi`→`multiple` alias, the optional WAI-ARIA inter-header arrow-key roving gap disclosed in JSDoc [Material's `@ContentChildren(MatExpansionPanelHeader)` doesn't cross the wrapper view boundary; → **#79** M4 real-browser verify], `expanded` controlled-binding footgun note, Book 01 §3.4→§3.3 citation fix). Gates: **157 tests** (135 lib + 22 forge), lib caelum-accordion 3514/4096 gzip, Forge 837 kB (< 850 kB warn), lint/prettier clean, provenance GREEN (0 new deps), preflight PASS 0-skipped. Follow-ups filed: **#78** (rich header content slot), **#79** (inter-header roving verification, M4), **#80** (parity extras — togglePosition/afterExpand/lazy body). · ☑ cae-chip (data-display Direct row) — #83 (PR #86): `p-chip` → **`cae-chip`** over a STANDALONE `MatChip` (new entry point `caelum/chip`; MatChip defaults `role=null` + injects no chip-set, so it's valid standalone for the `p-chip` case — a chip *set/grid* for tag entry is a separate slice, #84). Content-projected label; `removable` (default `false` = p-chip parity) renders a remove `<button matChipRemove>` and emits `(removed)`; `disabled`; `removeAriaLabel`. The remove glyph is a **self-authored inline SVG "×"** (stroked `currentColor`), NOT `mat-icon` — Material Icons is a Google-CDN font Caelum avoids (US-origin/no-CDN, M0-2). **Forge** adds a removable tag row backed by a signal list — click × drops the tag, announced in a persistent polite live region with a focus-move effect (Forge warn budget 850→900 kB; `MatChipsModule` ~34 kB new to Forge, app ~871 kB nearing the 1mb ceiling → #85). Adversarially reviewed (4-lens ultracode — a11y-focus/standalone-correctness/api-design/arch-packaging; **0 refuted**; fixed **1 CONFIRMED BLOCKER** [the remove `<button matChipRemove>` rendered Material's default `tabindex="-1"` — only a `MatChipSet` FocusKeyManager promotes it, absent standalone → keyboard-inoperable, WCAG 2.1.1 fail; fixed with explicit `[tabIndex]="0"`, regression-tested since the specs only tested `.click()`] + **2 CONFIRMED MAJOR** [focus stranded on `<body>` + no announcement on removal → Forge live region + focus-move, cae-chip documents the standalone limitation, managed set → #84; false "reachable by Tab" JSDoc → now true + honest]; MINOR/NIT applied). Gates: **167 tests** (144 lib + 23 forge), lib caelum-chip 2681/3200 gzip, Forge 871 kB (< 900 kB warn), lint/prettier clean, provenance GREEN (0 new deps), preflight PASS 0-skipped. Follow-ups filed: **#84** (cae-chip-set — managed removable-chip list with focus redirect + roving nav), **#85** (Forge bundle budget — @defer/route-split, nearing 1mb).
- **Exit criterion:** a representative Forge screen renders on Material with theme parity; the provenance scan is green in CI; every rendered color/space comes from a token variable (no hardcoded values); preflight green.
- **Leverage:** high — scaffolding, install wiring, Direct 1:1 ports, lint rules.
- **Status:** ◐ in progress — the **library prerequisite is DONE.** The Caelum knowledge library (`build_library`, **20/20 books — COMPLETE, all five volumes** (Vol I Foundations Books 01–04; Vol II Building on Primitives Books 05–11; Vol III The Adapter Layer — COMPLETE — Book 12 *The Adapter Pattern* — D-03 — + Book 13 *Data Grid Adapter* — headless TanStack, D-07 — + Book 14 *Charts Adapter* — render-it-yourself D3 SVG, D-08; **visx is React-bound** → D3-direct refinement, filed `DEC-CHARTS-LIB` for M2 — + Book 15 *Rich-Text Editor Adapter* — Lexical behind `CaeEditorAdapter`, the editor as a form control, D-09; Lexical's core framework-agnostic — favorable mirror of visx — so **D-09 stands as-is**; Vol III grounded in `research/notes/{tanstack-table,visx-charts,lexical-editor}.md`); Vol IV The Quality Volume — COMPLETE — Book 16 *Accessibility & Parity Verification* (the capability ledger, the implementer-vs-adversarial split, axe + keyboard + screen reader; backs **M4**'s parity gate; grounded in `research/notes/a11y-testing-tooling.md` — axe-core is MPL-2.0, a dev/test tool only) + Book 17 *Testing Strategy & Tooling* (the test pyramid, **Vitest is the v22 default runner** with Karma legacy, the CDK `ComponentHarness` depth, Playwright + the golden discipline; backs `add_test` + M4's visual-regression suite; grounded in `research/notes/angular-22-testing.md`) + Book 18 *Performance & Bundle Budgets* (performance as a budgeted/gated dimension *profiled-not-guessed*; the **two size gates** — Forge-app `angular.json` budgets vs the shipped-library per-secondary-entry-point gate, the latter a **design task for M0-4**; tree-shaking via APF/`sideEffects`/secondary entry points; `@defer`; zoneless/OnPush CD proven with the profiler; backs **M4**'s bundle-size gate + `profile_subsystem`/`optimize_loop`; grounded in `research/notes/angular-22-performance.md`); Volume V (Distribution & Adoption) COMPLETE — Book 19 (*Packaging, Versioning & Distribution* — the **two orthogonal provenances** [npm/SLSA *build* provenance via Trusted Publishing vs the separately-shipped *US-origin* attestation], `@angular/build:ng-packagr` + the Angular Package Format + the `exports` map + secondary entry points, `@angular/*` peer deps + lockstep semver + `ng-update`; realizes D-06; the shipped-library size gate + the US-origin-attestation emitter are M0-4 tasks; grounded in `research/notes/angular-22-packaging.md`) + Book 20 (*Migration & Adoption* — the living PrimeNG→Caelum `p-*`→`cae-*` map in `reference/COMPARISON.md`, strangler-fig incremental adoption, the standalone `ng generate` codemods distinct from Book 19's `ng-update`, and the forward-only lint-ratchet fitness function generalizing Book 12 §3.3 against adapter erosion); see [`textbooks/LIBRARY_OUTLINE.md`](../textbooks/LIBRARY_OUTLINE.md). **The 20-book library is COMPLETE (five audits green — 21 books/348 sections, refs 660/0, routing 82/82, links 144/0, research 0/0). REPO-1 (public go-live) DONE 2026-07-01 — repo live at [github.com/recon-research/caelum](https://github.com/recon-research/caelum), 12 tracker issues filed (#1–#12), `CI_POSTURE=full`, `main` **protected** (require PR + `static gates`, include-admins, no force-push). **M0-1 workspace DONE** (#1 → PR #16): a zoneless Angular 22 workspace — library `caelum` (ng-packagr) + Forge app (`@angular/build:application`), Vitest, `cae` prefix; `ng build`/`ng test` green. **M0-2 DONE (#2):** Material + CDK + Angular Aria installed at **22.0.3** lockstep (plain `npm install`, no `ng add`); Forge `matButton` build smoke green; the first **transitive provenance scan** ran (D-05/D-10, 15 runtime packages, adversarially verified) — **license axis GREEN** (all permissive+free), **origin GREEN for the shipped browser client** (parse5/entities/@standard-schema flagged non-US but contribute zero shipped browser bytes), **ratified as D-11** (#21 closed — shipped-runtime scope + allied-nation UK/DE fallback, Angular's closure accepted; report `docs/provenance/M0-2-transitive-provenance-scan.md`). **M0-3 theming token bridge DONE (#3):** two-tier `--cae-*` token set + the `mat.theme()`/`mat.theme-overrides()` bridge (`--mat-sys-*` ← `--cae-*`), `light-dark()`+`color-scheme` auto light/dark with a `[data-theme]` override, shipped as SCSS package assets and consumed end-to-end by Forge (light/dark toggle); token-only, builds+tests green (forge 4/4), prod 202 kB. Next: **#4** adapter-isolation lint + provenance CI gate + two size gates + US-origin attestation emitter (automates the M0-2 scan under D-11), then **#5** Direct components batch 1.**

### M1 — Composed components
- **Goal:** The common-case widgets that aren't drop-in but assemble from Material/CDK pieces.
- **Slices:** ☐ ConfirmDialog wrapper · ☐ Menubar (`MatToolbar`+`MatMenu`) · ☐ MultiSelect w/ filter + chip summary · ☐ basic `MatTable` screens (sort/paginate/sticky/expandable) · ☐ TabMenu · ☐ ContextMenu (CDK Menu) · ☐ SplitButton
- **Exit criterion:** the common-case component set is implemented **and parity-verified** (functional + a11y + visual scenarios green per component).
- **Leverage:** high — composition over documented Material/CDK primitives.
- **Status:** ☐ not started.

### M2 — Adapters for the three gaps
- **Goal:** Neutral interfaces + adapters for the three genuine gaps; each vetted candidate prototyped **in isolation** behind its adapter; provenance signed off (`D-07`/`D-08`/`D-09`).
- **Slices:** ☐ neutral grid interface + grid adapter (TanStack candidate) · ☐ neutral chart interface + chart adapter (visx/D3 candidate) · ☐ neutral editor interface + editor adapter (Lexical candidate) · ☐ provenance sign-off record per lib (transitive tree + license)
- **Exit criterion:** one real Forge screen per gap running on the chosen library, **behind the adapter only**, with provenance signed off and the ESLint isolation rule proving no leakage.
- **Leverage:** mixed — interface design + isolation is high; the provenance/legal sign-off is ~1× (the human + compliance).
- **Status:** ☐ not started. Directions endorsed (D-07 TanStack grid, D-09 Lexical editor); final provenance sign-off lands here. **Charts adapter (D-08) deferred** — team not using charts yet (build when the need arrives).

### M3 — Build-S/M long tail
- **Goal:** The individually-cheap custom widgets, ranked by general PrimeNG frequency (R7).
- **Slices** (★ = team-priority, pulled forward): ☐ ★ **image Carousel / Galleria / Image-preview** (media — CDK drag/overlay) · ☐ ★ **TreeSelect / TreeTable** (tree view beyond Direct MatTree) · ☐ InputNumber + InputMask + Password-meter + InputOtp directives · ☐ Calendar time-of-day picker (R2) · ☐ Rating · ☐ Skeleton · ☐ FileUpload (CDK drag-drop + HttpClient) · ☐ Splitter · ☐ OverlayPanel/Popover + ConfirmPopup · ☐ Breadcrumb · ☐ PanelMenu · ☐ OrderList/PickList · ☐ Avatar/Timeline/Tag (+ niche on demand)
- **Exit criterion:** the long-tail parity surface is complete to the documented frequency cutoff; niche widgets (Knob, OrgChart, MegaMenu, Dock) built only on demand and explicitly listed if deferred.
- **Leverage:** high — small, well-scoped custom components on CDK.
- **Status:** ☐ not started.

### M4 — Parity hardening & adoption
- **Goal:** Make "looks done" = "is done", and make the library adoptable.
- **Slices:** ☐ theming polish + density parity · ☐ full a11y audit (axe + manual keyboard, every built component) · ☐ visual-regression suite (light+dark) · ☐ capability ledger green · ☐ adoption / `p-*`→`cae-*` migration guide + usage-mapping doc
- **Exit criterion:** the parity checklist is green; the capability ledger shows every shipped component at `adversarial-passed`; the adoption guide is published.
- **Leverage:** mixed — automation/audit high; final parity judgment ~1×.
- **Status:** ☐ not started.

## Open decisions

**Resolved this session (2026-06-29)** — recorded in [ARCHITECTURE.md](ARCHITECTURE.md) Appendix A:
- **Naming** — library **Caelum**, demo **Forge** (chosen).
- **D-10 — "US-origin" definition** — *maintaining entity HQ'd in the US + permissive license (MIT/BSD/Apache-2.0) + no non-US runtime transitive dep* (adopted; **refined by D-11**).
- **D-07 / D-08 / D-09 — the three library directions** — **TanStack** (grid) / **visx** (charts) / **Lexical** (editor) endorsed.

**Resolved 2026-07-01** (recorded in Appendix A):
- **D-11 — refinement of D-10** (from #21, the M0-2 provenance scan) — origin gate is scoped to **shipped-runtime reachability** (not the full installed tree), with a **US-preferred / allied-nation (UK, Germany, …) fallback** when a need can't be met US-origin or self-built; **Angular's own transitive closure is accepted wholesale**; the license rule (permissive + free) is unchanged. Sign-off for D-07/D-09 grid/editor libs at M2 now runs against D-11.

Nothing currently blocks the build. Remaining (not blocking now):
- **Final transitive-provenance sign-off** for the endorsed grid/editor libs before pinning — at **M2** (D-07/D-09), per the D-10 rule.
- **Charts (D-08) deferred** — build when the team's chart need lands.

## Cut order

If time runs short, the order things get dropped (last-to-first) — decided in advance, not in a panic. **Never cut M0–M1** (foundation + common-case components — the bulk of any screen).

1. ✂ Rich-text editor adapter (M2) — heaviest gap, narrowest usage; defer to `contenteditable` stub or out-of-scope.
2. ✂ Charts adapter beyond a minimal set (M2) — ship a small visx chart set; defer exotic chart types.
3. ✂ Niche long-tail widgets (M3) — Knob, OrgChart, MegaMenu, Dock, Terminal — build only if a consumer needs them.
4. ✂ Visual-regression automation (M4) — fall back to manual snapshot review if the harness slips.
