# ROADMAP — Caelum

The live plan. Milestone order follows the brief's phased build order (the gitignored `_intake/` migration brief §5; see [`_intake/README.md`](../_intake/README.md)), adapted to Caelum's scope: an **app-agnostic, public** component library (not an in-place app migration — `D-06`). Onboarding drafted it; every session updates the status of the slice it touched. The `CLAUDE.md` Status block is the 10-line summary of this file.

**Status legend:** ☐ todo · ◐ in progress · ☑ done · ⊘ blocked · ✂ cut

> **Scope note (R7).** With no access to the consuming app we can't grep real `p-*` usage (brief §9.1) — but the team gave a usage signal directly (below). We build the full parity surface, ranked by that signal then by general PrimeNG frequency, and ship an adoption/usage-mapping guide so the team maps their own usage on adoption.
>
> **Team usage priority (2026-06-29, from the team via Connor):** highest dependence — **tables, forms, buttons, dynamic dialog, steppers, tree view, image carousel**. **Charts are not used now** (likely future). Build order is ranked by this; the **charts adapter (D-08) and editor adapter (D-09) are deprioritized** (see Cut order + D-08).

## Milestones

Each milestone has a **goal**, **slices**, a verifiable **exit criterion**, a **leverage** note, and **status**. Slices are tracked as [GitHub Issues](https://github.com/recon-research/caelum/issues) (the repo went live 2026-07-01; the M0 slices are issues #1–#8).

### M0 — Foundation & theming token bridge
- **Goal:** An Angular 22 workspace with the Caelum library + the **Forge** demo console, Material + CDK wired, the theming token bridge established, provenance/adapter ESLint guards in place, and the *Direct* form/button/panel/overlay/menu components ported.
- **Slices** — *done ✓; this is the index, full write-up in each linked issue/PR. The reusable recipe + gotchas behind them: [`docs/PATTERNS.md`](PATTERNS.md).*
  - **Foundation:** ☑ Angular 22 workspace #1 (PR #16) · ☑ Material+CDK+Aria install + provenance scan #2 · ☑ theming token bridge #3 · ☑ provenance/adapter/size gates + US-origin attestation #4 · ☑ configure_project CI matrix + required checks #6
  - **Direct components:** ☑ batch 1 button/card/checkbox/input #5 (PR #30) · ☑ batch 2 radio/select/textarea/tabs/tooltip #26 · ☑ batch 3 menu/stepper/tree #27 (PR #42) · ☑ cae-switch #68 (PR #69) · ☑ cae-toggle-button + cae-select-button #73 (PR #74) · ☑ cae-accordion + cae-expansion-panel #77 (PR #81) · ☑ cae-chip #83 (PR #86) · ☑ cae-chip-set #84 (PR #203 — breadth, later: the managed removable-chip list over mat-chip-grid, roving + focus-redirect; Forge tag-row dogfood; follow-ups #201/#202) · ☑ display primitives (progress-bar/spinner/divider) #88 (PR #89) · ☑ cae-slider #109 (PR #112) · ☑ cae-listbox #114 (PR #115) · ☑ cae-autocomplete #119 (PR #121) · ☑ cae-toolbar + cae-badge #126 (PR #130)
  - **Form-control depth:** ☑ per-component secondary entry points #28 (PR #44) · ☑ validation-error forwarding (input/textarea) #29 (PR #49) · ☑ cae-select forwarding + radio/checkbox a11y hook #47 (PR #52) · ☑ shared `CaeFormFieldControlBase` #46 (PR #55) · ☑ non-form-field ariaLabel/ariaLabelledby naming seam (checkbox/switch) #70 (PR #132)
  - **cae-button a11y forwarding + stepper:** ☑ tooltip #36 (PR #59) · ☑ menu-trigger #57 (PR #64) · ☑ disabledInteractive #58 (PR #66) · ☑ cae-stepper linear stepping #40 (PR #62)
  - **Service passthrough (D-15):** ☑ cae-toast #96 (PR #98) · ☑ cae-dialog #100 (PR #103) · ☑ cae-confirm #101 (PR #106)
  - **Infra / hygiene:** ☑ Forge @defer bundle budget #85 (PR #94) · ☑ python→python3 #7 (PR #124) · ☑ doc compaction #123 (this PR)
  - *Next slice candidates + the live resume point: the `CLAUDE.md` Status block.*
- **Exit criterion:** a representative Forge screen renders on Material with theme parity; the provenance scan is green in CI; every rendered color/space comes from a token variable (no hardcoded values); preflight green.
- **Leverage:** high — scaffolding, install wiring, Direct 1:1 ports, lint rules.
- **Status:** ☑ **done** (2026-07-04) — exit criterion met: a representative Forge screen renders on Material with theme parity, the provenance scan is GREEN in CI, every rendered colour/space comes from a token, and preflight is green. The **Direct-component parity family** is complete (~31 slices, indexed above); the D-15 dialog/toast slice landed; the non-form-field a11y seams (describedby #47, naming #70) are done. Metrics at M0 close: **310 tests** (272 lib + 38 forge), lib+Forge builds green, provenance GREEN, preflight PASS 0-skipped. Remaining Direct-component *parity extras* are `idea`-labelled follow-ups (not M0-blocking — they trickle in opportunistically or fold into M3). Foundation decisions are `D-01..D-15` (ARCHITECTURE Appendix A).

### M1 — Composed components
- **Goal:** The common-case widgets that aren't drop-in but assemble from Material/CDK pieces.
- **Slices:** ☑ ConfirmDialog wrapper — delivered early as `cae-confirm` #101 (D-15) · ☑ **Menubar** — `cae-menubar` #153 (horizontal application menu bar over `MatToolbar` + `cae-menu`; one-level dropdowns, CDK `FocusKeyManager` roving + Down/Up-opens; submenus/rich items/responsive/RTL/disabled-interactive are follow-ups #155) · ☑ **MultiSelect** w/ filter + chip summary — `cae-multi-select` #135 (PR #136; in-panel filter opt-in/off in v1 pending accessible impl #138) · ☑ **basic `MatTable` screen** — `cae-table` #141 (declarative columns/data config over MatTable + MatSort + MatPaginator, client-side sort + pagination; custom cell templates / sticky / expandable / selection are follow-ups; team's #1 dependence) · ☑ **TabMenu** — `cae-tab-menu` #164 (PR #166; the last canonical M1 slice — Direct wrapper over `mat-tab-nav-bar` + `mat-tab-link`, a tab-styled navigation/selection bar distinct from `cae-tabs`; v1 = manual-`active` mode [first-class p-tabMenu parity], data-driven `[items]` + two-way `[(activeValue)]` model + owned internal `mat-tab-nav-panel` for the full ARIA tabs pattern; value-based selection; opt-in `panelTabIndex`; 4-lens review 0 majors/5 minors applied; router-linked mode/icons/rich → #165) · ☑ **ContextMenu** — `cae-context-menu` #157 (right-click menu over the CDK Menu family `cdkContextMenuTriggerFor`; a *different* primitive from the MatMenu-based menu wrappers — behaviour-only, so the wrapper owns token-styling the overlay panel via `ViewEncapsulation.None`; empty-items disables the trigger; nested submenus / rich items / groups / global-open are follow-ups) · ☑ **SplitButton** — `cae-split-button` #148 (primary command + secondary-action dropdown, composed over `MatButton` + `cae-menu`; owns its `<button>`s so `type="button"` is explicit; icon/per-half-appearance/`(dropdownClick)` #149, rich dropdown items #150 are follow-ups)
- **Exit criterion:** the common-case component set is implemented **and parity-verified** (functional + a11y + visual scenarios green per component).
- **Leverage:** high — composition over documented Material/CDK primitives.
- **Status:** ☑ **done** (2026-07-05) — **exit criterion met:** the common-case composed set is implemented + parity-verified (functional + a11y specs green + a 4-lens adversarial review per component + a live Forge demo each). Canonical slices, all merged: `cae-multi-select` #135, `cae-table` #141, `cae-split-button` #148, `cae-menubar` #153, `cae-context-menu` #157, `cae-tab-menu` #164 — with ConfirmDialog delivered early as `cae-confirm` #101 (D-15). Metrics at M1 close: **375 tests** (332 lib + 43 forge), lib+Forge builds green, provenance GREEN, preflight PASS. Deep real-browser/visual-regression hardening is scoped to **M4** (this milestone's "visual scenarios" are the unit-tested Forge demos). Per-component parity *extras* are `followup`-labelled (#137/#138/#139/#143/#144/#145/#149/#150/#155/#158/#165) — non-blocking, drained opportunistically. A provisional Forge initial-bundle warn-budget raise (850→875 kB) rode in with #141 — `decision` #142 (reversible, silence ratifies; still the lone open decision). **History:** opened 2026-07-04 with `cae-multi-select` #135 (PR #136); `cae-table` #141 (the team's #1 dependence) landed next; ConfirmDialog wrapper already delivered as `cae-confirm` #101 (D-15). A provisional Forge initial-bundle warn-budget raise (850→875 kB) rode in with #141 — `decision` #142 (reversible; shipped-lib size discipline untouched). `cae-split-button` #148 (SplitButton) landed next — composed-over-composed (its "New member" demo drives the members `cae-table`), shipped as its own lazy chunk with no new eager weight, so #142's budget is untouched (Forge initial ~843 kB). `cae-menubar` #153 (Menubar) followed — the flagship composed widget (MatToolbar shell + one embedded `cae-menu` per group, bar roving via CDK `FocusKeyManager`); its deferred "command bar" demo drives a live log, own ~3 kB lazy chunk, Forge initial ~857 kB < 875 (#142 still untouched); 4-lens review fixed a Down/Up-opens a11y defect + a public-API leak (follow-ups #155). `cae-context-menu` #157 (ContextMenu) followed — the first wrapper over an *unstyled* CDK primitive (`@angular/cdk/menu`, right-click via `cdkContextMenuTriggerFor`), so the wrapper owns token-styling the overlay panel (`ViewEncapsulation.None`); its deferred "quick actions" demo drives a live log, own ~6 kB lazy chunk (the new CDK menu family stays off eager), Forge initial ~860 kB < 875 (#142 still untouched); follow-ups #158. Remaining canonical M1: **TabMenu** (`mat-tab-nav-bar`). Next-slice candidates + the resume point live in the `CLAUDE.md` Status block.

### M2 — Adapters for the three gaps
- **Goal:** Neutral interfaces + adapters for the three genuine gaps; each vetted candidate prototyped **in isolation** behind its adapter; provenance signed off (`D-07`/`D-08`/`D-09`).
- **Slices** (grid track underway; charts deferred; editor later): **grid** — ☑ **provenance sign-off + ratify D-07** (#169, `docs/provenance/M2-grid-tanstack-signoff.md` — PASS) · ☑ **neutral grid interface + `cae-data-grid` + client-side default adapter, vendor-free** (#170, PR #180 — `caelum/grid`: `Cae*` value types + `CaeGridAdapter<T>` port + `CAE_GRID`/`provideCaelumGrid()` + dependency-free `ClientGridAdapter` + `cae-data-grid` rendering its own DOM via cdk-virtual-scroll, `role=table`/aria-labelledby caption, public `exportRows()`, Forge "Activity log" demo; 4-lens-reviewed; Provisional on #168) · ☑ **TanStack adapter behind the *identical* interface + at-scale Forge screen = exit criterion MET** (#171, PR #183 — `@tanstack/table-core@8.21.3` headless core admitted behind the **unchanged** `CaeGridAdapter` port as `grid.adapter.ts`; `TanStackGridAdapter` bridges the imperative store into the port signals; `provideTanStackGrid()` a **separate** provider + optional-peer/external so the client default ships **zero** engine bytes; ESLint fence pinned to the dot-file (static + dynamic `@tanstack` imports error elsewhere); Forge `app-activity-grid-demo` drives **5000 rows** on TanStack in its own lazy chunk, initial **879.50 kB < 890**; 4-lens review w/ leakage lens fixed the sort-collation parity major via a shared `grid-sort` `compareValues` both engines sort by; **433 tests**; Provisional on #168) · ☑ **server-side / lazy engine = 3rd engine behind the identical port** (#176, PR #187 — `ServerGridAdapter` in `server-grid-adapter.ts`, dependency-free: holds no full set, emits `dataRequest` on sort/page, renders the slice pushed via `applyServerResult`; `dataRequest` reads the raw `_page` so `applyServerResult` never triggers a re-fetch; `cae-data-grid` gained a `[total]` input but **mode is keyed on the ENGINE** (`adapter.dataRequest() !== null`), not the input, with dev-guards; `provideServerGrid()`; Forge `app-orders-grid-demo` drives a 4800-row "server" (one 25-row page in the browser), 14th @defer, initial **879.64 kB < 890**; 4-lens-reviewed — 2 majors fixed [engine-keyed mode, exportRows scope-by-engine], enhancements #188/#189/#190; **454 tests**; Provisional on #168) · ☑ **grid loading/busy state, consumer-owned** (#188, PR #195 — `cae-data-grid` `[loading]`/`[loadingMessage]` mirroring p-table's `[loading]` input contract: `aria-busy` on the row viewport + loading-message-instead-of-empty; consumer-owned so it never gets stuck; client/TanStack byte-identical; Forge demo error-banner + Retry; 4-lens **0 surviving majors**, enhancements #192/#193/#194; **461 tests**) · ☑ **pager focus-on-disable** (#189, PR #197 — a11y: `cae-data-grid`'s pager kept keyboard focus off `<body>` when Prev/Next self-disables at the first/last page by moving it to the enabled sibling, **only when the pressed button held focus** (no cross-browser focus-theft — Safari/Firefox/programmatic clicks), deferred via `afterNextRender`; 2-lens focused review — both confirmed the focus-theft major, fixed; **464 tests**) · ☑ **pager/sort disable-while-loading + `[caeDataGridLoading]` busy slot** (#192, PR #199 — while `[loading]`, `cae-data-grid` makes its own sort headers + pager Prev/Next **`aria-disabled`** (not native `disabled`, so focus is retained) + **no-ops their handlers**, closing the overlapping-`dataRequest` footgun at the source; optional **decorative** (`aria-hidden`, `pointer-events:none`) **`[caeDataGridLoading]`** projection slot — no default ships, Forge dogfoods a pure-CSS spinner; client/TanStack byte-identical; 4-lens **0 majors**, 3 minors applied [slot renamed to element/vocab-consistent `caeDataGridLoading`; decorative-only WCAG 4.1.2 contract]; caelum-grid budget 20480→24576 B; **469 tests**) — grid **public-API shape** `decision` **#168 → D-17** (ratified by silence 2026-07-07: the library-grounded `CaeGridAdapter` port + `Cae*` value types; **#170 + #171 + #176 all shipped on it**). Grid follow-ups: #174/#175/#177/#178/#179 + #190 + #193/#194 + #216 (#182/#188/#189/#192 ☑ closed; #182 = the client-default engine tree-shake guard, PR #217, #216 its 2nd-engine follow-up). **editor** — ⤴ deferred to on-demand track **#232** (Lexical, D-09; Cut order #1) per **D-18**. **charts** — ⤴ deferred to on-demand track **#233** (D-14 D3-direct; Cut order #2) per **D-18**.
- **Exit criterion:** one real Forge screen per gap running on the chosen library, **behind the adapter only**, with provenance signed off and the ESLint isolation rule proving no leakage.
- **Leverage:** mixed — interface design + isolation is high; the provenance/legal sign-off is ~1× (the human + compliance).
- **Status:** ☑ **COMPLETE** (scoped to the grid gap by **D-18** #185, 2026-07-07; editor → on-demand track #232, charts → #233) — **grid track ☑ COMPLETE** (exit criterion met 2026-07-06). **#169 ☑** provenance sign-off + **D-07 TanStack ratified** (PASS: `@tanstack/table-core@8.21.3`, MIT, US, dependency-free) · **#170 ☑** the vendor-free neutral grid interface + `cae-data-grid` + client default + Forge demo (PR #180) · **#171 ☑** the TanStack adapter behind the *identical* port + at-scale (5000-row) Forge screen (PR #183) — **the isolation proof**: engine swapped by DI, `cae-data-grid` + specs unchanged, table-core confined to `grid.adapter.ts` (fence-verified) and kept off the eager bundle (optional-peer/external + deferred demo). Both grid PRs shipped on the #168 recommended default. **Remaining M2 gaps are parked:** **editor (D-09 Lexical)** is a cut-candidate (Cut order #1, deprioritized — own provenance sign-off first *if* pursued); **charts (D-14) deferred** (team not using). **#176 ☑** the server-side / lazy engine (`ServerGridAdapter` — a 3rd engine behind the identical port; `dataRequest`/`applyServerResult` seam + `[total]` input, engine-keyed mode; remote-backed Forge screen; PR #187, 4-lens-reviewed). **#188 ☑** consumer-owned grid loading/busy state (`cae-data-grid` `[loading]`/`aria-busy`, loading-vs-empty; PR #195, 4-lens **0 majors**; enhancements #192/#193/#194). **#189 ☑** pager focus-on-disable (a11y: keep keyboard focus off `<body>` when Prev/Next self-disables, no cross-browser focus-theft; PR #197, 2-lens focused review). **#192 ☑** pager/sort disable-while-loading + `[caeDataGridLoading]` busy slot (`aria-disabled` controls + no-op handlers close the overlapping-`dataRequest` footgun; decorative `aria-hidden` projection slot; PR #199, 4-lens **0 majors**). **M2 scope call RESOLVED** (`decision` **#185 → D-18**, human decided 2026-07-07, option a): M2 is scoped to the grid gap (done) and the milestone **advances to M3**; **editor (D-09)** → on-demand track **#232** (Cut order #1), **charts (D-14)** → on-demand track **#233** (Cut order #2). Independent grid follow-ups available anytime: #174/#175/#177/#178/#179 + #190 + #193/#194 + #216/#228.

### M3 — Build-S/M long tail
- **Goal:** The individually-cheap custom widgets, ranked by general PrimeNG frequency (R7).
- **Slices** (★ = team-priority, pulled forward): ☑ ★ **image Carousel / Galleria / Image-preview** (media — CDK drag/overlay; ☑ **Carousel** `cae-carousel` #273 · ☑ **Galleria** `cae-galleria` #274 · ☑ **Image** `cae-image` #275 · ☑ **ImageCompare** `cae-image-compare` #293 landed — the ★ media cluster is COMPLETE) · ☑ ★ **TreeSelect** `cae-tree-select` #279 landed (hierarchical node-select over cdkConnectedOverlay + MatTree; ☑ **TreeTable** `cae-tree-table` #262) · ◐ **input family**: InputNumber ☑ `cae-input-number` #301 (component, `number\|null` CVA over `Intl.NumberFormat`) · InputOtp ☑ `cae-input-otp` #303 (component, N-cell single `string` CVA via `NG_VALUE_ACCESSOR`, roving tabindex) · Password ☑ `cae-password` #304 (component; visibility toggle + advisory token-styled strength meter, enforced policy stays a form `ValidatorFn`) · InputMask ☑ `cae-input-mask` #302 (component; **unmasked**-string CVA, live re-mask + caret-after-next-slot discipline + paste-spread, no foreign mask lib) — **the input family is COMPLETE** · ☐ Calendar time-of-day picker (R2) · ☐ Rating · ☐ Skeleton · ☑ FileUpload #338 landed (native input + `DataTransfer` dropzone + HttpClient) · ☑ **Splitter** `cae-splitter` #323 landed (opens the Splitter family) · ☑ **ScrollPanel** `cae-scroll-panel` #328 landed (Splitter family sibling — Book 11 §3.2 both gaps now closed) · ☐ OverlayPanel/Popover + ConfirmPopup · ☑ **Breadcrumb** `cae-breadcrumb` #332 landed (semantic `nav` + `<ol>` + `aria-current`, silent `aria-hidden` token separators, no overlay — Book 09 §3.4) · ☐ PanelMenu · ☑ **drag-drop cluster COMPLETE** (☑ **OrderList** `cae-order-list` #336 · ☑ **PickList** `cae-pick-list` #337 · ☑ **FileUpload** `cae-file-upload` #338 landed — keyboard-reachable native input + `DataTransfer` dropzone, trust-boundary type/size validation before upload, `HttpClient` progress/cancel/retry, controlled CVA, per-row focus management; PR #346; deferred extras #341/#342/#345 — **request-config #345 landed** PR #348: withCredentials/headers/params, Content-Type-guarded; **order-list multi-select #341 landed** PR #351: `[(selection)]` ARIA listbox multiselect, block move, per-option describedby, 2 MAJOR fixed; **pick-list multi-select #342 landed** PR #353: per-list `[(sourceSelection)]`/`[(targetSelection)]` ARIA listbox multiselect (focus≠selection, pointer + keyboard, block transfer both directions) + per-option describedby, no MAJOR (2-lens); followups focus-restore #350, forge initial-bundle budget #354) · ☐ Avatar/Timeline/Tag (+ niche on demand)
- **Exit criterion:** the long-tail parity surface is complete to the documented frequency cutoff; niche widgets (Knob, OrgChart, MegaMenu, Dock) built only on demand and explicitly listed if deferred.
- **Leverage:** high — small, well-scoped custom components on CDK.
- **Status:** ◐ **active** — the current milestone (advanced here by **D-18** #185, 2026-07-07; M2 grid track complete). Team-priority (★) slices lead; independent breadth follow-ups run alongside — landed: cae-table #224 (deselect), #144 expandable detail rows (PR #243), #144 sticky header + sticky columns (declarative pass-through to MatTable's sticky API; Material supplies the CSS, so cae-table adds none; 2-lens review caught a WCAG 2.4.11 defect — a frozen leading column occluded the still-focusable built-in select/expand controls — fixed by auto-freezing them, closing #252, and by a reactive non-contiguous-run dev-warn), and **cae-tree-table #262** (PR #271) — the first ★ team-priority slice: a hierarchical `role=treegrid` over MatTable (flattened-tree per Book 10 §3.4, expansion-as-view-state, row-level roving keyboard, tree-aware `caeTreeCellDef`, new `caelum/tree-table` entry point + @defer'd Forge demo); the 2-lens review caught a keydown-hijack on interactive cell content (WCAG 2.1.1) + a treegrid caption-naming gap (fixed via `aria-labelledby`), and a roving-index desync (fixed by tracking the active node). Newest: **cae-carousel #273** (PR #277) — the first ★ media-family slice: a content-agnostic rotating carousel (signal sliding-window + `numVisible`/`numScroll`, circular, autoplay with WCAG 2.2.2 pause/stop + `prefers-reduced-motion` no-autostart, WAI-ARIA APG carousel a11y, roving-tabindex indicator group, `caeCarouselItem` slide projection, new `caelum/carousel` entry point + @defer'd Forge demo); no foreign carousel lib (Book 11 §3.5 gate 6). Its 2-lens review fixed a nav focus-strand (disabling prev/next at an end blurred the focused button → `aria-disabled` keeps focus), an unreachable-slide gap (`numScroll > numVisible` skipped interior slides → clamped), a nameless-in-prod roledescription (→ conditional), and removed an inert `responsiveOptions` false-affordance (deferred to #276); the API lens confirmed the page math + effect lifecycles are sound. Newest: **cae-tree-select #279** (PR #284) — the ★ value-picker family's tree control and TreeTable's form cousin: a hierarchical single/multi node-select whose **value is the node key(s)** (never label/expansion — Book 09 §3.5, the value boundary), rendered in the library's **first `cdkConnectedOverlay`** panel (MatTree + a selection `Set` + `cdkTrapFocus` focus-in/restore; five-beat overlay lifecycle Book 09 §2.2/§3.6), following the **#47 consumer-owned-error** selection-control pattern (`NG_VALUE_ACCESSOR` + `role=combobox` trigger + `ariaLabel`/`ariaLabelledby`/`ariaDescribedby`, not `CaeFormFieldControlBase` — a custom overlay has no inner Material control to bridge); reuses the `CaeTreeNode` model type-only; new `caelum/tree-select` entry point + @defer'd Forge demo. Its 2-lens review fixed `aria-controls`→the `role=tree` popup (was the wrapper div), single-mode `aria-selected` noise (→ only on the selected node), an empty-tree focus-strand + a `setDisabledState`-marks-touched wart, and added a `selectionMode` set-once dev-warn + a keyboard-`(activation)` selection test; the API lens confirmed value-fidelity + no click/activation double-fire (CDK-traced) and judged it correctly minimal. Newest: **cae-galleria #274** (PR #291) — the ★ media family's image gallery: a main image view + prev/next navigators + a **thumbnail tablist** (WAI-ARIA `tablist`/`tab`/`tabpanel`, roving tabindex, selection-follows-focus) + a polite position live region + a **fullscreen lightbox** opened through **`CaeDialog` (D-15)** — Material's centered modal supplies the focus-trap/`Escape`/backdrop/focus-restore for free (deliberately **not** tree-select's `cdkConnectedOverlay` — wrong shape for a centered modal, Book 09 §3.3, MatDialog Direct); typed `CaeGalleriaItem` model, two-way `[(activeIndex)]` shared with the lightbox (fullscreen nav live-syncs back), new `caelum/galleria` entry point + @defer'd Forge demo (defer count 17→18). Its 2-lens review (a11y + API) converged on one MAJOR — an orphan `role=tabpanel` + dangling `aria-labelledby` when the tablist isn't rendered (single-image / `showThumbnails=false`) — fixed by gating the tab semantics on a `hasThumbnails` computed; also shipped `role=group` + conditional `aria-roledescription` (mirror carousel, no region-landmark spam), an already-open guard on `openFullscreen()`, a `count>0` guard on the reconcile effect (no async-load index clobber → carousel gets the same via #290), and lightbox Home/End parity. **Thumbnail-strip cdk-virtual-scroll virtualization deferred to #286 (gated on #240, the M4 browser runner)** — jsdom can't measure the viewport, so verifying roving focus over a virtualized strip needs real layout (v1 renders the full strip, fully keyboard-tested). Newest: **cae-image #275** (PR #295) — the ★ media family's smallest member (`p-image` parity, Book 11 §3.4), completing the Carousel/Galleria/Image trio: a token-styled `<img>` with an **opt-in** (`preview` defaults off — parity, a bare image is inert) fullscreen **zoom / rotate / pan** preview opened through **`CaeDialog` (D-15, MatDialog Direct)** — the **same centered-modal substrate as the galleria lightbox** (deliberately not tree-select's `cdkConnectedOverlay`; a lightbox is centered, not trigger-anchored — Book 09 §3.3). Internal `CaeImagePreview` (not exported): a labeled `role=group` control set (zoom in/out, rotate L/R, reset, close) + `scale`/`rotation`/`pan` signals composed into one CSS transform (`translate() scale() rotate()`, pan in screen px); `aria-disabled` (not `disabled`) at the zoom limits keeps focus (WCAG 2.4.3); **every pointer gesture has a keyboard path** (`+`/`-` zoom, `0` reset, arrow-key pan — Book 11 §3.5 #1), pointer drag-pan a progressive enhancement; a polite zoom-% live region. New `caelum/image` entry point + @defer'd Forge demo (defer count 18→19). The zoom/pan transform is the primitive the galleria lightbox will later share (#289); the before/after `cae-image-compare` slider split to its own slice **#293**. Its 2-lens review (a11y + API) found **no MAJOR**; applied MINORs/NITs: `preview` default-**off** (p-image parity, was a least-surprise/parity gap), `<figure>`/`<figcaption>` caption association (WCAG 1.3.1, mirror galleria), "Reset view" rename (the control clears rotation+pan too — WCAG 4.1.2), an `isDevMode` invalid-zoom-bounds guard (contract `minZoom ≤ 1 ≤ maxZoom, step > 0`), rotation normalized `mod 360` (so `atIdentity`/Reset settle after a full turn), and test hardening (focus-retained-at-limit assertion + observable-DOM reads over white-box signals); the `afterClosed`/DestroyRef family-tidy (shared with galleria) filed as **#294**. Open: **cae-tree-select follow-ups #280 (checkbox tri-state) / #281 (focus-selected-on-open + multi-select live-region) / #282 (showClear/filter/leaf-only/dup-key-warn) / #283 (lazy/dataKey/virtual-scroll/mat-form-field variant)**; galleria follow-ups #286 (virtualization, #240-gated) / #287 (item/thumbnail templates) / #288 (parity extras: caption/indicators/numVisible/vertical/RTL/fullScreen) / #289 (lightbox zoom/pan, shared w/ #275); **cae-image follow-ups #293 (before/after `cae-image-compare` slider) / #294 (afterClosed+DestroyRef tidy, shared w/ galleria)**; carousel follow-ups #276 (responsive/vertical/swipe/RTL/focus-restore) / #290 (reconcile-guard); cae-tree-table follow-ups #263–#270 (selection/sort/lazy/sticky/2-D-roving/virtual-scroll/malformed-input/M4-verify); cae-table sticky **footer** #251 (blocked on a footer content model) + M4 focus-occlusion verify #254 + COMPARISON p-table frozen-API naming #253 + pager-outside-scroller #255, cae-chip-set, grid extras, test-infra #240 (M4 browser runner). Newest: **cae-input-number #301** (PR #307) — the first M3 **input-family** member (`p-inputNumber` parity): a **`number | null`** CVA over `Intl.NumberFormat` (decimal/currency, locale-aware parse — grouping strip + decimal normalize + sign, `0`/`null`/negative fidelity, reformat + snap-to-displayed-precision on blur), built as a **component** extending `CaeFormFieldControlBase` — a deliberate deviation from Book 08 §2.1's directive default for `<cae-*>` API consistency (`cae-input` had reserved the numeric model for it, excluding `type=number`). Native `Intl` only, no foreign lib (Book 08 §2.3, D-11). New `caelum/input-number` entry point + @defer'd Forge demo (defer count 19→20). Its 2-lens a11y/API review found **no MAJOR**; applied a runtime reformat effect (a currency/locale switch no longer leaves a stale display), blur snap-to-displayed-precision (WYSIWYG — the field never shows a value it wouldn't submit, p-inputNumber parity), a min>max fraction-digit guard (was an unguarded `Intl` `RangeError`), a NaN→empty `writeValue` coerce, and an overridable `inputMode` (the decimal pad hides the minus key). Siblings **cae-input-mask #302** (directive) / **cae-input-otp #303** (component) / **cae-password #304** (component) filed; parity extras **#305** (spinner/step, percent mode, live-format+caret, alt-numeral digits, clamp/prefix/keyfilter); pre-existing repo-wide demo heading-order (h1→h3) **#306**. Newest: **cae-input-otp #303** (PR #310) — the input family's **segmented** member (`p-inputOtp` parity, Book 08 §3.4): *N* single-character cells presenting as **one control** whose CVA value is the combined **`string`** code, via `NG_VALUE_ACCESSOR` — the **#47 consumer-owned-error** selection-control family, **not** `CaeFormFieldControlBase` (there is no single inner `matInput` to bridge across N cells; a **component** per §2.1, the documented family deviation). **Roving tabindex** (one tab stop) — deliberately **not** the book's `FocusKeyManager` default: the cells are native `<input>`s the user types into, so the manager's typeahead would hijack the digit keys and it would need a per-cell `FocusableOption` wrapper for no behavioural gain (the "laziest sufficient code" rung below it, documented). Behaviour: type-advance · Backspace clear-in-place / retreat-and-clear-prev · Arrow L/R + Home/End · **paste-spread** from the focused cell · **SMS one-time-code autofill spread** (autofill fires `input`, not `paste`, so a multi-char `input` spreads too — cell 0 omits `maxlength` so the OS code arrives intact); `integerOnly` (default) = digits + `inputmode=numeric`, clear it for an alphanumeric code; token-only cell styling (D-04). New `caelum/input-otp` entry point + @defer'd Forge demo (defer count 20→21). Its 2-lens review — a11y: **no MAJOR** (no keyboard trap / focus-drop / unreachable tab stop constructible); API: **2 MAJOR**, both the **runtime-`[length]`-change** class (tab stop drops out on shrink; stale chars resurrect on shrink-then-grow), **closed together**: an `activeTabStop` clamp (the group keeps exactly one tab stop even if `length` shrinks past the active cell — WCAG 2.1.1, defensive) + **`[length]` documented set-once** (like `cae-select-button.multiple`) with a runtime-change dev-warn (closes the value/tab-stop desync by contract); also applied a no-accessible-name dev-warn, a `numberAttribute` transform on `length`, and doc lines for faithful `writeValue` / middle-gap `join('')` collapse / `required` annotates-only. **756 tests** (692 caelum + 64 forge; 38 input-otp). Deferred: `mask` (dot-obscured cells) + `readonly` parity knobs **#309**; real-device autofill-spread verification rides the **#240** browser runner. Newest: **cae-password #304** (PR #313) — the input family's **password** member (`p-password` parity, Book 08 §3.5): a `matInput[type=password]` **component** extending `CaeFormFieldControlBase` (so it inherits label/hint/appearance/required/error-bridge like `cae-input`; the CVA value is the password **`string`** itself — no model/view split, and IME composition is buffered exactly like `cae-input`). Two additions grow the DOM (hence a component, not the book's directive default): an inline-SVG **visibility toggle** (a `matSuffix` `<button>` with self-authored eye / slashed-eye glyphs — **not** `mat-icon`, whose font is a Google-CDN dependency Caelum avoids for US-origin; a constant accessible name + `aria-pressed` per the WAI-ARIA toggle-button pattern; `writeValue` re-masks so a programmatically-written secret never renders in plaintext, and `[type]` is guarded by `toggleMask` so removing the toggle can't strand the field revealed), and an **advisory strength meter** — a module-local 0–4 heuristic (length + character-class variety) rendered as a 4-segment bar whose `error→warn→primary→success` ramp reads from `--cae-color-*` only (token-only theming D-04/Book 04 §3.6, no hardcoded colours) with a **context-prefixed** *"Password strength: X"* polite `role=status` live region so strength is never conveyed by colour alone (WCAG 1.4.1) nor mistaken for the enforced error. The meter is a **hint, never a gate**: the ENFORCED policy stays a form `ValidatorFn` (Book 07 §3.2) surfaced through the inherited #29/#47 error bridge, and the Forge demo shows a **divergent** case (meter reads "Good" while a required-digit rule still blocks); no scoring library is pulled in (Book 03 admit/reject, D-11 — a richer vetted estimator is the gated follow-up). New `caelum/password` entry point + @defer'd Forge demo (defer count 21→22). Its 2-lens review — **no BLOCKER**; each lens raised one MAJOR, both fixed: a11y — the strength was announced as a bare adjective that a screen reader could mistake for the enforced gate → the *"Password strength:"* context prefix; API — IME composition was unbuffered, diverging from the `cae-input`/`cae-textarea` family invariant and abortable once the field is revealed to `type=text` → buffered like `cae-input`; also applied visible-state coherence (the two guards above), a no-accessible-name dev-warn, the removal of a dead `strengthId`/`[id]` (both lenses), and a genuinely divergent advisory-vs-enforced test. **789 tests** (725 caelum incl. 33 password + 64 forge). Deferred parity extras (richer vetted estimator, caps-lock indicator, requirements checklist, strong-password/confirm validator helpers) → **#312**. Newest: **cae-input-mask #302** (PR #316) — the input family's **fourth and last** member (`p-inputMask` parity, Book 08 §3.2), **completing the family**: a fixed-template masked input built as a **component** extending `CaeFormFieldControlBase` — resolving the ticket's open directive-vs-component sub-decision in `plan_work` to **component for family consistency** (the human confirmed), mirroring `cae-input-number` #301's documented deviation from Book 08 §2.1's directive default; a lone `[caeInputMask]` directive would re-solve the label/hint/error-bridge wiring the base gives for free and be the family's odd one out. **Model/view split** (Book 08 §2.2, the R3 scar): the CVA value is the **unmasked** string (`2125550142`) while the inner `matInput` shows the mask (`(212) 555-0142`); `keepLiteral` opts into committing the decorated form. A ~90-line local mask engine (`parseMask`/`conform`/`caretForDataIndex`) — **no foreign mask lib** (Book 08 §2.3, D-11) — drives live re-masking on **every keystroke** with **caret-after-next-slot** discipline: the native value is managed **imperatively** (`setSelectionRange` synchronously in the handler), **not** via a `[value]` binding whose async re-write would clobber the caret — the key deviation from #301's blur-only reformat, since a mask can't dodge the caret. Also: **paste-spread** (a pasted decorated string conforms identically to typed digits), **IME composition buffering**, auto `inputmode=numeric` for all-digit masks. New `caelum/input-mask` entry point + @defer'd Forge demo (defer count 22→23, prints the unmasked model in a readout so the split is watchable). Its 2-lens review — **no BLOCKER**; a11y **MAJOR** (Backspace onto a literal was a no-op loop — `conform` re-inserted the literal and the caret snapped back, dead-ending keyboard/AT deletion) fixed with a **`beforeinput`-driven** collapsed-delete that removes the nearest **data** character; API **MINOR** (a mask literal a token predicate also accepts → silent view/model divergence) → dev-warn; plus an `isComposing` guard (stray post-`compositionend` input), a `.trim()` on the accessible-name warn, a format-`hint` a11y JSDoc note, and deletion/overflow/all-literal/`keepLiteral` test coverage. The API lens hand-traced the mask engine + caret math + effect idempotence and confirmed them load-bearing-correct. **821 tests** (757 caelum incl. 32 input-mask + 64 forge). Deferred parity extras (placeholder slots, structural `caeMaskComplete` validator, backslash escaping, KeyFilter) → **#315**. **The M3 input family (input-number #301 / input-otp #303 / password #304 / input-mask #302) is now COMPLETE.** Newest: **cae-image-compare #293** (PR #319) — the ★ media cluster's **niche fourth member** (`p-imagecompare` parity, Book 11 §3.4), a before/after **reveal slider** split out of #275 onto a **distinct substrate** (a reveal slider, not the `cae-image` dialog lightbox), so its own `caelum/image-compare` entry point rather than folding in. Two stacked `<img>` (before/after `src`+`alt` inputs rendered internally, so they overlap exactly and the component owns the alt text); the top layer is clipped by a `clip-path` computed and revealed by a divider that **is the APG window-splitter separator** (Book 11 §3.2 — the same a11y work `cae-splitter` will need): a focusable `role=separator` with `aria-orientation`/`aria-valuenow`/`aria-valuemin`/`aria-valuemax` + a **full keyboard resize path** (arrows nudge by `step`, Home/End snap, PageUp/Down coarse; Book 11 §3.5 #1) whose horizontal axis (drag + Left/Right) **resolves through `Directionality` for RTL** (Book 04 §3.5 — the **first Caelum component to inject `@angular/cdk/bidi`**; `clip-path: inset()` has no logical form so the hidden side is dir-resolved, while the divider uses logical `inset-inline-start`). The pointer drag is **native Pointer Events + `setPointerCapture`** (a reveal tracks a pointer *position*, not a repositioned element, so `cdkDrag` would be the wrong heavier tool — §3.5 #6 no-foreign-drag-lib satisfied by the platform); `value` is a `model<number>` (zoneless, §3.5 #4) normalized to [0,100]; **no LiveAnnouncer** (§3.5 #2 — `aria-valuenow` on the focused separator is announced like a slider thumb, so a parallel live region would double-announce). New `caelum/image-compare` entry point + @defer'd Forge demo (defer count 23→24, two-way `[(value)]` readout mirrors the divider). Its 2-lens review — **no BLOCKER**: API **MAJOR** (the pointer guard was `buttons&1`, which honored a cross-element drag passing over the divider and let a non-primary-button press jump the reveal) fixed with a real **dragging-state flag** (primary-button `pointerdown` only, cleared on `pointerup`/`pointercancel`, mirroring `image-preview`'s `panning()`); API **MINOR** (NaN/out-of-range two-way writes) fixed with a NaN-safe `clamp` + a normalize `effect` so `value()` self-heals into [0,100]; a11y **MINOR** (`aria-valuetext` was hardcoded English, overriding `aria-valuenow` in every locale) → **locale-neutral `"N%"`** (consumer-overridable formatter deferred); a11y **MINOR** (both-images-alt-empty is invisible to AT) → dev-warn; plus a non-positive-`step` dev-warn, a focus-ring surface-halo (visible over any image), and an explicit `focus()`-after-drag. Both lenses confirmed the clip/RTL/pointer-geometry/zoneless math and the window-splitter ARIA contract are correct. **842 tests** (778 caelum incl. 21 image-compare + 64 forge). Deferred parity extras (vertical orientation, click-the-track-to-jump, a content-projection variant, an i18n `aria-valuetext` formatter) → **#318**. The `afterClosed`/DestroyRef tidy **#294** is **not** touched by this slice (image-compare has no dialog). **The ★ media cluster (carousel #273 / galleria #274 / image #275 / image-compare #293) is now COMPLETE.** Newest tidy: **#294** (PR #321) — the family-wide lightbox `afterClosed`/`DestroyRef` debt landed: `cae-image` + `cae-galleria` now `pipe(takeUntilDestroyed(destroyRef))` and a `DestroyRef.onDestroy` closes the open modal, so tearing down a host tears down its lightbox instead of stranding focus on `<body>` (menubar precedent; #293 out of scope — no dialog). **844 tests** (780 caelum + 64 forge). Newest: **cae-splitter #323** (PR #326) — **opens the Splitter family** (`p-splitter` parity, Book 11 §3.2): a container `cae-splitter` + projected `cae-splitter-panel` (tabs/stepper `contentChildren` idiom, content captured as a `TemplateRef` and interleaved with the dividers), horizontal/vertical `[layout]`, (N−1) keyboard-resizable dividers that **reuse the #293 APG window-splitter separator** on flex panes — `role=separator`, dynamic per-pair `aria-valuemin/max` reflecting each panel's `minSize`, keyboard resize (axis arrows RTL-flipped for horizontal, Home/End snap, Page = coarse axis arrows), native `setPointerCapture` resize with the #293 primary-button dragging-index guard, RTL via `Directionality`. Live sizes: a **pure-computed seed + override signal** (avoids a projection-timing seed bug where an untracked seed read `[size]` before the projected inputs resolved), normalized to 100, NaN-safe-clamped against each `minSize`; `(resizeEnd)` output. New `caelum/splitter` entry point + @defer'd Forge demo (nested H+V, defer count 24→25). Its 2-lens review — **no BLOCKER**; both lenses independently confirmed the **MAJOR**: the seed ignored `minSize`, so a mis-declared `[size]` rendered a pane below its min and announced an ARIA-invalid triple (`aria-valuenow` outside `[valuemin,valuemax]`, WCAG 4.1.2), silently — fixed with an `enforceMinSizes` water-fill (the seeded layout now respects every min) + a `valueNow` clamp into the announced range for the infeasible case; plus Page-keys-as-coarse-axis-arrows (a vertical PageUp/ArrowUp direction clash), a `setLeading` changed-flag so `resizeEnd` only fires on a real change, and dev-warns for `size<minSize` / blank `ariaLabel` / non-positive `step` (restoring #293 parity). **886 tests** (822 caelum incl. 42 splitter + 64 forge). Deferred parity extras (state persistence, collapse/expand, px min/max, double-click reset, gutterSize, per-divider names, aria-controls, pointer gutter-offset) → **#325**. Newest: **cae-scroll-panel #328** (PR #330) — the Splitter family's ScrollPanel sibling (`p-scrollpanel` parity, Book 11 §3.2, closing both §3.2 gaps): a token-styled cross-browser scroll container built the laziest-sufficient way the book prescribes — native `overflow:auto` + token scrollbars, **no custom-scrollbar engine** (D-11 clean). A11y is the parity leg: a native `ResizeObserver` (guarded for non-RO/SSR) drives an `overflowing` signal so the host is `tabindex=0` (axe `scrollable-region-focusable`) only while it overflows and — when named — a `role=region`; neither when it fits. `CdkScrollable` host directive registers it with the `ScrollDispatcher`. New `caelum/scroll-panel` entry point + @defer'd Forge demo (defer count 25→26). Its 2-lens review — **no BLOCKER**; a11y **MAJOR** (removing `tabindex` from a focused container when its overflow cleared dumped focus to `<body>`, WCAG 2.4.3) fixed with an `isFocused` signal keeping it `tabindex=-1` while focused; plus a surface-halo focus ring (WCAG 1.4.11), and focus-preservation + `ResizeObserver`-wiring regression specs. **899 tests** (835 caelum incl. 13 scroll-panel + 64 forge). Deferred (#329): horizontal-only-nowrap post-mount overflow detection + animated-threshold hysteresis. **The Splitter family (splitter #323 / scroll-panel #328) is now COMPLETE.** Newest: **cae-breadcrumb #332** (PR #334) — the navigation-trail component (`p-breadcrumb` parity, Book 09 §3.4, COMPARISON row 119), built the laziest-sufficient way the book prescribes verbatim: a semantic `<nav aria-label>` + `<ol>` with `aria-current="page"` on the current page (non-link) and token-styled separators — **no overlay, no CDK, no menu machinery** (Angular core only, no Material import). Data-driven `[items]` + optional `[home]` **unified into one `crumbs` trail** so the last crumb is the current page even on a home-only page; `[separator]` (default `/`), `[ariaLabel]` (default "Breadcrumb"), `(itemSelect)` output; follows the `cae-tab-menu` precedent (data-driven nav, routerLink deferred). New `caelum/breadcrumb` entry point + @defer'd Forge demo (defer count 26→27). Its 2-lens review — **no BLOCKER**; each lens one MAJOR, both fixed: (a11y) separators are silent `aria-hidden` `<span>` glyphs rendered as text, **not** CSS `::before` content — Chromium/WebKit expose pseudo-element content to the a11y tree, so `::before` would announce the glyph between every crumb (the WAI-ARIA APG technique renders it silently); this also removed a `JSON.stringify`-into-CSS injection surface; (API/demo) a real `href` tore down the SPA before the `(itemSelect)` echo could paint → the Forge demo uses fragment urls. Also applied: the home+items unification (fixes a home-only no-current-page gap + a DRY duplication), an empty-crumbs guard (no empty landmark), a whitespace-safe `ariaLabel` default, and links underlined at rest (WCAG 1.4.1/F73). **913 tests** (849 caelum incl. 14 breadcrumb + 64 forge). Deferred parity extras (routerLink mode, `{item,originalEvent}` payload, icons, overflow-collapse) → **#333**. Newest: **the Book 11 §3.3 drag-drop cluster is open** (planned + ticketed: OrderList #336 / PickList #337 / FileUpload #338). **cae-order-list #336** (PR #339) landed — a keyboard-operable, drag-reorderable `role=listbox` (`p-orderList` parity) over `cdkDropList` + `LiveAnnouncer`: `role=option` rows with a roving tabindex (signal `activeIndex`, the `cae-input-otp` no-CDK-key-manager precedent for a data-driven `@for` with no child option directives), a control column of 4 move buttons (up/top/down/bottom), a content-agnostic `caeOrderListItem` projected template (`{{item}}` fallback), and `[(value)]` model + `(reorder)`; reorders via `moveItemInArray`, every move announced *"Moved to position N of M"* (§3.5 #2), no foreign drag lib (§3.5 #6). New `caelum/order-list` entry point + @defer'd Forge demo (defer count 27→28). Its 2-lens review — **no BLOCKER**; both lenses converged on one MAJOR (move buttons use `aria-disabled`, **not** native `[disabled]`, so a button that reaches a bound stays focusable instead of blurring the keyboard user to `<body>` mid-reorder), plus a doc MAJOR (the headline example must use the signal `[value]`/`(valueChange)` two-way form + `$any(item)` cast — a one-way `[value]` leaves the list uncontrolled) and 3 MINORs (a visually-hidden `aria-describedby` reorder instruction; an `activeIndex`-clamp effect so an external list-shrink can't leave a stale index that resurrects on regrow; a non-interactive-row caveat, `role=option` forbids nested focusables); RTL/`Directionality` is correctly **not** needed (§3.5 #3 is horizontal-axis only, this is a vertical list). **929 tests** (865 caelum incl. 16 order-list + 64 forge). Deferred parity extras (multi-select, in-list filter, per-item disabled, drag handle, custom `trackBy`, controls-position, virtualization #240-gated) → **#336** follow-ons. Newest: **cae-pick-list #337** (PR #343) landed — two connected keyboard-operable `role=listbox` lists (`p-pickList` parity) over `cdkDropList` + `cdkDropListConnectedTo`: a control column between them (move-selected/move-all, both directions) makes transfer fully keyboard-reachable, each list keeps its own roving-tabindex active row, every transfer announced via `LiveAnnouncer`; content-agnostic `caePickListItem` template, `[(source)]`/`[(target)]` models + `(transfer)`; **v1 transfer-only** (within-list drag reorder disabled via `cdkDropListSortingDisabled` so §3.5's keyboard-path-for-every-drag holds — reorder-within deferred), reusing order-list's `aria-disabled`-at-bounds pattern; no foreign drag lib (§3.5 #6). Its 2-lens review (a11y + API) found **no MAJOR, no functional defect**; applied 2 low-sev fixes (a distinct-references doc caveat + a mirror drop-direction test) and confirmed RTL is correctly deferred (CDK `Directionality` already handles the drag axis). **946 tests** (882 caelum incl. 17 pick-list + 64 forge). Deferred parity extras (within-list reorder + its buttons, multi-select, header slots, in-list filter, RTL glyph axis, describedby-placement + dark-mode-ring a11y checks) → **#342**. Newest: **cae-file-upload #338** (PR #346) landed — the cluster's **third and final** member (`p-fileUpload` parity): a keyboard-reachable native `<input type=file>` + native `DataTransfer` dropzone (**not** `cdkDropList` — that engine transfers `cdkDrag` items, never OS file drops, so an inert directive would fail liveness; native `dragover`/`drop` still satisfies every §3.5 leg: keyboard path = the input, drop validation, `LiveAnnouncer`, signals, no foreign lib), **trust-boundary type/size validation on every intake path — input, drop, and `writeValue` — rejecting before upload** (the non-negotiable), `HttpClient` `reportProgress` per-file progress/cancel/retry (unsubscribe aborts the XHR), a controlled CVA whose value is the accepted `File[]` (#47 consumer-owned error via `ariaDescribedby`), and per-row focus management so a Cancel↔Retry swap or a row removal never strands focus to `<body>` (WCAG 2.4.3). Angular core + `@angular/common/http` + `@angular/cdk/a11y` only (no foreign uploader, Book 03/D-11). New `caelum/file-upload` entry point + @defer'd Forge demo (selection-only + self-contained; the upload path is spec-verified via `HttpTestingController`; defer count 29→30). Its 2-lens review (a11y + security/validation) found **2 MAJOR, both fixed**: (security) `writeValue` seeded files without validating → an oversize/wrong-type file set through the form model could reach `http.request` → now validates on that path too; (a11y) the per-row Cancel/Retry/Remove buttons unmounted on activation and stranded focus → `afterNextRender` focus to the row's live action button / neighbor; plus 3 MINORs (store the subscription only if still open — a synchronous interceptor could desync the map; negative `maxFileSize` → no-limit instead of the fail-closed reject-everything footgun; announce the not-configured error for SR parity). **970 tests** (906 caelum incl. 24 file-upload + 64 forge). Deferred parity extras (image thumbnails/preview, chunked/resumable, concurrent-upload limit, `withCredentials`/headers config, drag-over paste + folder upload, per-file row template) → **#345**. **The Book 11 §3.3 drag-drop cluster (OrderList #336 / PickList #337 / FileUpload #338) is now COMPLETE.**

### M4 — Parity hardening & adoption
- **Goal:** Make "looks done" = "is done", and make the library adoptable.
- **Slices:** ☐ theming polish + density parity · ☐ full a11y audit (axe + manual keyboard, every built component) · ☐ visual-regression suite (light+dark) · ☐ capability ledger green · ☐ adoption / `p-*`→`cae-*` migration guide + usage-mapping doc
- **Exit criterion:** the parity checklist is green; the capability ledger shows every shipped component at `adversarial-passed`; the adoption guide is published.
- **Leverage:** mixed — automation/audit high; final parity judgment ~1×.
- **Status:** ☐ not started.

## Open decisions

**Resolved this session (2026-06-29)** — recorded in [ARCHITECTURE.md](ARCHITECTURE.md) Appendix A:
- **Naming** — library **Caelum**, demo **Forge** (chosen).
- **D-10 — "US-origin" definition** — *maintaining entity HQ'd in the US + permissive license (MIT/BSD/Apache-2.0) + no non-US runtime transitive dep* (adopted; **refined by D-11**).
- **D-07 / D-08 / D-09 — the three library directions** — **TanStack** (grid) / **visx** (charts) / **Lexical** (editor) endorsed. *(D-08's visx choice later superseded by **D-14** → D3-direct; see below.)*

**Resolved 2026-07-01** (recorded in Appendix A):
- **D-11 — refinement of D-10** (from #21, the M0-2 provenance scan) — origin gate is scoped to **shipped-runtime reachability** (not the full installed tree), with a **US-preferred / allied-nation (UK, Germany, …) fallback** when a need can't be met US-origin or self-built; **Angular's own transitive closure is accepted wholesale**; the license rule (permissive + free) is unchanged. Sign-off for D-07/D-09 grid/editor libs at M2 now runs against D-11.

**Resolved 2026-07-02** (recorded in Appendix A — all four ratified by the human on the recommended default; #9/#10/#11/#39 closed):
- **D-12 — zoneless-compatible invariant** (#9) — `OnPush` + signal-driven CD, no zone-coupled `NgZone`/`zone.js`; now a §2 invariant (every `cae-*` already complied — this pins existing practice as a commitment).
- **D-13 — Material → Aria → CDK → bespoke reach-for ladder** (#10) — refines D-02 into an ordered per-component preference.
- **D-14 — charts → D3-direct** (#11) — supersedes D-08's visx choice (visx is React-bound); build on framework-agnostic `d3-scale`/`d3-shape`; confirm modules + walk the transitive tree at the M2 charts sign-off.
- **D-15 — cae-dialog / cae-toast = service passthrough** (#39) — injectable `CaeDialog` / `CaeToast` over `MatDialog` / `MatSnackBar` (named `cae-toast` per the COMPARISON map); **unblocks the dialog/toast slice** (the last blocked slice — now buildable when scheduled).

Nothing currently blocks the build. Remaining (not blocking now):
- **Final transitive-provenance sign-off** for the endorsed grid/editor libs before pinning — at **M2** (D-07/D-09), per the D-11 rule.
- **Charts deferred** — build when the team's chart need lands; foundation is now **D3-direct (D-14)**, not visx.

## Cut order

If time runs short, the order things get dropped (last-to-first) — decided in advance, not in a panic. **Never cut M0–M1** (foundation + common-case components — the bulk of any screen).

1. ✂ Rich-text editor adapter (M2) — heaviest gap, narrowest usage; defer to `contenteditable` stub or out-of-scope.
2. ✂ Charts adapter beyond a minimal set (M2) — ship a small D3-direct chart set (D-14); defer exotic chart types.
3. ✂ Niche long-tail widgets (M3) — Knob, OrgChart, MegaMenu, Dock, Terminal — build only if a consumer needs them.
4. ✂ Visual-regression automation (M4) — fall back to manual snapshot review if the harness slips.
