# AUTOMATION.md — The Operator Console

The automation surfaces beyond the core loop. §1–2 and §6 are **already wired** in this template (§6 defaults to the `light` CI posture); §3–5 **require the human once** (credentials/UI). Standing rule either way: `.claude/settings.json` is permission machinery — the agent only changes it on your explicit, in-your-own-words instruction. Each item is tiered like a research note. Lazy-read: nothing here loads into sessions.

## 1. Hooks — zero-token mechanical gates `[production-proven]` *(wired)*

Six hook scripts in [`.claude/hooks/`](../.claude/hooks/), wired in [`settings.json`](../.claude/settings.json) (each file's header documents its contract; all **fail open** — any internal error allows, so a hook can never wedge the loop; docs: https://code.claude.com/docs/en/hooks, accessed 2026-06-10). The last two are **local telemetry** — zero-token JSONL appends under `.claude/metrics/` (gitignored) feeding `scripts/metrics.py` › Local telemetry:

| Hook | Event | What it does |
|---|---|---|
| [`block_naked_todos.py`](../.claude/hooks/block_naked_todos.py) | `PreToolUse` (Bash\|PowerShell) | Blocks `git commit` while the staged diff adds a naked TODO/FIXME — local CI parity at the moment of commit, zero tokens. Blocking hooks take precedence over the allowlist, so the pre-approved `git commit:*` can't bypass it. The matcher covers **both shell tools** — on native Windows the agent commits via PowerShell, and a Bash-only matcher silently never fires. |
| [`block_chained_merge.py`](../.claude/hooks/block_chained_merge.py) | `PreToolUse` (Bash\|PowerShell) | Blocks two merge shapes with zero legitimate uses, in **any spelling of the gh binary** (#342's shared `GH` token) and across **every** merge in a compound command: **(1)** a checks-watch/poll **chained into `gh pr merge` in one command** — fires the merge the instant the watch exits, even over a red gate, and where branch protection is unenforced nothing else stops it (#176 → #177); **(2)** `--admin` **in any argument position** on a merge (#423) — the branch-protection override that settings.json structurally cannot deny (prefix matching: `gh pr merge 5 --admin` sails past the flag-first deny *and* matches the `pr merge:*` allow — the `$comment`'s honest limitation); on free-plan private downstreams this rule is the only mechanical `--admin` guard. **Binds the owner too** — PreToolUse can't tell human from agent; a genuine override is the owner's call, run outside the session (D-422, ratified — guard classes split: only "never, by anyone" shapes may block; "not unsupervised" rules ship as reminders). Checks run *after* a merge (the post-merge glance) stay allowed; `--admin` attribution is scoped to each merge's own command segment. Carries `guard: #253, #423` provenance; denies append to the guard-hits ledger with the rule that fired. |
| [`block_commit_rules.py`](../.claude/hooks/block_commit_rules.py) | `PreToolUse` (Bash\|PowerShell) | The **escalation rung** (#310, program #301): blocks a `git commit` whose **explicitly-authored** message breaks the commit-message mechanics — subject folded into the body (no blank line after the summary), summary over the **subject cap** (`SUBJECT_CAP` in the hook — the number's single home, deliberately not repeated here; chars, not bytes, so em-dashes don't triple-count), or an **AI-attribution trailer** (Co-Authored-By: Claude / Claude-Session / Generated-with-Claude; human co-author trailers stay legal). **Second surface, #735:** it also blocks `gh pr create|edit --title` over `TITLE_CAP` — derived as `SUBJECT_CAP` minus a reserve for the ` (#NNN)` GitHub appends *after* every hook has run, so a title that is legal as a commit subject is not legal as a title. Not a fourth rule and no new admission: it is this same shape at the other surface that writes a subject onto `main`, which on a squash-merging repo is whichever of the two the repo's `squash_merge_commit_title` setting selects. It carries its own rule name (`pr-title-too-long`) only so the ledger can retire the two enforcement points separately. `--fill` is covered transitively (its title comes from a commit already judged); `gh pr merge --subject` and web-UI edits are named residuals in the hook header, not silent gaps. Graduated from the `commit` reminder on **ledger evidence, never posture** (#310's own rule): 96 reminder fires since 2026-07-15 while 38 of 122 authored subjects shipped over-cap (char-accurate, squash suffix stripped), plus a live same-session ignore (PR #523's commit). Every count in this row was measured when the cap was 72 and is kept as history, never re-fitted — see the cap-change note at the end. Judge honesty: the rung's contract is verdict + what-to-do-instead, and the judge is as cheap as the rule allows — here `len()` and a regex; a cheap-*model* judge is reserved for a fuzzy rule (e.g. over-implementation) if its evidence ever arms. Judges only messages it can read — the here-doc (`-F -`) or `-m` payloads; `--amend`/editor/`-F <file>` commits pass unjudged (fail-open), and heredoc *prose* mentioning commits is carved out (#252). Never-by-anyone class (D-422): a compliant rewrite always exists, so no use requires the violating shape — binds the owner too. Denies append to the guard-hits ledger; retire when a review period shows zero denies while authored subjects stay compliant, judged at retro (#253). **`--report` (#676) is the adoption instrument**: `python3 .claude/hooks/block_commit_rules.py --report` prints the over-cap ratio **split by recency** (recent 20 / mid 21–60 / older 61–120), the worst offenders, and the cross-repo reference points — refusing nothing, exit 0 always. Run it *before* deciding whether this guard suits your corpus, because a blocking guard's admission bar is a claim about the corpus it was measured on (README › blocking guards, #611). **The decision reads the recent row** (#697): a flat window straddles convention changes and reports the era rather than the practice — a downstream measured 40% flat over 60 against 10% over its last 20, i.e. converged, and the flat number would have told it to decline a guard costing it nothing (#682). The 2/21/36-per-60 spread that opened #645 was all flat-window, so read those as upper bounds — but an upper bound is not a promise of collapse: re-measuring the 21 found recent and flat identical at 35% (#705), so the era effect is corpus-specific too. It strips GitHub's post-hook ` (#NNN)` squash suffix, load-bearing rather than cosmetic: raw-vs-stripped read **16 vs 0** here when that was written and **39 vs 3** on the window that has since slid past it (11× when #636 first measured it; 43-vs-36 on a downstream that re-measured) — all at a cap of 72, so a raw count condemns a guard that is doing its job. At 100 this repo reads 0 vs 0, which is *not* evidence the strip is unnecessary: it only means our subjects now sit far from the bar, and the strip still decides the verdict for any corpus whose subjects land near whatever cap it runs. A high number is migration cost, not a wrong guard — D-422 admitted this on *shape*, never frequency, and this repo itself measured 31% at admission. **It ships wired** — #645 answered **B** (2026-07-28): the guard stays on by default and the *measurement* changed. Note the reason, which #705 corrected: the **raw** spread overstated the real one, but measured suffix-stripped the spread is **2 · 21 · 36 per 60** — real and corpus-specific, not artifact. B rests on the rule being admitted on *shape*, never frequency; a high honest number is migration cost, not a refutation. A recent window that still fails usually means a documented convention producing long subjects by construction (#681) — a real fork for the adopting owner, not evidence the rule is wrong. **Cap change, 2026-07-28 (#715, owner's call): 72 → 100.** The shape is untouched — D-422 admitted this on shape, never on frequency — so this tunes the parameter and leaves the holding intact. Basis: all eight sibling repos measured from here at both values (#707), where 72 put their recent windows at 5–45% over and 100 puts seven of eight at 0%, with 84% of everything 72 refused sitting at or under 100; the eighth is the #681 by-construction case. Cost bought knowingly: 72 exists so `git log --oneline` fits 80 columns. The re-key also fixed the reason it was hard — the literal lived in fourteen places, including ~12 selftest fixtures keyed to 72, so changing it turned the guard's own suite red (the #693 shape on the most-adopted constant here); everything now derives from `SUBJECT_CAP`, verified green and mutation-equivalent at caps 72/100/150. |
| [`inject_rule_reminders.py`](../.claude/hooks/inject_rule_reminders.py) | `PreToolUse` (Bash\|PowerShell + AskUserQuestion) | Injects the matching one-line rule as `additionalContext` at the moment of a high-stakes command — `git commit` (staging/here-doc/no-trailer rules, **plus path-aware lines from the staged index** — `textbooks/` → regen SECTIONS.json+audits, `research/notes/` → URL+date+tier, `research/experiments/*/results/` → pre-registration precedes results, `settings.json` → #242 grant classes, `ARCHITECTURE.md` → D-NN rows are commitments; #279), **plus a default-branch line when HEAD is the trunk** (branch first — `ship_pr` step 2; a reminder rather than a block because committing on the trunk is legitimate pre-`origin`, the template's own backlog phase — D-422, #662), `gh pr merge` (read-green-first + separate-command, #177; routes to `ship_pr` steps 5–7), `gh pr create` (ship_pr step 4: claim exists, body-file, receipt owed), a push aimed at `main` (never direct), **branch creation** (`checkout -b` / `switch -c` — the claim moment, `ship_pr` step 0, structurally *before* the work instead of after every expensive thing a slice does, #634), a branch's **first push** (`-u`/`--set-upstream` — demoted to the late backstop for a branch created outside a matched command), both prefix-independent because a `slice/` literal was silently inert on topical-prefix downstreams (#275), and `gh issue close` (inbox close owes a receipt, decision close owes a D-NN row; #279). The merge/pr-create/first-push texts also **name the owning skill and the steps owed** — hooks fire regardless of context decay, so they carry routing back into skill-step-backed mechanics a hand-driven session would silently skip (#263, from a live downstream field failure). Top-of-context prose decays as a session grows; this rides recency instead of fighting it (#252) — reminders are pointers to each rule's durable home, never second copies. **Non-blocking by design**: emits *no* `permissionDecision`, so it can never widen or narrow the permission flow. Fires append to `.claude/metrics/guard_hits.jsonl` (gitignored) — the guard-lifecycle telemetry (#253): zero fires = dead weight, constant fires = misaimed noise. A separate `AskUserQuestion` matcher (owner go-ahead in-session 2026-07-16, #290) fires the **elicitation bar** at the moment a structured question reaches the owner — options owe end-state consequences, recommended default first, entropy-worthy forks only; routes to `understand_intent` + `research/notes/intent-elicitation.md`. Timing honesty (hook header): `additionalContext` lands with the tool result, i.e. with/after the answer — it audits the exchange (repair before building on the answer), it cannot gate the ask. |
| [`session_start_banner.py`](../.claude/hooks/session_start_banner.py) | `SessionStart` (startup\|resume\|clear + compact) | Injects a 1–3 line banner: Status-anchor staleness (As-of sha vs HEAD) + the open decision queue. The `compact` matcher re-grounds the agent right after a surprise compaction; `clear` covers post-`/clear` windows, which start just as blank. ~100 tokens. Fail-open side effect (#47): appends the session-start event (`source` incl. `compact`) to `.claude/metrics/events.jsonl` — how `metrics.py` counts compactions — and rewrites `.claude/metrics/session.json` with the same row (#95): the current session's identity, read by claim comments (`PROJECT_CONVENTIONS.md` › Concurrent writers). |
| [`session_close_contract.py`](../.claude/hooks/session_close_contract.py) | `Stop` | **The landing-block gate (#749)** — a turn that used a work tool (`Edit`/`Write`/`NotebookEdit`/`Bash`/`Agent`/`Task`/`Workflow`; read-only tools are the exempt pure-conversation case) must close with the four fields §8 specifies, and a `compact` verdict must additionally carry the `/compact` block and have surveyed the owner. **Blocking, but never trapping**: exit 2 feeds the reason back to the model, and `stop_hook_active` caps it at ONE nudge per turn, so the gate costs a retry and can never deadlock a session. The nudge quotes the **live** cost/context figures (imported from `slice_telemetry.format_status`, never re-derived) so a single retry can comply; an unreadable ledger degrades to the command to run, never to a guessed number — a fabricated context % is worse than none when the owner uses it to decide whether to compact. Detects the turn by reading `tool_use` blocks back to the last genuine owner message in the transcript, and takes the message text from `last_assistant_message` because the transcript may lag the final message (the documented caveat). Bias is deliberate and asymmetric: `Bash` counts as work even though much of it is read-only, because a spurious block costs four skimmable lines while a missed hand-back costs the exact thing the hook exists to prevent. Fail-open on everything else — unparseable stdin, missing transcript, dead ledger, subagent context all exit 0. |
| [`log_skill_usage.py`](../.claude/hooks/log_skill_usage.py) | `PostToolUse` (Skill) | Appends one JSONL row per skill invocation to `.claude/metrics/skill_usage.jsonl` (gitignored) — the CMMI-L4 usage ledger behind `scripts/metrics.py` › Local telemetry. A skill at 0 invocations in a mature ledger flags dead weight or broken routing (the #6 measure-first discipline). Zero tokens; PostToolUse can't block by design. |
| [`log_agent_dispatch.py`](../.claude/hooks/log_agent_dispatch.py) | `PostToolUse` (Agent\|Task) | Appends one row per **subagent dispatch** to `.claude/metrics/agent_dispatches.jsonl` (gitignored) — the fan-out signal behind the receipt's `agents=` field (#392). Why it exists: `usd` is a *session*-cost delta and so counts every dispatch, while `Δlines` counts none of them, so under the skill-eval admission gate (#302) the "cost drift without churn" tripwire fired on every delegated slice **by construction** (PR #386: 19 lines, 10 dispatches, $15.67) — an alarm that always fires has stopped being one. Telemetry only, fail-open, cannot block. **Wiring owner-applied in-chat 2026-07-18** (#395). `slice_telemetry.py` prefers this ledger; on a box whose wiring hasn't landed (e.g. a fresh downstream copy pre-onboard) it falls back to counting `Agent` tool-use blocks in the harness session transcript — same ledger discipline (harness-written, never self-counted), but it depends on the undocumented `~/.claude/projects/<slug>/<session>.jsonl` layout, which is exactly the kind of local-path assumption that failed silently on one fleet box (#366). |
| [`log_permission_denials.py`](../.claude/hooks/log_permission_denials.py) | `PermissionDenied` (all tools) | Appends one JSONL row per denied tool call (tool, truncated detail, reason) to `.claude/metrics/permission_denials.jsonl` (gitignored) — each denial is an autopilot stall; `metrics.py` trends the count so retrospectives see interruptions as data. The matching behavior rule (denial = decision point → survey the owner) is `CLAUDE.md` › Working style. Telemetry only, fail-open. **Verified live 2026-07-08**: the event fires for auto-mode *classifier* denials as well as rule/interactive denies — first datum from a downstream copy's ledger (intake #118, recorded via #125); classifier reasons may arrive as "No reason provided". |

Hook config **hot-reloads** — a file watcher picks up settings edits mid-session, so a new or changed hook fires without a restart (https://code.claude.com/docs/en/hooks, accessed 2026-07-18: *"Direct edits to hooks in settings files are normally picked up automatically by the file watcher"*; the page also documents a `ConfigChange` event). This doc previously claimed the opposite — snapshot-at-session-start, effective next session — which two live observations contradicted before the upstream doc did (#252's heredoc fix firing on its own next push, PR #257; #263's pr-create rule firing on its own shipping PR, #266). `/hooks` is a **read-only** browser, not a way to apply changes. Don't add a **blocking** PreCompact hook — wedging compaction at full context is worse than anything it could check.

**Cost attribution stays receipt-based.** The OTel export path (`claude_code.cost.usage` with `skill.name`/`agent.name` attributes) and a 1.5×-median per-slice overrun tripwire were both evaluated and **declined** — measured verdicts + re-evaluation triggers: #312 (spike comment) and follow-up #437; the halves-based `compute_drift` (`scripts/metrics.py`) stays the cost-regression guard.

**Testing a hook without wrecking the ledger it writes (#319).** Every hook here is fail-open *and* side-effecting: piping a live payload into one runs its real side effects. Hit live — a fire-test with `"session_id":"selftest"` rewrote `.claude/metrics/session.json`, and the next merge receipt posted as `by=…/selftest` with `usd=n/a`, corrupting the claim row too. Two isolation levels, in preference order:

1. **A side-effect-free `--selftest` entry** — assert the hook's *pure core* (the classify/count/message functions), never `main()`. This is why `session_start_banner.py --selftest` skips `log_event()`. Register it in `SELFTEST_HOOKS` (`scripts/audit_ops_config.py`) so every preflight re-proves it; being offline and side-effect-free is the bar for belonging on that list. **The split's second payoff is the larger one and went unused for weeks (#707):** a predicate that takes the corpus and returns a count runs against *any* corpus, including a **downstream's** — so a number a downstream reports about itself is recomputable here from read-only access in minutes, instead of relayed back as a re-measure request that costs weeks and can be wrong about the ask itself. Purity buys foreign-corpus measurement, not just selftest-drivability; the method and its permission caveat live in [`harvest_downstream`](../.claude/skills/harvest_downstream/SKILL.md) › step 5.
2. **When you must fire the real thing** (liveness evidence — the selftest proves logic, not that the hook *runs*), point `CLAUDE_PROJECT_DIR` at a throwaway directory, since that is what every hook here resolves its write paths from. Then **verify the real ledger afterwards** rather than assuming: the mistake above looked fine at the prompt and only surfaced a merge later.

## 2. The `settings.json` posture *(ships with the template — adopted by the kickoff authorization)*

**Approval model:** the allowlist arrives with the copy; the README's one-sentence kickoff line (*"…you have my permission to adjust the Claude Code settings and set everything up"*) is the downstream owner's adoption of it, plus permission for setup-time adjustments (machine-local `settings.local.json`, requested trims). An agent must never **widen** the allowlist beyond the shipped form on its own — platform safety blocks agent-authored permission widening as self-modification, by design; a fresh, explicit owner instruction in chat is the only path. Same boundary, commit-side (#242, surfaced by a downstream syncing D-210): a settings change touching both classes ships as **two commits** — owner-gated grant edits (`allow`/`deny`) separate from mechanical updates (hooks, statusLine, attribution) — so a downstream sync can apply the mechanical commit wholesale and route the grant commit to its owner, instead of splitting one diff by hand at apply time.

What the committed file carries, and why:

- **allow** — the full autopilot loop in **both shell tools** (Bash *and* PowerShell — on native Windows the agent runs PowerShell, and an un-mirrored allowlist prompts at every step): read git/gh, tracker writes, branch/commit/push, PR create+checks+merge, `scripts/preflight.{sh,ps1}`, and the library/research audits (cwd-independent, so both root-relative and in-dir spellings work). The git grants are **narrowed** to the loop's actual shapes: `checkout -b` (not bare `checkout`, which would pre-approve `checkout -- .` discards), `push -u origin` / `push origin` (not bare `push`), `worktree add/list/prune` (not `remove`); `git rm` and `git stash` are deliberately absent (rescue-branch, don't stash), and PowerShell gets **no `git commit`** — commits route through the Bash here-doc path (conventions › PR / commit mechanics).
- **deny** — tripwires for the policy-forbidden forms in both shells: `git push --force` / `-f`, `gh pr merge --admin`. The prefix grammar can't catch every flag position (`git push origin --force`, `gh pr merge 12 --admin` slip the prefixes), so these are seatbelts, not the guard — the guard is branch protection with **"include administrators"** where the plan enforces it (§5, including the free-tier caveat) plus the written policy (`PROJECT_CONVENTIONS.md` › Merge policy).
- **hooks** — §1, via the braced `${CLAUDE_PROJECT_DIR}` placeholder, which Claude Code substitutes itself before any shell parses the command — so the wiring is shell-independent and works from any launch directory (the unbraced form relied on POSIX expansion and could invert the fail-open design into a fail-closed block on Windows). **Verified on Windows** (Windows 10, PowerShell 5.1, 2026-06-11): at cold startup the banner fires, and `block_naked_todos` blocks a staged naked-TODO `git commit` through **both** the Bash and PowerShell tools — via the script's own `BLOCKED:` message, not `python: can't open file`. That block surfaces as a hook *error* carrying the `BLOCKED:` line — Claude Code's framing for a deny (exit 2), i.e. the gate working, **not** a broken hook to file a ticket against. (The substitution is Claude-Code-version-dependent; re-confirm on a new box by staging a bare `TODO`, attempting a commit, expecting `BLOCKED:`, then discarding.)
- **statusLine** — the context-window meter the owner otherwise lacks ([`.claude/statusline.py`](../.claude/statusline.py); docs: https://code.claude.com/docs/en/statusline.md, accessed 2026-07-02): `<model> | ctx NN% used | $X.XX`, color-banded (green &lt;60 / yellow / red ≥80) with a `-> prepare compaction` nudge at ≥80% — the human's indicator and `prepare_compaction`'s proactive contract now agree on when it's time. Display-only, **no permission surface**. Fail-soft side effect: each refresh atomically upserts `.claude/metrics/sessions/<session_id>.json` (gitignored) with running cost/context totals — the last write is the session's final ledger row, aggregated by `scripts/metrics.py` › Local telemetry. Windows: statusline commands run through Git Bash when present, else PowerShell; the forward-slash `python3 .claude/statusline.py` spelling works in both and assumes launch-directory cwd (the same assumption the allowlist's relative `scripts/preflight.*` spellings already make). Interpreter spelling is `python3` across hooks/statusLine (D-210, from harvest #203): bare Ubuntu 24.04+ ships no `python`, and a wrong spelling here fails *silently*; Windows needs `python3` resolvable — Store Python ships it, python.org installs take a one-time `mklink python3.exe python.exe`. Applies on the next status update after the settings change (no restart); remove the `statusLine` key (or `/statusline delete`) to turn it off. **Host caveat (#508, root-caused downstream):** the Claude Code **desktop app** never drives the `statusLine` command — the script is fine (feed it a sample stdin JSON and it snapshots correctly), the host feature is absent — so on such hosts the sessions ledger stays empty, receipts read `usd=n/a` (`tok-*` is that box class's cost axis, #426), and the two ledger-fed alarms (median peak context ≥85%, session cost / merged PR) can silently never fire; `metrics.py`'s Sessions-recorded row names this state and self-heals if snapshots ever appear.
- **Optional, not set:** `fallbackModel` — fallback model(s) tried when the primary is unavailable (changelog v2.1.166) — would keep an overnight run alive through an availability blip. Verify the current key shape in the docs before adding. `[published]`

## 3. `@claude` GitHub Actions — answer decision issues from your phone `[production-proven]`

`anthropics/claude-code-action@v1` responds to `@claude` mentions on issues/PRs and can implement + open PRs (https://code.claude.com/docs/en/github-actions, accessed 2026-06-10). With it, commenting `@claude go with option B` on a `decision` issue gets the re-route implemented remotely — the decision loop no longer waits for your desk.

- **Auth (Max/Pro):** run `claude setup-token` locally → repo secret `CLAUDE_CODE_OAUTH_TOKEN` → pass as `claude_code_oauth_token` (https://github.com/anthropics/claude-code-action/blob/main/docs/setup.md, accessed 2026-06-10).
- **PUBLIC-repo security** (https://github.com/anthropics/claude-code-action/blob/main/docs/security.md, accessed 2026-06-10): triggers are restricted to users with **write access** by default — keep it that way; never set `allowed_non_write_users`; leave `show_full_output` off (logs are public); third-party issue text is prompt-injection surface even with sanitization; bound cost with `claude_args: --max-turns`.
- Minimal workflow (`.github/workflows/claude.yml` — not shipped; add when you add the secret):

```yaml
name: Claude
on:
  issue_comment: { types: [created] }
  pull_request_review_comment: { types: [created] }
jobs:
  claude:
    if: contains(github.event.comment.body, '@claude')
    runs-on: ubuntu-latest
    permissions: { contents: write, pull-requests: write, issues: write, id-token: write, actions: read }
    steps:
      - uses: anthropics/claude-code-action@v1
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          claude_args: "--max-turns 30"
```

## 4. Scheduled runs — nightly hygiene without a session `[production-proven]`

- **Desktop app → Routines → New routine → Local** (https://code.claude.com/docs/en/desktop-scheduled-tasks, accessed 2026-06-10): runs a fresh session on a schedule, optionally in an isolated worktree, with per-task always-allow permissions. Suggested nightly prompt: *"Run scripts/preflight (audits + hygiene), `python3 research/tools/_audit_research.py --strict-staleness`, and reconcile the CLAUDE.md Status block against `gh issue list`/`gh pr list`; file a `followup` issue for anything found; change nothing else."*
- **Cloud Routines** `[experimental — research preview]` (https://code.claude.com/docs/en/routines, accessed 2026-06-10): machine-off schedules and GitHub triggers — but only `pull_request`/`release` events (not issues), so decision-issue automation still needs §3.
- **Headless CI** (`claude -p --bare --output-format json`) for scripted gates in Actions (https://code.claude.com/docs/en/headless, accessed 2026-06-10). Note: from June 15, 2026, subscription `claude -p` draws from a separate monthly Agent SDK credit pool.

## 5. Branch protection (repo setup, attended)

Require PRs + the CI checks **and enable "include administrators"** — without it, `gh pr merge --admin` (inside the pre-approved `gh pr merge:*` grant) bypasses every gate. `onboard` Mode A marks this step attended; `gh api` is deliberately not pre-approved. **Which checks to require depends on the CI posture + gating (§6):** in `light` **and in `full`+`advisory`**, require only `static gates` — the heavy jobs are posture-/gating-skipped on PRs, and a skipped job satisfies a required check, but a job whose workflow never runs sits "Expected" forever and blocks the PR; in `full`+`blocking`, also require `heavy matrix gate` — the aggregation job (#206): its name is stable while the matrix-leg names shift with the platform trim, and it fails when `classify changes` itself errors — the zero-coverage skip that the individual heavy checks would satisfy as green; in `manual`, require no checks (the pasted preflight is the gate). Never require `classify changes` or the matrix legs directly (plumbing / unstable names; the aggregate covers both).

**Free-tier caveat (hard-won):** on a free-plan **private** repo GitHub does not enforce branch protection or rulesets at all — the protection UI exists but the rules don't bind (configuring required status checks via the API returns 403 outright). Mechanical enforcement requires GitHub Pro, an org plan, or making the repo public. Until then the written merge policy (`PROJECT_CONVENTIONS.md`) plus the allowlist's narrowed grants are the *only* guard — treat the settings.json `$comment`'s honesty about prefix-matching holes as load-bearing, and revisit protection the moment the repo's plan or visibility changes.

## 6. CI minutes & pacing — the posture system `[production-proven]` *(wired — `light` + `blocking` by default)*

**The burn problem.** GitHub-hosted runners are free on **public** repos; private repos draw from the plan's monthly pool — **Free: 2,000 min, Pro: 3,000 min** — and Windows runners bill ~1.7× Linux ($0.010 vs $0.006/min) (https://docs.github.com/en/billing/concepts/product-billing/github-actions, accessed 2026-06-12). The pre-posture workflow (6 jobs × 2 OSes, every PR push **and** every main push) billed several minutes per push; field report 2026-06-12: a downstream private copy burned ~2,000 min in one week — a whole month's Free pool. Public repos never feel this, which is exactly why a template developed on a public repo didn't.

**The fix: two repo variables + automatic path modularity.** `ci.yml` reads `CI_POSTURE` (how much runs) and `CI_GATING` (when the heavy matrix runs); both default to the safe value when unset, because an unset variable is the empty string in a job `if:` (https://docs.github.com/en/actions/learn-github-actions/variables, accessed 2026-06-12).

```
gh variable set CI_POSTURE --body <light|full|manual>     # unset = light
gh variable set CI_GATING  --body <blocking|advisory>     # unset = blocking
```

**`CI_POSTURE` — how much runs:**

| Posture | What runs automatically | Typical burn | Pick when |
|---|---|---|---|
| `light` *(default)* | one consolidated `static gates` job per PR — the full audit battery: library + research audits, doc budgets, ops-config, shellcheck (#643), repo links, name leaks, secret scan (#221), staleness, skill-eval stamps, closing keywords (#727 — the only gate that reads the PR title/body, so CI is where it earns its keep), TODO hygiene, ticket-first (ubuntu, ~1 min; this cell is the prose single home — `ci.yml` is authoritative) | ~5–15 min/day | private repo on Free/Pro — the recommended default |
| `full` | static gates + build/test matrix + lint (see gating for *when*) | ~100–150 min/day blocking; far less advisory | public repo (minutes free), org pool, or self-hosted runner |
| `manual` | nothing | 0 | zero budget; a clean preflight pasted in the PR is the gate |

**`CI_GATING` — when the heavy matrix runs** (only meaningful in `full`; in `light` the matrix is already on-demand):

| Gating | Heavy matrix | Merge gate | The trade-off |
|---|---|---|---|
| `blocking` *(default)* | on the PR | heavy must be green to merge | safest; you wait for the matrix on every code PR |
| `advisory` | **post-merge on the main push** | `static gates` + preflight; heavy is a sanity check | no PR wait; a rare post-merge red is fixed forward via a filed `bug` |

Advisory's safety rests on one fact: **preflight runs the same heavy gates locally before every push**, so advisory *defers re-running already-passed tests*, it doesn't skip testing. A post-merge red therefore means an environment-specific drift (an OS-only failure the local run couldn't see) — the rare 1% — and the next `onboard` catches it (Mode B checks main health first thing), files a `bug`, and fixes forward. Cheap, because nothing was built on top yet. This is the right default for an **active solo autopilot loop on a private repo** (the case the field report came from); keep `blocking` for shared repos, public APIs, or anything where a transiently-red main is costly.

**Path modularity (always on, no variable).** A **doc-only** change — every changed file is `*.md`, or under `docs/` or `_intake/` — never triggers the heavy matrix, in any posture. A cheap `classify changes` job (~10s, hand-rolled, fail-safe to "run the matrix") sets a `code` output; the heavy jobs gate on it via `needs:`. `static gates` still runs on docs (links, citations, SECTIONS freshness, hygiene). This removes the "15 minutes of full CI just to edit the roadmap" tax that motivated the change. The classifier itself is gated to run only when the heavy matrix could need it (full posture, or an explicit escalation), so it adds nothing in light/manual.

Escalation in **any** posture/gating: label a PR **`full-ci`** (heavy matrix for that PR, even doc-only — the label event re-triggers the run) · **`gh workflow run ci.yml`** (one full run) · push an **`M*`/`v*` milestone tag** (milestone exits always re-verify everything). Pacing changes *when CI re-verifies*, never *what must pass*: `scripts/preflight.{sh,ps1}` runs every gate locally before every push in every posture/gating, and `definition_of_done` still collects the evidence. The consolidation is itself part of the savings — split into four jobs, the same ~90s of audit work bills four runners' checkout + setup time.

**Why job-level `if:` and not `on:` filters** (load-bearing): a job skipped by its `if` conditional reports its status as "Success" — required checks stay satisfied — while a workflow skipped by path/branch filtering leaves its required checks "Pending" and **blocks the PR from merging forever** (https://docs.github.com/en/pull-requests/collaborating-with-pull-requests/collaborating-on-repositories-with-code-quality-features/troubleshooting-required-status-checks, accessed 2026-06-12). Both the path classifier and the posture/gating live in job `if:` for exactly this reason. Consequence for §5: require only the checks the posture+gating actually runs on a PR (`light` or `full`+`advisory` → `static gates`; `full`+`blocking` → also `heavy matrix gate`, the aggregate).

**Self-hosted runner — `full` posture at zero minutes** (the power option for an always-on spare box): repo Settings → Actions → Runners → "New self-hosted runner", run GitHub's installer on the spare machine (as a service for unattended), then retarget jobs — `runs-on: [self-hosted, Linux]`, or swap the matrix entries. Self-hosted runners "are free to use with GitHub Actions, but you are responsible for the cost of maintaining your runner machines" (https://docs.github.com/en/actions/hosting-your-own-runners/managing-self-hosted-runners/about-self-hosted-runners, accessed 2026-06-12). **Security boundary: "Self-hosted runners should almost never be used for public repositories"** — any fork PR can execute code on your machine (https://docs.github.com/en/actions/security-for-github-actions/security-guides/security-hardening-for-github-actions, accessed 2026-06-12). A private autopilot repo is the natural fit.

The posture **and gating** are chosen at onboarding by the setup interview (`onboard` Mode A) and recorded in `PROJECT_CONVENTIONS.md` › Operating posture. This template repo itself runs the unset-`light`/`blocking` default — it's public (minutes free either way), but it dogfoods the configuration downstream copies inherit.

## 7. Path-scoped rules — `.claude/rules/` `[production-proven]` *(wired — one rule; verified live 2026-07-18, #376)*

Native Claude Code memory machinery (https://code.claude.com/docs/en/memory, accessed 2026-07-18; surveyed in `research/notes/agent-project-systems.md`): a `.claude/rules/*.md` file with `paths:` frontmatter (a glob list) injects only when a matching file is read — subsystem conventions at **zero standing token cost**. A rule *without* `paths:` loads at launch with CLAUDE.md priority, i.e. it's just CLAUDE.md with extra steps — don't ship one. Shipped: [`scripts-conventions.md`](../.claude/rules/scripts-conventions.md) (scripts / workflows / hooks). House stance (#307):

- **Complement, don't subsume — ratified on new grounds (#376).** Rules injection is **verified live**: on Claude Code CLI 2.1.214/Linux the `scripts-conventions.md` rule arrived with a matching `scripts/` Read — mid-session, in a window resumed from `/compact` minutes earlier (2026-07-18), so the on-match trigger survives compaction and the earlier "launch-snapshotted" reading is dead (routed from #267's line-117 finding). The dir-scoped `research/CLAUDE.md` and `textbooks/CLAUDE.md` reminders stay anyway: conversion buys nothing (both mechanisms are zero-standing-cost on-touch triggers) and costs coverage — rules never reach independent subagents (next bullet), while research/ is exactly where worker fan-outs read. Equal price, narrower reach — declined.
- **Rules never reach independent subagents** (docs) — worker contracts stay in `.claude/agents/` (#309); a rule is main-session context only.
- **Frontmatter sanity is audit-gated** (`audit_ops_config` check 7): an invalid or empty `paths:` glob matches nothing silently (v2.1.207 semantics) — the same silent-dark-file class the skills-catalog audit kills for skills.
- **Authoring**: pointer-not-copy (the `research/CLAUDE.md` reminder pattern), and the **derivability test** decides what lives in always-loaded CLAUDE.md vs a rule (`audit_over_engineering` › derivable): keep pitfalls, rationale, deltas-from-default; what the repo can answer is bloat.

## 8. The session-close contract — the landing block `[production-proven]` *(wired — enforced by `Stop`, #749)*

The single home of the hand-back format. Owner-reported across several of his projects (2026-07-29): *"I get left with text dumps with no clear direction and it's confusing to try and untangle what it needs from me (if it even needs anything from me), what was done in simple terms, and what the next steps should be… it takes me time to figure out if I'm supposed to tell it to continue or prepare to compact."*

**Every turn that used a work tool ends with these four fields, in this order.** The order is the owner's own (he wrote it after declining all three surveyed options, #749) — the verdict lands **last**, where a conclusion goes, not first.

| Field | What it owes |
|---|---|
| `**Session**` | What changed **and why it was built that way**, in plain speak. His example of the register: *"we added machinery since the prose kept being ignored and was wasting tokens when agents tried to fix the issue."* A file list is not this — git already has the file list; the **judgement** is the deliverable. |
| `**Cost**` | `python3 scripts/slice_telemetry.py status` — spend + context %. Both come from the statusline's per-session snapshot, which already computed them; the owner works from a **phone**, where the statusline that displays them is invisible, so without this field he has to type `/usage` to decide anything. |
| `**Needs you**` | The ask, or the explicit word **"nothing"**. Silence is not an answer — "if it even needs anything from me" is half the reported complaint. |
| `**Next**` | `continue here: <why>` or `compact: <why>`. The one thing he cannot infer, and inferring it is what he asked us to stop making him do. |

**The verdict rule (his):** context **under 25%** *and* a next slice that is small or needs this one's context ⇒ **continue here**; otherwise **compact**. The 25% is about **output quality** — *"I don't want to get near max context since the model gets dumber with too much context"* — **not** about running out of room. Don't let it drift into a compact-when-full rule; that fires far later and gives back the quality he's buying. Other continue-reasons are legitimate: he gave these as the daily pattern, not an exhaustive list.

**Read the context figure; never estimate it (#765).** It comes from `slice_telemetry.py status` — the same command the **Cost** field already owes — and *never* from a sense of how much the session has done. An agent has no introspective access to its residual window: everything it can feel (pages read, hours worked, lines written) tracks **effort**, and a compaction resets the window without resetting the feeling. The error is therefore one-directional and always expensive — over-estimating throws away a good window and pays a full re-onboard to get back, while under-estimating is caught by the harness. Measured twice: ~50% felt against 20% actual at #729 (three slices shipped in the window that would have been discarded), and ~28% felt against 15% at #777, one message from a needless `/compact`. The `Stop` hook holds the stated figure to the ledger's, because this is the second half of a rule whose first half was already mechanical — #487's opener line derives its ticket count from `ready_work.py`, while the context figure was left to a guess.

A `compact` verdict ends the session, so it owes two more things, both checked: the message actually carries the fenced `/compact` block (*"I run into issues where it hasn't prepared a compaction message"*), and the turn **surveyed** him. `prepare_compaction` owns the compaction procedure and this contract routes to it — it is not a second copy.

**Survey cadence (#749).** End of session: **always**, standing question = what's next over the ready set — a guaranteed steering opportunity, not only when something is blocked. Mid-slice: when the agent **discovers** it needs the owner (a real fork, a discovered blocker, a denied tool call — the denial protocol in `CLAUDE.md` › Working style is the special case this generalizes). Not owed for every default the agent could reasonably pick itself; he declined that variant explicitly.

| moment | landing block | survey |
|---|---|---|
| hand-back after real work | yes | no |
| session end | yes | yes — "what's next" standing |
| mid-slice, genuine fork or blocker | n/a | yes |
| pure-conversation turn (a question answered, nothing done) | no | no |

**Why this is machinery and not a paragraph.** All of it already existed as prose — `prepare_compaction` carries a proactive "run me unprompted at a clean checkpoint" contract *and* a `next session:` opener line (#487) that answers the does-this-wait-on-you question exactly. It sat behind a skill a session had to **choose** to invoke, and sessions didn't. A failure of prose is not repaired by more prose, which is why the fix is a `Stop` hook and why that reasoning is itself the worked example in the `**Session**` row above.

