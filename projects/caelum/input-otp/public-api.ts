/**
 * Secondary entry point `caelum/input-otp` (#303) — the one-time-code input (`p-inputOtp` parity),
 * the segmented member of the M3 input family (sibling of `cae-input-number` #301). *N* single-char
 * cells presenting as one `string` CVA value, with roving-tabindex focus and paste/autofill spread;
 * see {@link CaeInputOtp}.
 */
export * from './input-otp';
