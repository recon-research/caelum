/**
 * Public API for the `caelum/password` secondary entry point (#304) — `cae-password`, the input
 * family's password member (`p-password` parity): a `matInput[type=password]` with an inline
 * visibility toggle + an advisory strength meter, extending `CaeFormFieldControlBase`. Sibling of
 * `cae-input-number` (#301) and `cae-input-otp` (#303).
 */
export * from './password';
