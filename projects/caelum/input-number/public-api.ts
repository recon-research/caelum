/**
 * Secondary entry point `caelum/input-number` (#301) — the dedicated numeric text input
 * (`p-inputNumber` parity), first member of the M3 input family (mask/otp/password follow in
 * #302–#304). A `number | null` CVA over `Intl.NumberFormat`; see {@link CaeInputNumber}.
 */
export * from './input-number';
