/**
 * Public API for the `caelum/input-mask` secondary entry point (#302) — `cae-input-mask`, the
 * input family's fixed-template masked text input (`p-inputMask` parity): a component extending
 * `CaeFormFieldControlBase` whose CVA value is the **unmasked** string while the view shows the
 * mask. Last member of the input family — sibling of `cae-input-number` (#301),
 * `cae-input-otp` (#303), and `cae-password` (#304).
 */
export * from './input-mask';
